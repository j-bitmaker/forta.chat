# Баги звонков, которые упираются в действия владельца проекта

Это то, что нельзя ни воспроизвести, ни проверить на текущем стенде — два
эмулятора Pixel (API 35), Maestro, Vitest, JVM-тесты. Всё остальное чинится без
твоего участия; чем что воспроизводится — см. `call-bug-reproduction-matrix.md`.

Цифры получены прогоном `scripts/triage-call-reports.mjs` по выгрузке открытых
задач: **83 открытых отчёта о звонках**. Кластеры пересекаются — один отчёт может
попасть в несколько.

> **История цифр, потому что я дважды ошибся.** Сначала было 334 отчёта, потом
> 84 открытых. Затем я объявил, что верное число — 63, и списал разницу на
> дата-сет, снятый до коммита `8557404a`. Это было неверно: 63 получались
> из-за дефекта в самом фильтре `isCallRelated`. В нём не было **ни одного
> русского слова про слышимость** — только английские `hear` / `microphone` /
> `mic` / `speaker`, — поэтому «Не слышу собеседника», самая частая формулировка
> во всём корпусе, набирала ноль совпадений и отбрасывалась до кластеризации.
> «микрофон» не совпадает с `mic` ни одной буквой. Плюс отсечка голосовых
> сообщений была мёртвым кодом: она выполнялась уже после раннего возврата и
> всегда давала `true`. Оба дефекта починены, отсечка голосовых теперь работает
> по-настоящему. **Верное число — 83**, то есть исходные 84 были почти точны, а
> мои 63 — ошибка. Нераспознанными остались четыре отчёта: в трёх нет ни одного
> симптома (#1023, #906, #1138), четвёртый — iOS (#538).

---

## B. Нужен реальный OEM-аппарат — 49 отчётов

| Кластер | Открытых |
|---|---|
| нет звука / односторонний | 38 |
| динамик не переключается | 8 |
| эхо, слышно самого себя | 3 |

Эти дефекты живут в коде вендорской прошивки: сломанный аппаратный эхоподавитель,
застревающий глобальный флаг заглушки микрофона, асинхронный сброс режима связи
через 1–5 секунд, privacy shield в MIUI, игнорирование обоих API переключения
динамика. На образах Google соответствующего кода физически нет — воспроизводить
нечего.

**Отдельно и важно.** После замены вендорского списка на рантайм-пробу
(`3cc4feee`, `5cd50126`) осталась ровно одна проверка, которую нельзя сделать
никак иначе — **не срабатывает ли проба ложно на аппарате с исправным
эхоподавителем**. Проба использует сессию 0, к которой не привязан живой захват,
и HAL, включающий подавление только против реального потока, может ответить «нет
контроля». Направление ошибки безопасное (лишнее включение микрофона ничего не
ломает), поэтому значение намеренно не заведено в выбор AEC-движка — но
подтвердить это надо на железе.

**Что покупать.** Не флагман. Жалобы концентрируются на бюджетном сегменте: у
Samsung модели SM-A107F и SM-A155F дают 8 отчётов из 30, тогда как на всю линейку
S приходится 6. Ориентир — A-серия Samsung или бюджетный Xiaomi, **Android 11–13,
WebView 146 и новее**.

**Почему подойдёт почти любой реальный телефон.** Решает не марка, а WebView: 73%
отчётов приходят с версий 146–151, а эмулятор работает на 124 — двадцать с лишним
версий Chromium разницы ровно в том слое, где живёт WebRTC. WebView обновляется
через Play и одинаков у всех вендоров. Специфику MIUI (автозапуск, удержание в
памяти) Samsung, разумеется, не покроет.

---

## C. Нужен реальный аппарат **и** push — 6 отчётов

Кластер кнопки приёма: Xiaomi ×3, Honor, Infinix, Tecno, Itel — **ни одного
Samsung**.

Три гипотезы уже отработаны на эмуляторе и отброшены: двойной тап (защита держит,
флоу `06-call-answer-doubletap.yaml` проходит), серия звонков подряд, приём при
заблокированном экране. Остались приём из шторки уведомления и приём при спящем
процессе — оба живут в push-пути.

**Оговорка.** Разбор кода показал два дефекта, каждый из которых сам по себе даёт
симптом «нажимаю принять — звонок сбрасывается», и оба чинятся без аппарата:
рингтон, переживающий ответ и через 30 секунд кладущий трубку, и подмена
единственного слота звонка исходящим вызовом во время ринга. Возможно, после этих
правок кластер закроется сам. Проверить — только на аппарате.

---

## D. Нужен `google-services.json` — 2 отчёта

Входящий звонок доходит только при запущенном приложении.

Это **не** узкое место, каким казалось вначале. Объективная проверка: из 62
отчётов с диагностикой push-приглашения дошли у **49** — доставка работает,
проблема в обработке. Явное упоминание фона или закрытого приложения есть лишь у
двух отчётов.

Что даёт файл: воспроизведение входящего при полностью выгруженном процессе,
замер задержки доставки, проверка отклонения из шторки и протухания приглашений
по TTL.

---

## E. Нужен APK Bastyon — 5 отчётов

**Проверено 2026-09-18 на Samsung SM-A528B:** Bastyon 1.8.124 и Forta вошли одним аккаунтом (`Testtest11223344`),
звонит веб TEST1. Скрипты `scratchpad/run-dual-*.sh`, логи `scratchpad/runs/dual*`.
- Bastyon в фоне показывает только обычное уведомление; открытый Bastyon звонит своим рингтоном, а Forta поднимает
  свой экран поверх. Падений (#760) нет ни в одном из 11 звонков.
- Ответ в Forta: рингтон Bastyon замолкает через 0,1 с, микрофон Bastyon не берёт, звук в обе стороны есть
  (48 КБ от телефона, 53 КБ к телефону за 14 с). Односторонний звук (#847, #1112) не воспроизвёлся.
- Всплывающее уведомление Forta ложится поверх карточки звонка Bastyon, и «Ответить» Forta оказывается там же, где
  кнопка ответа Bastyon: нажатие, задуманное для Bastyon, отвечает в Forta (`dualb3`, `dualb4`) — это #809 п. 1. Решение владельца 2026-09-18: оставить как
  есть.
- Ответ в Bastyon при живой странице Forta: Forta закрывает свой звонок через ~2,5 с по `onAnsweredElsewhere`.
- Ответ в Bastyon, когда страница Forta заморожена в фоне: Forta звонит, пока человек уже говорит в Bastyon, до
  таймаута рингера (30 с) — это #809 п. 2. Сигнал «ответили на другом устройстве» приходит только через синхронизацию
  JS, а пушем не приходил. Владелец выбрал вариант А (2026-09-18): приложение ставит правило пушей для
  `m.call.select_answer`, нативная часть гасит звонок, если на него ответили не на этом телефоне. Проверено с владельцем 2026-09-18
  (`ownerb3`): после ответа в Bastyon рингер Forta снят через 28 мс после пуша (`manual-verification.md`, «Forta
  перестаёт звонить, когда на звонок ответили на другом устройстве»).
- Найдены и починены две ошибки Forta, от Bastyon не зависящие: после отмены звонящим (`e059cc1e`) и после отклонения
  по таймауту (`ffeb0a46`) замороженная страница регистрировала звонок заново, и телефон звонил второй раз 30 с.
- Bastyon не убирает свои уведомления о звонке после его конца — это поведение Bastyon.


#847, #809, #760, #1091, #1112 — на устройстве установлены и авторизованы оба
приложения, звонок приходит дважды либо не проходит вовсе.

Логика дедупликации покрыта юнит-тестами, но полный сценарий требует второго
приложения рядом: гонка «FCM против Matrix-sync» между двумя получателями
воспроизводится только так.

---

## F. Не Android — 3 отчёта

- Веб: #936 (размер картинки в видеозвонке ~30% экрана), #1047 (посторонний
  контакт после регистрации — вообще не про звонки).
- iOS: #538.

Отдельные поверхности, к Android-ветке отношения не имеют.

---

## G. Второй входящий во время разговора — закрыто решением владельца

**Решение владельца 2026-09-17: закрыть.** Поведение остаётся как есть: пока идёт разговор, второму звонящему приходит
отказ, ждущего вызова нет. Жалоб пользователей на это нет; опасная часть (второй входящий рвал живой разговор) починена
раньше. Ниже — история вопроса.


Не из отчётов — найдено при разборе кода, но путь достижимый и последствия
тяжёлые, поэтому фиксирую здесь, а не молча оставляю.

**Что в коде.** `CallConnectionService.currentConnection` — один глобальный слот
на всё приложение. Ни один из двух вызывающих `addNewIncomingCall`
(`FortaFirebaseMessagingService`, `CallPlugin.reportIncomingCall`) не проверяет,
идёт ли уже разговор. Значит второй входящий доходит до
`onCreateIncomingConnection` и вытесняет из слота соединение живого разговора.

**Что уже сделано.** Второй входящий больше не вытесняет живой разговор из
слота: `onCreateIncomingConnection` отвечает `BUSY` и оставляет слот за текущим
звонком, так что `reportCallEnded` до него по-прежнему достаёт и аудиорежим
сбрасывается. Экран входящего, который FCM поднимает отдельно от Telecom, тоже
обезврежен — его «Отклонить» и 30-секундный авто-сброс больше не кладут трубку
установленному звонку (`DisplacedConnectionPolicy`). С 2026-09-15 пуш этот экран
во время разговора не поднимает вовсе: сначала смотрит слот Telecom и только
передаёт звонок в JS, который отвечает отказом.

**Что осталось.** Звонящему B отвечает «занято» — ждущего вызова нет. И
`reportCallEnded` по-прежнему не привязан к `callId`: пока слот один, это
безопасно, но любая схема с двумя одновременными звонками потребует соединений
по `callId`.

**Почему нельзя дочинить без тебя.** Правильное решение — соединения по
`callId` вместо одного слота, и продуктовое решение о том, что вообще должен
видеть человек: ждущий вызов, автоматический отказ «занято» или ничего.
Оба варианта проверяются только на аппарате с двумя реальными звонящими —
на эмуляторе Telecom ведёт себя иначе, а поведение шторки и Bluetooth-гарнитуры
отличается у каждого вендора.

**Что нужно от тебя:** аппарат и ответ на вопрос «что должно происходить, когда
звонят во время разговора».

---

## H. `MAESTRO_E2E_TARGET_ROOM` в `.env` — чтобы E2E перестали падать

`scripts/e2e-android.sh --all` прогоняет четыре однодевайсных флоу. Два из них
(«Open a chat from the list», «Send a text message») падают не из-за
приложения, а потому что в `.env` не задан `MAESTRO_E2E_TARGET_ROOM` — Maestro
подставляет литерал `undefined` и ассерт не находит комнату. Скрипт теперь
предупреждает об этом по имени переменной до запуска, но подставить значение
может только владелец тестового аккаунта.

**Что нужно от тебя:** дописать в `.env` строку с названием (или частью
названия) комнаты, которая гарантированно есть в списке чатов у аккаунта из
`MAESTRO_E2E_PRIVATE_KEY`. Формат — в `.env.example`. Секрет это не содержит,
но и в репозиторий класть не надо.

После этого 4/4 однодевайсных флоу должны проходить. Флоу с тегом `call`
запускаются отдельно — `scripts/e2e-call.sh`, ему нужны два устройства.

---

## Сводка

| Группа | Что нужно от тебя | Отчётов |
|---|---|---|
| B | реальный OEM-аппарат: A-серия Samsung или бюджетный Xiaomi, Android 11–13, WebView 146+ | 49 |
| C | аппарат + `google-services.json` | 6 |
| D | `google-services.json` | 2 |
| E | APK Bastyon | 5 |
| F | веб / iOS — другая поверхность | 3 |
| G | — закрыто 2026-09-17: второму звонящему отказ, ждущего вызова нет | найдено в коде |
| H | одна строка в `.env` — `MAESTRO_E2E_TARGET_ROOM` | 2 падающих E2E |

Проверки уже сделанных фиксов, которые требуют человека, ведутся отдельно —
`manual-verification.md`.

## Разбор отчётов 2026-09-21

Открыто 508 отчётов, про звонки 104 (13 сентября было 489 и 98). Группы: «не слышно» 46, «завис после звонка» 41,
видео 16, двойной звонок 11, громкая связь 11, не соединяется 8, кнопка ответа 7, прочее 6, нет рингтона 4, качество 3,
входящий в фоне 2.

С 13 сентября подано 24 отчёта, 7 про звонки. Версии: 1.12.2 — 11, 1.11.1 — 9, 1.13.4 — 2; сентябрьские починки не
выпущены, поэтому ни один отчёт их не отражает. Новых видов ошибок звонков нет: #1375, #1370, #1383, #1377 — «не
слышно» (группа B, нужен аппарат), #1386, #1379 — телефон остался в режиме звонка (`MODE_IN_CALL`,
`MODE_IN_COMMUNICATION`), #1376 — только громкая связь, #1388 — просьба о выключателе входящих звонков.

Не про звонки, но заметно:
- Файлы под Tor: #1387, #1371, #1367, #1382 (не отправляются видео, не грузятся фото и видео, не открываются файлы из
  Bastyon) — у всех Tor включён; совпадает с починкой `36c9a6e9`.
- «После редактирования сообщение становится зашифрованным»: 11 отчётов от 4 человек (#1300 … #1381). Найдено,
  починено и проверено 2026-09-21 — правка в группе теряла `hash` общего ключа; запись «Отредактированное сообщение в
  группе не становится „[encrypted]“» в `manual-verification.md`.
- «Приходят зашифрованные сообщения»: #1389, #1368 (один пользователь), #1385, #1373, #1372, #1369 (один пользователь),
  #1381 и #1367 (отредактированные сообщения) — 8 отчётов от 3 человек за неделю. Вне работы по звонкам; нужен
  отдельный разбор.

