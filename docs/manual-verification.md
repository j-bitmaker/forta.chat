# Фиксы, ждущие ручной проверки

Сюда попадает фикс, корректность которого **нельзя подтвердить автоматически** —
тестами, типами или сборкой. Обычно это означает: нужен реальный аппарат, нужен
взгляд человека на экран, или проверяется поведение при обновлении поверх старой
установки.

Список **пополняется тем же коммитом, что и фикс** — см. правило в `CLAUDE.md`,
раздел «Верификация перед коммитом». Проверил — поставь дату и перенеси запись
вниз, в «Проверено»; удалять не надо, история проверок полезнее пустого списка.

Формат записи:

```
### Название
- Коммит: e67f3863
- Почему нужен человек: <что именно не проверяется автоматически>
- На чём: <эмулятор / реальный аппарат / конкретная модель>
- Шаги: <по пунктам>
- Статус: ☐ не проверено
```

---

## Ожидают проверки

### Разговор переживает завершение предыдущего звонка
- Коммит: `00ddc636`
- Почему нужен человек: проверяется гонка жизненного цикла сервиса — ОС решает,
  когда выполнить отложенный `onDestroy`. Воспроизводится только на реальном
  планировщике, и охотнее на прошивках, которые тормозят завершение сервисов.
- На чём: реальный аппарат, желательно Xiaomi / HONOR / realme.
- Шаги:
  1. Завершить звонок и **сразу** (в течение секунды-двух) начать новый.
  2. Второй звонок обязан соединиться и сохранить звук — старый экземпляр сервиса
     не должен сбросить аудиорежим и закрыть соединения нового.
  3. Повторить 5–10 раз: гонка не срабатывает каждый раз.
- Измерено 2026-09-10 на Samsung SM-A528B ↔ веб TEST1, Wi-Fi, сборка
  `01fedeed` (`scratchpad/run-redial.sh`, логи `scratchpad/runs/redial-*`,
  `redial2-*`): две серии по 10 циклов «Samsung кладёт трубку с экрана звонка и
  сразу набирает снова». Новый `launchCallUI` шёл через 1,6–2,2 с после
  `endCall` предыдущего звонка; исключение — набор после несоединившегося
  цикла, там скрипт сам ждал 5 с. Соединились 10 из 10 во второй серии и 9 из
  10 в первой. В каждом соединившемся звук шёл в обе стороны: через 7 с после
  соединения у телефона 18–23 КБ входящего звука при `totalAudioEnergy`
  1,6–2,3, у веба входящий поток 2,9–3,9 КБ/с с ненулевой энергией. Через 30 с
  после каждой серии — `MODE_NORMAL`.
  Несоединившийся цикл 4 первой серии — не эта гонка. На телефоне invite ушёл
  через 3,3 с после `endCall`, Telecom перевёл звонок в DIALING, и до нашего
  отбоя через 48 с других `endCall` не было. Сервер держал hangup прошлого
  звонка 12,7 с и invite 9,3 с, а у веба `handleIncomingCall` так и не
  появился. Почему — узкий фильтр лога веба тогда не сохранил, строк SDK в нём
  не было. Во второй серии фильтр расширен; задержка сервера повторилась
  слабее (5,1 с на hangup, 2,0 с на invite), и звонок соединился за 13 с.
  **Чего это не доказывает:** строк `ACTION_STOP for start generation … ignored`
  и `… on a superseded instance` ноль в обеих сериях. Отложенный `onDestroy`
  старого экземпляра на Samsung ни разу не наложился на новый звонок, то есть
  сама гонка не сработала. Нужен Xiaomi / HONOR / realme.
- Статус: ☐ не проверено

### Зависший звонок отпускается при возврате в приложение
- Коммит: `00ddc636`
- Почему нужен человек: нужен именно тот случай, когда таймер соединения не успел
  сработать — Doze, замороженный процесс или заблокированный полноэкранный
  интент. На эмуляторе Doze симулируется, но поведение Telecom отличается.
- На чём: реальный аппарат.
- Шаги:
  1. Дождаться входящего звонка с заблокированным полноэкранным интентом (или
     заблокировать его в настройках уведомлений) и **не** трогать уведомление.
  2. Дождаться, пока звонящий положит трубку, свернуть приложение на минуту.
  3. Открыть приложение. В `adb logcat -s CallConnectionService` должна быть
     строка про освобождение зависшего соединения.
  4. Проверить регулировку громкости медиа — она обязана работать. До фикса
     ползунок оставался в режиме звонка.
- Разобрано 2026-09-10, на стенде (Samsung по USB) **воспроизвести нечем**, и
  вот почему — чтобы следующий не тратил прогон: подметание требует соединения
  в состоянии `STATE_RINGING`, у которого `ringingSinceMs` старше 45 с
  (`StaleCallPolicy.isStaleRinging`). До этого возраста его гасит любой из двух
  таймеров главного лупера — 30-секундный дедлайн `IncomingRinger` и
  45-секундный `armRingTimeout`. Чтобы не сработал ни один, процесс должен быть
  заморожен или CPU уснуть: Doze через `dumpsys deviceidle force-idle` на
  подключённом по USB аппарате не даёт CPU уснуть, а `am force-stop` убивает и
  само соединение вместе с процессом. Нужен настоящий сон устройства (аппарат
  без провода, экран выключен) либо прошивка, которая замораживает процесс —
  то есть человек и время, а не adb.
- Статус: ☐ не проверено

### Проба эхоподавителя не срабатывает ложно на здоровом аппарате
- Коммиты: `3cc4feee`, `5cd50126`
- Почему нужен человек: проба обращается к аудио-HAL с сессией 0, к которой не
  привязан живой захват. HAL, включающий подавление только против реального
  потока, может ответить «нет контроля» — и здоровый аппарат будет помечен как
  сломанный. На эмуляторе этого слоя нет вообще.
- На чём: реальный аппарат **вне** списка `BROKEN_HW_AEC_VENDORS` — Samsung,
  Pixel, OnePlus, Motorola.
- Шаги:
  1. Поставить сборку, позвонить.
  2. `adb logcat -s AudioRouter` — найти строку решения о микрофоне.
  3. Убедиться, что на здоровом аппарате принудительное включение микрофона
     **не** применяется, и что звук в разговоре нормальный, без эха.
- Измерено 2026-09-10 на Samsung SM-A528B (лог
  `scratchpad/runs/audio-143137.log`) — и ожидание шага 3 оказалось строже,
  чем контракт кода:
  ```
  14:32:15.728 NativeWebRTCManager: Audio processing: vendor=samsung brand=samsung hardwareAEC=true
  14:32:29.015 AudioLifecycle: [vendor=SAMSUNG] explicit setMicrophoneMute(false) on start
  ```
  То есть путь WebRTC оставил аппаратный AEC (это главное — на здоровом
  Samsung софтверный AEC не навязан), а вот проба `queryHardwareAecAvailability`
  всё-таки ответила «нет контроля» и включила вендорную поблажку с unmute.
  Сам `AudioRouter` это и предсказывает в комментарии: проба «can say a healthy
  device has broken HW AEC, never the reverse», и это считается безопасным,
  потому что единственное следствие — `setMicrophoneMute(false)`, который
  только освобождает захват. Проверено, что конфликта с нашим mute нет:
  `setMicrophoneMute` встречается **только** в `AudioRouter`, приложение глушит
  микрофон через WebRTC-дорожку.
  **Остаётся человеку:** слышимость и отсутствие эха.
- Статус: ☐ не проверено

### Пустые каналы уведомлений не возвращаются после обновления
- Коммит: `fb0e6650`
- Почему нужен человек: проверяется путь обновления поверх установки, где старые
  каналы уже созданы. Чистая установка этот сценарий не воспроизводит.
- На чём: аппарат или эмулятор со **старой** сборкой, поверх которой ставится новая.
- Шаги:
  1. Поставить старую сборку, запустить, дойти до чата — каналы `messages` и
     `calls` появятся.
  2. Обновиться на новую сборку, запустить, свернуть, запустить снова.
  3. Настройки → Приложения → Forta Chat → Уведомления: пустых каналов быть не
     должно.
  4. Позвонить на этот аппарат — входящий обязан звонить, канал
     `incoming_call_v2` не затронут.
- Измерено 2026-09-10 на Samsung SM-A528B, после установки сегодняшней сборки
  поверх предыдущих: `dumpsys notification` знает у `com.forta.chat` ровно два
  канала — `active_call` («Активный звонок», importance 2) и `incoming_calls`
  («Входящие звонки», importance 4). Ни `messages`, ни `calls` — легаси-идшники
  из `LEGACY_*_CHANNEL_IDS` — на аппарате не заведены, пустых каналов нет.
  Входящий звонок в том же прогоне звонил.
  **Чего это не доказывает:** `messages_v2` и `incoming_call_v2` создаёт
  FCM-сервис, а на sideload-сборке пуша нет (`google-services.json`
  отсутствует), поэтому именно путь «v1.x создал `calls` → новая сборка его
  удалила» тут не воспроизводится. Нужна пара «release-сборка v1.x → release из
  CI».
- Статус: ☐ не проверено

### В «Аккаунтах вызовов» значится Forta Chat
- Коммит: `9e5adff6`
- Почему нужен человек: экран системный, автотестом не читается.
- На чём: реальный аппарат — на эмуляторе раздел бывает скрыт.
- Шаги:
  1. Настройки → Приложения → Аккаунты вызовов (путь зависит от прошивки).
  2. Убедиться, что запись называется «Forta Chat», а не «Bastyon Chat».
  3. **Важно:** проверить именно на аппарате, где приложение стояло до обновления —
     идентификатор аккаунта намеренно не менялся, входящие должны работать как
     раньше.
- Пробовал закрыть через adb 2026-09-10 — не выходит, и вот почему, чтобы не
  пробовать снова: `dumpsys telecom` (в том числе с `--noredact`) печатает
  аккаунт как `ComponentInfo{com.forta.chat/…CallConnectionService}, ***` —
  подпись скрыта, в дампе её нет вовсе. Системный экран по
  `android.telecom.action.CHANGE_PHONE_ACCOUNTS` на этой прошивке показывает
  только SIM-аккаунты («SIM 1», «SIM 2», «Приоритетная SIM-карта для вызовов»),
  self-managed в него не попадают. Нужен человек и путь Samsung-настроек.
  Косвенно: аккаунт зарегистрирован и рабочий — все звонки этой сессии прошли
  через него.
- Измерено 2026-09-12 на Samsung SM-A528B (Android 14), сборка `f83b30e7` с FCM,
  с разрешения владельца. Экраны открывал через adb, текст читал дампом интерфейса.
  - «Учетные записи для вызовов» (`CHANGE_PHONE_ACCOUNTS`, внутри «Настроек
    вызовов» Samsung): «Приоритетная SIM-карта для вызовов», «SIM 1», «SIM 2» и
    пункт «Активные уч. записи для вызовов».
  - «Активные уч. записи для вызовов» открывает «Аккаунты для звонков», тот же
    экран, что `com.android.server.telecom/.settings.EnableAccountPreferenceActivity`.
    Список пуст.
  - `SHOW_CALL_ACCOUNT_SETTINGS` на этой прошивке никто не обрабатывает. В самих
    «Настройках вызовов» пункта аккаунтов нет, есть только «Другие параметры
    вызова» и «Дополнительные услуги».
  - Итог: self-managed аккаунт Forta Chat на Samsung не показан ни в одном списке
    аккаунтов для звонков, поэтому шаги 1–2 здесь не проверить. Шаг 3 не
    проверялся: неизвестно, стояла ли на аппарате сборка старше `9e5adff6`.
  - Нужен аппарат, прошивка которого показывает self-managed аккаунты.
- Статус: ☐ на Samsung не проверяется — self-managed аккаунты скрыты (2026-09-12)

### Tor работает после удаления libconjure.so
- Коммит: `9b425964`
- Почему нужен человек: автотест подтверждает лишь отсутствие ссылок в коде, но не
  работоспособность демона.
- На чём: реальный аппарат, желательно в сети с блокировками.
- Шаги:
  1. Включить Tor в настройках, дождаться статуса «работает».
  2. Отправить сообщение и позвонить через Tor.
  3. Проверить подключение мостов obfs4 и snowflake.
- Измерено 2026-09-10 на Samsung SM-A528B (Android 14), Wi-Fi, сборки
  `87e8dd1b` и `a9bb63ca`. Логи `scratchpad/runs/tor-*` и `tor-fixed2-*`,
  скрипты `scratchpad/run-tor.sh` и `run-tor2.sh`: Tor включается через стор
  приложения, как переключателем в настройках.
- Шаг 1: демон без `libconjure.so` поднимается. Режим «Всегда» на ru-локали
  сам выбирает Snowflake:
  ```
  19:52:20.711 TorManager: [BOOT] T+835ms torrc written mode=ALWAYS bridge=SNOWFLAKE snowflakeBinExists=true
  19:54:01.349 TorManager: [BOOT] T+101475ms Bootstrap 100%
  19:54:01.351 TorManager: [BOOT] T+101478ms Tor ready, starting reverse proxy
  ```
  Во втором запуске — 100% за 67 с. Оба раза проверка выхода вернула
  `isTor: true`.
- Шаг 2 на `87e8dd1b` провалился: service worker ронял каждый запрос с телом,
  сообщение не дошло, звонок не соединился (запись «Запросы приложения идут
  через Tor на Android»). На `a9bb63ca` сообщение с телефона веб получил через
  6 с, счётчик «Всего, Tor» вырос с 0 до 43 947 байт при неизменном прямом, а
  звонок соединился через 8 с после набора. Ошибок `duplex` — 0 на 38
  запросов через Tor.
- Шаг 3: Snowflake работает (выше). Без моста демон за 7 минут не ушёл дальше
  `Bootstrap 10%` — прямой Tor в этой сети, видимо, блокируется, и о демоне это
  не говорит. obfs4 из приложения не выбрать: `TorBridgeType` — только
  `none | snowflake`, хотя `libobfs4proxy.so` в APK есть. obfs4 и сеть с
  блокировками остаются непроверенными.
- Статус: ☐ не проверено

### Переключатель движка WebRTC меняет поведение
- Коммит: `d0261dc2`
- Почему нужен человек: смысл переключателя — сравнить два движка на конкретном
  проблемном аппарате. На эмуляторе сравнивать нечего.
- На чём: аппарат, на котором воспроизводится жалоба на звук.
- Шаги:
  1. Настройки → раздел звонков → переключить движок на WebView.
  2. Позвонить, оценить звук.
  3. Вернуть нативный, позвонить снова.
  4. Отправить отчёт о проблеме — выбранный движок должен попасть в диагностику.
- Статус: ☐ не проверено

### Таймлайн аудио доезжает в реальном отчёте
- Коммит: `e8b1954c`
- Почему нужен человек: проверяется сквозной путь до задачи в трекере, а не только
  формирование строки.
- На чём: любой аппарат или эмулятор с настроенной отправкой отчётов.
- Шаги:
  1. Позвонить, завершить звонок.
  2. Отправить отчёт о проблеме.
  3. В созданной задаче найти секцию с таймлайном аудио и убедиться, что события
     режима и маршрутизации записаны.
- Статус: ☐ не проверено

### Второй звонок перерисовывает экран входящего
- Коммит: `e3079e09`
- Почему нужен человек: нужны три аккаунта — на аппарат звонит A, и пока экран
  входящего висит, звонит B. На стенде из двух эмуляторов третьего нет, а
  привязка вьюх Android-юнит-тестом не проверяется.
- На чём: реальный аппарат + два звонящих аккаунта.
- Шаги:
  1. Позвонить с аккаунта A, не отвечать — экран входящего на месте.
  2. Через 10–15 секунд позвонить с аккаунта B на тот же аппарат.
  3. На экране обязано смениться имя, тип звонка и инициалы на аккаунт B,
     а обратный отсчёт — начаться заново с 30. До фикса `setIntent()` подменял
     звонок «под капотом»: на экране оставался A, а зелёная кнопка отвечала B.
  4. Нажать «Принять» — соединиться должно именно с B.
- Измерено 2026-09-10 на Samsung SM-A528B (Android 14), три стороны: веб TEST1
  (A) и веб `test23438111` (B). Сборка `f83b30e7` с FCM, приложение на экране
  (JS жив), `USE_FULL_SCREEN_INTENT` не выдан. Логи `scratchpad/runs/rebind2-*`,
  скрипт `scratchpad/run-rebind.sh`: B заранее открывает чат и звонит по
  файлу-триггеру через 8 с после появления рингера A. Первый прогон
  (`rebind1-*`) не засчитан: B дозвонился через 7 с после того, как рингер A
  сам отклонил звонок на 30-й секунде.
  - Шаги 1–2 проходят: рингер A на экране (`test3823818`), B позвонил через
    21 с после начала звонка A.
  - Шаг 3 проходит в той части, что видна этой парой аккаунтов. В 22:47:41.653
    в логе `onNewIntent: second call 1789069660105wNd11mr5fMJ9bMkp displacing
    1789069639285hgNn3nMdc8lxRzqJ on screen`. Через 3 с на экране
    `test23438111` и отсчёт `28s` — у A оставалось бы ~7 с. Инициалы у обоих
    аккаунтов одинаковые («TE»), тип звонка у обоих голосовой, так что их смену
    не увидеть.
  - **Шаг 4 провален: пока приложение открыто, «Принять» не может соединить
    с B.**
    ```
    22:47:41.397 I/Capacitor/Console: [call-service] handleIncomingCall: callId=1789069660105wNd11mr5fMJ9bMkp, roomId=!NDxNWedLDBbmFoqirc:matrix.pocketnet.app, type=voice
    22:47:41.401 I/Capacitor/Console: [call-service] handleIncomingCall: already in call, rejecting
    22:47:41.653 D/IncomingCallActivity: onNewIntent: second call 1789069660105wNd11mr5fMJ9bMkp displacing 1789069639285hgNn3nMdc8lxRzqJ on screen
    22:47:41.739 E/CallConnectionService: onCreateIncomingConnectionFailed
    22:47:41.760 E/CallConnectionService: onCreateIncomingConnectionFailed
    22:47:52.211 I/Capacitor/Console: [call-service] rejectCall: invoked, hasCall=true
    22:47:52.261 D/IncomingCallActivity: Dismissing incoming call screen (remote hangup)
    22:47:52.262 D/CallConnection: onDisconnect: 1789069639285hgNn3nMdc8lxRzqJ
    ```
    - JS отклонил B через 4 мс после приглашения по `/sync`: `hasLiveCall` в
      `call-store.ts` считает живым и ещё звонящий A. Экран перерисовался на B
      через 250 мс после отказа — пользователю показан звонящий, которому уже
      отказано, со свежим отсчётом.
    - Telecom не принял регистрацию B (`onCreateIncomingConnectionFailed` ×2),
      слот по-прежнему держит A в RINGING. Комментарий в `onNewIntent` («its
      Telecom connection was still RINGING, and onCreateIncomingConnection
      released it») здесь не выполняется: до `onCreateIncomingConnection` B не
      дошёл.
    - Нажатие в этом окне никого бы не соединило. `IncomingAcceptPolicy`
      отвечает слоту только с тем же id, поэтому «Принять» ушло бы в JS как
      ответ на B, а B JS уже отклонил.
    - В 22:47:52, через 32 с после приглашения A, сработал 30-секундный таймер
      JS для A (`call-service.ts`, «Auto-reject after 30s»). Он отклонил A и
      закрыл рингер, показывавший B, — через 11 с после перерисовки вместо
      обещанных 30. Нажать было не на что (`no btn_accept on screen`); обоим
      звонящим пришёл отказ.
  - Вариант с убитым JS, когда рингер поднимает только пуш, не проверен. Сборка
    предупреждает, что без full-screen intent входящий будет только heads-up.
    Повторить на стадии, где владелец выдаёт это разрешение.
- Статус: ☐ шаг 4 провален 2026-09-10, нужна починка отдельным шагом
- После `27541373` звонок из другой комнаты экран не перерисовывает, и шаги 3–4
  этой записи верны только для повторного звонка из той же комнаты. Проверка
  новой логики — запись «Второй звонок из другой комнаты не забирает экран у
  звонящего».

### Аудиорежим сбрасывается без JS-финализации: убитый процесс, FCM-hangup, серия звонков
- Коммит: `07dd6ee8`
- Почему нужен человек: юнит-тесты доказывают, что каждый нативный хук завершения
  зовёт `CallTeardown.endCall`, а политика выдаёт нужные действия; androidTest —
  что `forceStop` возвращает `MODE_NORMAL` на эмуляторе. Не доказано: что
  OEM-прошивка (Samsung) не переустанавливает режим после нашего сброса, что после
  смерти процесса во время ринга не остаётся `MODE_RINGTONE`, и путь FCM-hangup
  при убитом процессе — push на эмуляторе нет. На эмуляторе подтверждено: штатное
  завершение → `actions=[]`; `kill -9` во время набора → новый процесс на
  cold-start увидел `audioMode=3` и открытый маркер и сделал
  `forceStop(teardown COLD_START)` через 1,1 с после смерти процесса; `kill -9`
  во время ринга → MODE_RINGTONE сброшен системой через 3 с, ничего не висит.
- На чём: Samsung и Pixel, CI-сборка (push). Шаги и логи — F25 в
  `docs/call-fix-checklist.md`.
- Шаги:
  1. Серия из 10 звонков Samsung↔Pixel: положил я / положил собеседник / не
     ответил 45 с / отклонил / смахнул из недавних во время разговора. После
     каждого через 30 с `adb shell dumpsys audio | grep -A3 "Audio mode"` →
     `MODE_NORMAL`.
  2. FCM-hangup при убитом процессе: на Pixel во время ринга смахнуть приложение
     из недавних, на Samsung положить трубку. В `adb logcat -s CallTeardown` на
     Pixel — `endCall reason=REMOTE_HANGUP`; через 30 с режим `MODE_NORMAL`.
  3. Cold-start: убить процесс Pixel во время разговора, открыть приложение —
     в логе `endCall reason=COLD_START … sessionMarkerOpen=true`; режим
     `MODE_NORMAL`.
  4. Redial сразу после завершения: у второго звонка есть звук в обе стороны.
- Измерено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, Wi-Fi,
  сборка `87e8dd1b`. Вместо Pixel — веб, поэтому шаг 2 (FCM) и половина Pixel
  не проверены. Логи `scratchpad/runs/amode-*` и `amode2-*`, скрипты
  `scratchpad/run-audio-reset.sh` и `run-audio-reset2.sh`.
- Шаг 1: 12 звонков, по два на каждый способ завершения. Через 30 с после
  каждого — `MODE_NORMAL`, все 12 раз:

  | как закончился | `endCall` на Samsung | `audioMode` |
  |----------------|----------------------|-------------|
  | положил я | `DISCONNECT` | 3 |
  | положил собеседник (веб через 8 с) | `DISCONNECT` | 3 |
  | Samsung звонит, веб не отвечает | `DISCONNECT` после `onRejectReceived()` | 3 |
  | веб звонит, Samsung не отвечает | `REJECT`, затем `DISCONNECT` | 1 |
  | отклонил | `REJECT` | 1 |
  | смахнул из недавних в разговоре | `DISCONNECT` | 3 |

  Без ответа звонок кончается на 30-й секунде, а не на 45-й: исходящий
  отклоняет рингер веба, входящий — `IncomingRinger` («no answer in 30s …
  auto-rejecting» → `REJECT`). Во втором входящем JS-таймер сработал на
  ~130 мс раньше (`[call-service] rejectCall` → `m.call.reject`), и натив
  закрыл экран как «Remote hangup» → `DISCONNECT`. Смахивание:
  ```
  19:28:13.787 CallConnectionService: Task removed while a connection was live — disconnecting 1789057672080M4rGBvM3I4aDSrQI
  19:28:13.788 CallTeardown: endCall reason=DISCONNECT callId=1789057672080M4rGBvM3I4aDSrQI State(audioMode=3, …)
  19:28:46.146 CallTeardown: endCall reason=COLD_START callId=null State(audioMode=0, … sessionMarkerOpen=false …) actions=[]
  ```
  Ещё 3 попытки второго круга не в счёт: после смахивания приложение
  открылось на списке чатов, скрипт не нашёл кнопку звонка, и звонков не было.
- Шаг 3, `kill -9` в принятом входящем разговоре:
  ```
  19:41:55.652 Telecom: ServiceDeathRecipient: binderDied: ConnectionService …CallConnectionService died.
  19:41:55.652 AS.AudioService: SetModeDeathHandler client died
  19:41:55.655 Telecom: TC@119: SET_DISCONNECTED … Reason: (CS_DEATH)
  19:41:55.667 ActivityManager: Scheduling restart of crashed service com.forta.chat/.plugins.calls.CallConnectionService in 1000ms for connection
  19:41:56.707 CallTeardown(19991): endCall reason=COLD_START callId=null State(audioMode=0, otherCallLive=false, foregroundServiceRunning=false, routerActive=false, sessionMarkerOpen=true, ringingCallId=null) actions=[]
  ```
  Через 30 с — `MODE_NORMAL`. Строка из шага есть, но режим на Samsung
  сбросила система: Telecom разорвал звонок по смерти сервиса, `AudioService`
  снял режим умершего клиента. Новый процесс поднялся как перезапуск упавшего
  `CallConnectionService`, ещё до ручного открытия, и увидел `audioMode=0` —
  сбрасывать было нечего. Ветку `forceStop`, которую эмулятор отработал при
  `audioMode=3`, этот аппарат не проверил.
- Шаг 4 закрывает серия перезвонов из записи «Разговор переживает завершение
  предыдущего звонка» (сборка `01fedeed`): 19 из 20 звонков соединились через
  1,6–2,2 с после `endCall` прошлого, в каждом звук шёл в обе стороны;
  двадцатый потерян в задержке сервера.
- Шаг 2 (FCM-hangup) — измерено 2026-09-10 на Samsung SM-A528B ↔ веб TEST1,
  сборка `f83b30e7` с `google-services.json` (лог `scratchpad/runs/push-hangup-*`,
  скрипт `run-push-hangup.sh`). Приложение смахнуто до звонка, звонок пришёл
  пушем, рингер смахнут во время звонка, веб положил трубку через 25 с после
  набора. **Сам путь шага не отработал: пуш о hangup до телефона не дошёл.**
  Веб отправил `m.call.hangup` в 21:26:05.450 по часам телефона, а
  `FortaPush: Push received` — строка, которую сервис пишет на любой пуш, — за
  прогон одна: invite в 21:25:43.210. Во всех семи push-прогонах этого дня
  (`push-*`) пуш приходил ровно один — invite, и в обоих прогонах с ответом
  (`push-accept-home*`), где веб клал трубку сам, пуша о hangup тоже не было.
  Вероятная причина — правила пушей homeserver не уведомляют о `m.call.hangup`
  (стандартные правила Matrix звонят только на `m.call.invite`); не проверено.
  Что было вместо: рингер смолк при смахивании (`IncomingRinger stop` в
  21:25:51.759), а соединение Telecom оставалось в RINGING до 45-секундной
  страховки — `Ring timeout` → `onReject` в 21:26:28.339,
  `endCall reason=REJECT` при `audioMode=1`. Через 30 с — `MODE_NORMAL`.
- Шаг 2, проверка гипотезы о правилах пушей — 2026-09-14 (время по часам телефона)
  на Samsung SM-A528B ↔ веб `test23438111`, сборка `044f25f5` с FCM, с разрешения
  владельца. На аккаунте телефона `Testtest11223344` временно добавлено правило
  `underride/com.forta.call.hangup`: `event_match type=m.call.hangup → notify`
  (скрипт `scratchpad/web/push-rule.mjs`). Прогон `run-push-hangup.sh pr2`, логи
  `scratchpad/runs/pr2-*`. **С правилом пуш о hangup дошёл, и нативная ветка
  отработала:**
  ```
  20:38:39.345 I/FortaPush: Call invite: callId=1789407517969taSfG5y4YVmGH9tF, …
  20:39:02.795 D/FortaPush: Call ended remotely (type=m.call.hangup), tearing down incoming UI
  20:39:02.802 I/CallTeardown: endCall reason=DISCONNECT callId=1789407517969taSfG5y4YVmGH9tF State(audioMode=1, …)
  20:39:05.860 I/IdleProcessExit: Call over and nothing left to present — ending the process (disconnect 1789407517969taSfG5y4YVmGH9tF)
  ```
  - Рингер закрылся через 7 мс после пуша. В прогоне `pr1` за 4 минуты до этого
    правило ещё не было добавлено (первая попытка не нашла токен): пуша о hangup
    не было, соединение закрыла 45-секундная страховка (`Ring timeout`).
  - Соединение принадлежало слоту, поэтому teardown пошёл через его
    `onDisconnect` (`reason=DISCONNECT`), а не через
    `CallTeardown.endCall(REMOTE_HANGUP)`. Через 30 с — `MODE_NORMAL`.
  - Побочный эффект: `GET /_matrix/client/v3/notifications` показывает этот
    hangup как уведомление с `read=false`, то есть он попадает в серверный
    счётчик непрочитанного.
  - Правило удалено сразу после прогона (`DELETE` → 200, повторный `GET` → 404).
- Шаг 2, счётчик непрочитанного при правиле — 2026-09-15, 01:03–01:08 по часам
  телефона, та же модель и сборка, веб TEST1 (`test3823818`), приложение открыто на
  списке чатов. Скрипт `scratchpad/run-hangup-counter.sh`, логи
  `scratchpad/runs/hcount-norule-*` и `hcount-rule-*`. Бейдж чата с TEST1 — это
  серверный `getUnreadNotificationCount("total")`. Он читался из `aria-label`
  строки списка и из хранилища чатов, значения совпали во всех замерах.

  | Звонок от TEST1 | Без правила | С правилом |
  |---|---|---|
  | Пропущенный, TEST1 кладёт трубку через ~10 с | 0 → 1 | 2 → 4 |
  | Принятый, TEST1 кладёт трубку | 1 → 2 | 4 → 6 |

  - С правилом hangup-пуш дошёл в обоих звонках (`Call ended remotely`). У
    принятого звонка соединение закрылось через 2 мс после пуша, `MODE_NORMAL`
    вернулся. У пропущенного JS закрыл звонок на 50 мс раньше пуша, и пуш повторно
    вызвал `endCall reason=REMOTE_HANGUP` для уже закрытого звонка; видимых
    последствий нет.
  - `/notifications` за эти 20 минут: 4 invite и 2 hangup, все `notify`,
    `read=false`.
  - Вывод: правило добавляет +1 к бейджу за каждый звонок, завершённый
    собеседником. Пропущенный звонок показывает «2» при одной записи в чате.
    Правило действует на весь аккаунт, поэтому то же увидят другие клиенты.
    Правило снова удалено (`DELETE` → 200, `GET` → 404). Ставить ли его из
    приложения — решение владельца.
  - Правило для `m.call.select_answer` ставить нельзя. Вызывающий шлёт это событие
    на каждый принятый звонок, а нативная ветка `FortaFirebaseMessagingService`
    на него закрывает соединение того же callId, так что принятый звонок
    оборвётся. Вывод сделан по коду SDK и приложения, на аппарате не проверялся.
- Статус: ☐ не проверено

### Ответ из шторки и с экрана блокировки переживает 30-ю секунду
- Коммит: `d606afdc`
- Почему нужен человек: контракт-тесты доказывают, что рингтон/вибрация/дедлайн
  принадлежат `IncomingRinger`, что каждый путь ответа его останавливает и что
  интенты шторки несут нужные флаги и `roomId`. Не доказано без аппарата: что
  системная шторка действительно доставляет тап через `onNewIntent` на всех
  OEM-лаунчерах, что keyguard с PIN отпускает WebView после `onNewIntent`, и
  путь FCM при убитом процессе (push на эмуляторе нет). На эмуляторе
  подтверждён только цикл рингера: `arm` → 30 с → `auto-rejecting` → `stop` →
  Telecom REJECT → teardown; тап по Answer в шторке на софтверном рендере до
  PendingIntent не доходит (SystemUI в ANR).
- На чём: Samsung и Pixel; для убитого процесса и keyguard — CI-сборка (push).
  Шаги и логи — F26 в `docs/call-fix-checklist.md`.
- Шаги:
  1. Pixel звонит, на Pixel нажать Home во время ринга, принять из шторки,
     слушать 45 с: рингтон замолкает в момент ответа, звонок жив.
  2. То же с заблокированным экраном (PIN): звонок соединяется без ручной
     разблокировки или после подтверждения PIN, но не висит в «Соединение…».
  3. Убитый процесс (CI-сборка): смахнуть Forta из недавних, позвонить,
     принять из шторки при выключенном экране — соединяется.
  4. Отклонить из шторки при живом процессе — у звонящего «отклонён» ≤ 3 с.
- Измерено 2026-09-10 (UTC) на Samsung SM-A528B (Android 14), сборка
  `f83b30e7` с FCM, процесс жив, звонящий — веб TEST1. Скрипт
  `scratchpad/run-shade.sh`: во время ринга Home, шторка раскрывается
  (`cmd statusbar expand-notifications`), кнопка уведомления в SystemUI
  нажимается через `adb input tap`. Логи `scratchpad/runs/shade-*`, время
  телефона местное.
  - Шаг 1 проходит на Samsung. «Ответить» в шторке дошло до рингера через
    `onNewIntent`, рингтон остановлен через 39 мс, звонок прожил 45 с:
    ```
    00:25:11.875 D/IncomingCallActivity: onNewIntent: dispatching action=accept on resident instance
    00:25:11.875 D/IncomingCallActivity: Accept pressed
    00:25:11.914 D/IncomingRinger: stop callId=1789075497936ZA6datkny0aIGO8K
    00:25:11.914 D/CallConnection: onAnswer: callId=1789075497936ZA6datkny0aIGO8K, roomId=!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app
    00:25:12.240 I/Capacitor/Console: [NativeCallBridge] matrixCall ready, answering (matchById=true, matchByRoom=false): 1789075497936ZA6datkny0aIGO8K
    ```
    - У веба `connected/stable` через 6 с после нажатия; за следующие 45 с
      входящие байты 497 → 106 040, энергия звука 2,66 — звук с телефона идёт.
    - Через 6 и через 45 с сверху `CallActivity`, режим
      `MODE_IN_COMMUNICATION`. Веб положил трубку в 00:26:04 (`onDisconnect` →
      `CallTeardown: endCall reason=DISCONNECT`), после этого `MODE_NORMAL`.
    - Pixel-половина не закрыта.
  - Шаг 4 проходит на Samsung: веб узнал об отказе через 615 мс после
    «Отклонить» в шторке (`onRejectReceived` в консоли веба,
    2026-09-10T21:26:55.771Z). Время нажатия снято часами того же Mac до
    `adb input tap`, так что 615 мс — верхняя граница.
    ```
    00:26:55.071 D/IncomingCallActivity: onNewIntent: dispatching action=decline on resident instance
    00:26:55.071 D/IncomingCallActivity: Decline pressed
    00:26:55.102 D/IncomingRinger: stop callId=1789075603043SyKbQrIfRk0MEnA3
    00:26:55.103 D/CallConnection: onReject: callId=1789075603043SyKbQrIfRk0MEnA3, roomId=!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app
    00:26:55.104 I/CallTeardown: endCall reason=REJECT callId=1789075603043SyKbQrIfRk0MEnA3 State(audioMode=1, otherCallLive=false, …)
    ```
    После отказа `MODE_NORMAL`.
  - Шаг 2 проходит на Samsung: экран заблокирован (PIN) и погашен, звонок
    соединился и 45 с шёл с таймером, без «Соединение…». Скрипт
    `scratchpad/run-lock.sh`, логи `scratchpad/runs/lock-answer1-*`; блокирует
    телефон и отвечает владелец.
    ```
    01:10:52.229 D/CallConnectionService: onCreateIncomingConnection: callId=1789078251029fRuckoxwKqjG1sOJ, caller=test3823818, …
    01:10:52.354 I/ActivityTaskManager: Activity requesting to dismiss Keyguard: ActivityRecord{… com.forta.chat/.plugins.calls.IncomingCallActivity …}
    01:10:53.285 D/IncomingRinger: arm callId=1789078251029fRuckoxwKqjG1sOJ
    01:11:02.455 D/IncomingCallActivity: Accept pressed
    01:11:02.473 D/IncomingRinger: stop callId=1789078251029fRuckoxwKqjG1sOJ
    01:11:02.474 D/CallConnection: onAnswer: callId=1789078251029fRuckoxwKqjG1sOJ, roomId=!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app
    01:11:02.807 I/Capacitor/Console: [NativeCallBridge] matrixCall ready, answering (matchById=true, matchByRoom=false): …
    ```
    - Рингер поднялся поверх блокировки: `IncomingCallActivity` сверху при
      `isKeyguardShowing=true`. Ответ — кнопкой «Принять» на рингере.
    - У веба `connected/stable` в 22:11:06.883Z (UTC), через 4,4 с после
      «Принять». К этому моменту блокировка уже снята
      (`isKeyguardShowing=false`), сверху `CallActivity`. Чем владелец снял
      её при ответе (PIN или биометрия), он не помнит.
    - Каждые 5 с на экране звонка идёт таймер (00:10 → 01:35) и кнопки
      «Выкл. микрофон», «Вкл. видео», «Динамик телефона»; входящие байты у
      веба 11 541 → 216 335.
    - Веб положил трубку в 01:12:43 (`onDisconnect` → `CallTeardown: endCall
      reason=DISCONNECT`), после этого `MODE_NORMAL`.
- Статус: ☐ не проверено

### ICE-кандидаты до answer доходят до соединения
- Коммит: d4ae4282
- Почему нужен человек: юнит-тесты доказывают буферизацию и порядок на
  двойнике peer connection и подключение буфера в `onPeerConnectionCreated`.
  Не доказано: что на реальном нативном движке (libwebrtc в WebRTCPlugin) и
  на реальном WebView `remoteDescription` появляется ровно к моменту, когда
  буфер сбрасывается, и что доля «Соединение… → обрыв» между сетями реально
  падает. На эмуляторах прогон до ICE-пути не дошёл: звонок заканчивался в
  пределах 10 с после ринга.
- На чём: Samsung (сотовая) ↔ Pixel (Wi-Fi), обе сборки. Шаги и логи — F27
  в `docs/call-fix-checklist.md`.
- Шаги:
  1. 5 звонков Samsung→Pixel с мгновенным ответом, 5 обратно.
  2. У звонящего в логе есть `[ice-buffer] holding candidate` и
     `remote description set, adding N held candidate(s)`, нет
     `failed to add remote ICE candidate`.
  3. Все 10 звонков соединяются в пределах 5 с после ответа.
- Измерено 2026-09-10 на Samsung SM-A528B ↔ веб TEST1, обе стороны в одной
  Wi-Fi, сборки `0f262954` и `01fedeed` (код буфера в них одинаковый; логи
  `scratchpad/runs/fx-*`, `fx3-*`, `reneg-*`, `redial*`): 25 соединившихся
  звонков — 22 Samsung→веб (веб отвечал сразу, как появлялась кнопка) и 3
  веб→Samsung (Samsung отвечал тапом через adb). Строк `[ice-buffer]` нет ни
  в logcat, ни в консоли веба: кандидаты ни разу не пришли раньше answer,
  держать буферу было нечего; `failed to add remote ICE candidate` — ни одной.
  До соединения: у звонящего Samsung 0,0–0,6 с после того, как answer
  применился (`onSignalingChange: STABLE`), у отвечающего — 2,7 с от `onAnswer`
  во всех трёх. **Шаг 2 на одной сети не достаётся** — нужен прогон на
  сотовой, последний пункт плана.
- Статус: ☐ не проверено

### Одна камера на исходящий видеозвонок
- Коммит: 4c23a27b
- Почему нужен человек: `TrackAttachPolicyTest` доказывает правило выбора
  соединений, `TrackAttachContractTest` — что `CallActivity` только
  привязывает превью, что `startLocalVideo` под `mediaLock` и что все
  `addTrack` локальных треков идут через одну точку.
  Не доказано: что гонка main-поток (`CallActivity`) ↔ plugin-поток
  (`startLocalMedia`) на реальном аппарате перестала открывать камеру дважды,
  и что картинка и звук в видеозвонке доходят в обе стороны на реальных
  камерах и OEM-энкодерах. Эмулятор: прогон 2026-09-08 (исходящий
  видеозвонок callA→callB, подсчёт `Local video started` в logcat) не
  завершён — хост дважды зависал и перезагрузился до ответа callB;
  эмуляторного подтверждения нет, только тесты.
- На чём: Samsung ↔ Pixel, обе сборки, реальная камера. Шаги и логи — F28
  в `docs/call-fix-checklist.md`.
- Шаги:
  1. 3 видеозвонка Pixel→Samsung и 3 Samsung→Pixel по 20 с.
  2. У звонящего в логе ровно одна `Local video started with camera` на
     звонок и по одной `attached video track` на соединение; нет ошибок
     камеры.
  3. Все 6 звонков с картинкой и звуком в обе стороны.
- Измерено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, сборка с
  `87e8dd1b`: до этого коммита исходящий видеозвонок с Android не соединялся
  вовсе (запись «Исходящий видеозвонок с Android соединяется»). Логи
  `scratchpad/runs/video-out3-*` и `video-say-*`, скрипт
  `scratchpad/run-video-out.sh`. Только Samsung→веб, 4 звонка по 20 с.
- Шаг 2, звонящий Samsung: в каждом звонке ровно одна
  `Local video started with camera` и одна `attached video track` (соединение
  одно), `startLocalVideo(reuse)` нет:
  ```
  19:14:33.353 NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
  19:14:33.522 NativeWebRTCManager: [pc_1_1789056873369] createPeerConnection: attached video track
  19:14:57.718 CameraCapturer: Stop capture done
  19:14:57.720 Camera2Session: Stop camera2 session on camera 1
  ```
  Камера открывается раньше соединения (`pcs=0`) и цепляется к нему в
  `createPeerConnection`. `CameraAccessException` — 0, строк
  `Camera2Session`/`CameraCapturer` с `error`/`failed` — 0. `notifyError` с
  кодами 4 и 5 (10–12 на звонок) приходят только через 53–109 мс после
  `Stop capture done` — кадры, застигнутые остановкой. Строки `CHIUSECASE` —
  шум HAL Qualcomm, не ошибки. Второй `Stop capture done` в той же
  миллисекунде пишет `dispose` с `No session open`, а не вторая камера.
- Шаг 3, Samsung→веб: у веба за 20 с 262–266 декодированных кадров, разрешение
  доходит до 640×360 (звонок 1) и 960×540 (звонки 2–4). У телефона через 12 с
  после соединения 243–248 кадров и 34,7–37,5 КБ звука от веба. Звук с
  телефона в тихой комнате — 1,9–2,0 КБ за 20 с: видеозвонок идёт на громкой
  связи, шумоподавление Samsung глушит тишину, работает DTX. С `say` рядом с
  телефоном (звонок 4) — 54,4 КБ за 20 с, 3 795 Б/с, прирост энергии +0,41.
- Не проверено: Pixel, Pixel→Samsung и видеозвонок, принятый на Samsung.
- Статус: ☐ не проверено

### Громкая связь: отказ и пин ручного выбора
- Коммит: 62581806
- Почему нужен человек: `AudioRoutePolicyTest` доказывает таблицу
  решений, `AudioRoutePinContractTest` — отказ без старта роутера и reject
  до JS, vitest — откат переключателя и тост. Не доказано: слышимая смена
  маршрута на реальном динамике/наушнике и поведение с настоящей
  Bluetooth-гарнитурой (на эмуляторе нет ни того, ни другого), а также
  прошивки, игнорирующие оба API переключения (OnePlus #1334).
- На чём: Samsung ↔ Pixel, обе сборки, Bluetooth-гарнитура. Шаги и логи —
  F29 в `docs/call-fix-checklist.md`.
- Шаги:
  1. Ранний тап «динамик» до соединения: кнопка откатывается, тост
     «Не удалось переключить звук…», в логе `refused: router inactive`.
  2. В разговоре динамик вкл → выкл → вкл: маршрут слышимо меняется.
  3. Динамик включён, подключить гарнитуру: звук остаётся на динамике;
     выключить динамик — уходит в гарнитуру; отключить гарнитуру —
     fallback.
- Измерено 2026-09-10 на Samsung SM-A528B (лог
  `scratchpad/runs/audio-143137.log`), шаг 2 — на уровне фреймворка:
  ```
  14:32:37.012 AudioRouter: setDevice: SPEAKER (pinned)
  14:32:37.017 AudioLifecycle: setCommunicationDevice(SPEAKER): true
  14:32:58.017 AudioRouter: setDevice: EARPIECE (pinned)
  14:32:58.021 AudioLifecycle: setCommunicationDevice(EARPIECE): true
  14:33:08.198 AudioRouter: setDevice: SPEAKER (pinned)
  14:33:08.210 AudioLifecycle: setCommunicationDevice(SPEAKER): true
  ```
  Система подтверждает каждый выбор своим `AS.AudioDeviceBroker … role:output
  type:speaker` / `type:earpiece`, то есть пин доезжает до HAL и маршрут
  действительно меняется туда-обратно. Шторка при этом закрывается, тоста нет.
  **Шаг 1 достать не удалось:** роутер поднимается за ~1,5 с после `onAnswer`
  (`isActive:true` в 14:32:27.9), а самый ранний тап по шторке пришёлся на
  14:32:37 — окно отказа к этому моменту давно закрыто. Отказ остаётся
  доказанным только тестами.
  **Остаётся человеку:** слышимость и Bluetooth-гарнитура (шаг 3).
- Измерено 2026-09-11 на Samsung SM-A528B, сборка `f83b30e7` с FCM (логи
  `scratchpad/runs/early-pick1-*`, `early-pick2-*`, `early-spk4-*`; время —
  часы телефона, UTC+3).
  - Шаг 1. Во время звонка на Android на экране нативная шторка «Аудио». До
    соединения окно отказа на этом аппарате закрыто: роутер активен раньше,
    чем экран звонка принимает нажатия, и «Динамик» до соединения
    применяется (`setDevice: SPEAKER (pinned)` за 822 мс до
    `reportCallConnected`). Сам отказ достигнут в окне после teardown:
    `refused: router inactive` и русский тост — выдержка в записи «Нативная
    шторка выбора звука сообщает об отказе» («Проверено»). Откат
    JS-переключателя эти прогоны не проверяли.
  - Шаг 2 — ещё раз на уровне фреймворка, три выбора в одном звонке:
    ```
    01:52:12.865 AudioRouter: setDevice: SPEAKER (pinned)
    01:52:12.868 AS.AudioDeviceBroker: setCommunicationRouteForClient … type:speaker
    01:52:23.234 AudioRouter: setDevice: EARPIECE (pinned)
    01:52:23.237 AS.AudioDeviceBroker: setCommunicationRouteForClient … type:earpiece
    01:52:31.600 AudioRouter: setDevice: SPEAKER (pinned)
    01:52:31.602 AS.AudioDeviceBroker: setCommunicationRouteForClient … type:speaker
    ```
- Измерено 2026-09-13 на Samsung SM-A528B с AirPods Pro 2, сборка `0bdde967` с FCM
  (`scratchpad/run-stage3-route.sh`, логи `scratchpad/runs/route3b-*`, `route3d-*`;
  время — часы телефона, UTC+3). **Выбор «Динамик» не включает громкий динамик.**
  - Прежние блоки «Измерено» ошибочны: строка `AS.AudioDeviceBroker
    setCommunicationRouteForClient … type:speaker` — только запрос приложения.
    Аудиорежимом звонка владеет Telecom (`mAudioModeOwner … mUid=1000`), и звуковая
    политика после каждого выбора оставляет трубку:
    ```
    02:12:39.467 AudioRouter: setDevice: SPEAKER (pinned)
    02:12:39.471 AudioLifecycle: setCommunicationDevice(SPEAKER): true
    02:12:39.471 APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_EARPIECE, @:}
    ```
    В звонках 2026-09-10 (`audio-143137.log`) и 2026-09-13 — 16 выборов и ни одного
    `selected devices {AUDIO_DEVICE_OUT_SPEAKER}` за время разговора; Telecom ни разу
    не входил в `ActiveSpeakerRoute`. Владелец слышал писк из верхней части телефона
    при любом выборе.
  - Шаг 2 не проходит — см. выше.
  - Шаг 3 не проверить, пока динамик не работает. Гарнитуру переключает сам Telecom:
    при подключении AirPods он уводит звонок в них (`SWITCH_BLUETOOTH` →
    `ActiveBluetoothRoute`; звук в AirPods подтвердил владелец) независимо от выбора в
    приложении. С подключёнными AirPods звуковая политика после выборов «Динамик» и
    «Динамик телефона» оставляет Bluetooth; при отключении AirPods звонок возвращается
    в трубку.
  - Починку владелец отдал без его участия: маршрут должен идти через соединение
    Telecom.
- Статус: ☐ не проверено

### Факты ICE и Tor в отчёте
- Коммит: e9ec3f09
- Почему нужен человек: тесты доказывают счётчики, предупреждение,
  провайдера и строки отчёта. Не доказано: что на реальной сети
  (Samsung на сотовой) relay-кандидаты действительно собираются и отчёт
  с аппарата доходит с этими строками (отчёты уходят только из CI-сборки
  с токеном), и что homeserver вообще отдаёт TURN — это `curl` к
  `/voip/turnServer` у владельца.
- На чём: Samsung на сотовой ↔ Pixel на Wi-Fi, CI-сборка. Шаги — F30 в
  `docs/call-fix-checklist.md`.
- Шаги:
  1. 30 с разговора, отчёт с Samsung: строки `ICE candidates` и
     `ICE result` в разделе Call diagnostics.
  2. Tor включён: тост при наборе и при ответе; строка `Tor` в отчёте.
  3. `curl … /voip/turnServer`: есть ли `turns:` на 443/tcp.
- Измерено 2026-09-10, шаг 3 — без `curl`, по конфигурации живого звонка у
  веба (`pc.getConfiguration().iceServers`; учётные данные отброшены до
  записи, лог `scratchpad/runs/fx-out-web.jsonl`). STUN зашит в
  `matrix-js-sdk-bastyon` (`checkTurnServers`: случайные `relayN.expressturn.com`
  плюс metered, twilio, cloudflare), TURN приходит от homeserver через
  `/voip/turnServer` (`res.uris`):
  ```
  turn:relay18.expressturn.com:3478?transport=udp
  turn:relay2.expressturn.com:3478?transport=tcp
  ```
  **`turns:` нет, TURN на 443/tcp нет** — в сети, где открыт только 443,
  relay-кандидатов не соберётся. Выборка одна: набор может меняться от ответа
  к ответу. Сам relay рабочий: в звонке 15:24 UTC (`fx3-out-web.jsonl`) пара
  у веба сошлась как `srflx/relay` — телефон говорил через TURN. Шаги 1–2
  (отчёт с CI-сборки, тост Tor) по-прежнему открыты.
- Измерено 2026-09-10, половина шага 2: Samsung, Tor «Всегда» через Snowflake,
  исходящий звонок вебу (лог `scratchpad/runs/tor-file-events.log`, скрипт
  `scratchpad/run-tor3.sh`). `Toast.vue` закрывается сам через 3 с, поэтому
  тост ловил `MutationObserver` на `[role=status]`, а не опрос текста. Тост
  появился через 1,2 с после набора, но по-английски — «Calls don't go through
  Tor: the other party can see your IP address.» — при русском интерфейсе.
  Причина: `tRaw` читал только сохранённую локаль и без неё отвечал
  по-английски, а стор локали берёт русский из языка устройства и ничего не
  сохраняет. Именно так на телефоне, где язык вручную не выбирали. Исправлено в
  `7416cdc3`, тесты в `tRaw.test.ts`; русский тост на аппарате после фикса не
  перепроверен. Тост при ответе и строка `Tor` в отчёте не проверены.
- Статус: ☐ не проверено

### Full-screen intent и качелька громкости
- Коммит: 1adc0949
- Почему нужен человек: contract-тест доказывает `STREAM_RING` и гейт
  API 34, vitest — баннер, composable и поле отчёта. Не доказано: что
  системный экран действительно открывается и что после включения
  разрешения экран входящего снова поднимается на заблокированном
  аппарате; что качелька во время ринга реально глушит рингтон (нужен
  аппарат с Android 14+; эмуляторного прогона не было).
- На чём: Pixel (Android 14+) и Samsung, обе сборки. Шаги — F31 в
  `docs/call-fix-checklist.md`.
- Шаги:
  1. Выключить «Полноэкранные уведомления» для Forta Chat, открыть
     Настройки → Уведомления: баннер и кнопка → системный экран; включить
     → баннер исчезает.
  2. Отчёт из приложения при выключенном разрешении: строка
     `Full-screen intent | REVOKED`.
  3. Во время ринга нажать «громкость −» или «громкость +».
     **Ожидается:** рингтон и вибрация замолкают сразу, экран входящего и
     обратный отсчёт остаются, звонок без ответа завершается по сроку (30 с).
     Уровень громкости звонка при этом не меняется: пока звонок в Telecom
     звонит, система отдаёт качельку на `silenceRinger`, как штатная
     звонилка.
- Измерено 2026-09-10 на Samsung SM-A528B (Android 14), сборка `f83b30e7` с
  FCM, звонящий — веб TEST1. Логи и скриншоты `scratchpad/runs/volrock1-*`,
  `scratchpad/runs/fsi-*`; скрипт `scratchpad/run-volume-rocker.sh`.
  - **Шаг 1 провален: пока экран «Уведомления» открыт, баннер не следует за
    разрешением.** Кнопка открывает системный экран
    (`com.android.settings/.Settings$AppManageFullScreenIntentsActivity`), но
    статус читается только при монтировании (`onMounted` →
    `detectFullScreenIntent` в `NotificationSettings.vue`), а возврат из
    системного экрана его не перечитывает. Время UTC, скрипт
    `scratchpad/run-fsi-retest.sh`, скриншоты `scratchpad/runs/fsiretest-*.png`:
    ```
    20:59:23Z владелец выключил разрешение (appops deny)
    21:00:59Z вернулся в приложение: {"view":true,"banner":false,"status":{"allowed":false,"manageable":true}}
    21:01:05Z «Уведомления» открыты заново: {"view":true,"banner":true,"status":{"allowed":false,"manageable":true}}
    21:01:06Z «Разрешить полноэкранные уведомления» → системный экран
    21:01:39Z владелец включил разрешение (appops allow)
    21:01:58Z вернулся в приложение: {"view":true,"banner":true,"status":{"allowed":true,"manageable":true}}
    21:02:07Z через 9 с: {"view":true,"banner":true,"status":{"allowed":true,"manageable":true}}
    ```
    - Проба читает DOM (`[data-testid="fsi-banner"]`) через CDP и тут же
      спрашивает плагин (`PushData.getFullScreenIntentStatus()`).
    - Ошибка в обе стороны: после выключения баннера нет, после включения он
      висит с текстом «Android отключил полноэкранные уведомления». Верное
      состояние появляется только при повторном открытии экрана.
    - `openFullScreenIntentSettings()` резолвится сразу после `startActivity`;
      при возврате в логе `Capacitor/AppPlugin: No listeners found for event
      resume`.
    - Первая попытка (20:43Z) не засчитана: владелец нажал «Назад» в
      приложении через 0,7 с после возврата, и экран закрылся до пробы.
    - Починка — отдельным шагом, отложена владельцем (стадия 7) вместе с
      шагом 3: перечитывать статус, когда приложение снова становится активным.
  - Шаг 2 не проверялся: отчёт из приложения сразу публикуется issue на
    GitHub, и владелец решил его не отправлять. С плагина снято значение, из
    которого строится строка: при выключенном разрешении
    `getFullScreenIntentStatus` → `{"allowed":false,"manageable":true}`.
  - **Шаг 3 провален: во время ринга качелька не меняет громкость и не глушит
    рингтон.** Приложение открыто, экран не заблокирован, разрешение выдано;
    сверху `IncomingCallActivity` (поверх него heads-up «Входящий вызов»),
    `STREAM_RING` = 7.
    ```
    23:48:56.498 I/WindowManager: interceptKeyBeforeQueueing: VOLUME key-down while ringing: Silence ringer!
    23:48:56.521 I/Telecom: Call: Send silence to connection service for call [Call id=TC@169, state=RINGING, tpac=ComponentInfo{com.forta.chat/com.forta.chat.plugins.calls.CallConnectionService}, …]
    23:48:56.523 I/TelecomFramework: CallConnectionService: silence TC@169_1
    23:48:56.570 D/AS.AudioService: adjustSuggestedStreamVolume() stream=2, flags=4116, caller=android, volControlStream=-1, userSelect=false
    23:48:56.579 D/AS.AudioService: adjustStreamVolume() stream=0, dir=0, flags=4116, caller=android
    23:48:56.601 D/vol.VolumeDialogControl: onVolumeChangedW stream = 0, flags = 4112, lastAudibleStreamVolume = 8, changed = false, showUI = false, dualAudio = false
    23:49:06.305 I/WindowManager: interceptKeyBeforeQueueing: VOLUME key-down while ringing: Silence ringer!
    ```
    - Пока наш self-managed звонок в Telecom в состоянии RINGING, система
      забирает нажатие качельки раньше окна и вызывает `silenceRinger`. До
      `AudioService` доходит только отпускание клавиши — с `dir=0`, поэтому
      `volumeControlStream = STREAM_RING` из `1adc0949` громкость не меняет.
    - Telecom передаёт `silence` в `CallConnectionService`, но `CallConnection`
      не переопределяет `onSilence()`, и рингтон играет дальше: до отбоя в
      23:49:09 в логе нет ни одного `IncomingRinger: stop`.
    - «Громкость −» (23:48:56) и «громкость +» (23:49:06) не сдвинули ни один
      поток: `STREAM_RING` 7 → 7 → 7, панель громкости не появилась. На слух
      не проверено: владелец не слушал.
    - Для звонка, зарегистрированного в Telecom, ожидание шага невыполнимо:
      платформа отдаёт качельку на `silenceRinger`, как в штатной звонилке.
      Починка — отдельным шагом, отложена владельцем (стадия 7): `onSilence()`
      глушит рингтон и вибрацию, не снимая 30-секундный срок рингера
      (`IncomingRinger.stop` снимает и его); ожидание шага 3 переписать.
- Починка шага 1 — `be63a993`, 2026-09-11. Экран «Уведомления» перечитывает
  разрешение, когда приложение снова становится активным (`appStateChange` в
  `NotificationSettings.vue`; подписка снимается при закрытии экрана).
  Перепроверка — `scratchpad/run-fsi-retest.sh` на APK с починкой: после
  возврата из системного экрана баннер следует за разрешением в обе стороны
  без повторного открытия «Уведомлений».
- Починка шага 3 — `66a5afbc`, 2026-09-11. `CallConnection.onSilence()` вызывает
  `IncomingRinger.silence()`: тот глушит рингтон и вибрацию и больше ничего не
  трогает — экран рингера, его 30-секундный срок и 45-секундный backstop
  соединения остаются. Если рингер того же звонка поднимут повторно (тап по
  уведомлению), заглушённый звонок не зазвонит снова. Перепроверка —
  `scratchpad/run-volume-rocker.sh` на APK с починкой: сразу после
  `Silence ringer!` в логе `CallConnection: onSilence` и
  `IncomingRinger: silence callId=…`, экран входящего на месте до отбоя.
  Прогнать дважды: при открытом приложении и при свёрнутом. На push-пути
  уведомление канала `incoming_call_v2` само один раз проигрывает рингтон
  (звук канала, `TYPE_RINGTONE`), и `onSilence` его не останавливает. Если
  после нажатия звук продолжается до конца мелодии — это он, отдельная
  задача.
- Перепроверено 2026-09-14 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5`
  с FCM, звонящий — веб TEST1; разрешение переключал владелец.
  - **Шаг 1 прошёл** (`be63a993`; `scratchpad/run-fsi-retest.sh`, события
    `scratchpad/runs/fsiretest-events.log`, скриншоты `fsiretest-*.png`). Баннер
    следует за разрешением в обе стороны сразу после возврата, без повторного
    открытия «Уведомлений»:
    ```
    17:59:20Z «Уведомления» открыты: {"view":true,"banner":false,"status":{"allowed":true,"manageable":true}}
    17:59:44Z владелец выключил разрешение (appops deny) и вернулся в приложение
    17:59:44Z проба: {"view":true,"banner":true,"status":{"allowed":false,"manageable":true}}
    17:59:50Z «Разрешить полноэкранные уведомления» → системный экран
    17:59:57Z владелец включил разрешение (appops allow)
    18:00:00Z вернулся, проба: {"view":true,"banner":false,"status":{"allowed":true,"manageable":true}}
    18:00:08Z через 8 с: {"view":true,"banner":false,"status":{"allowed":true,"manageable":true}}
    ```
    Когда баннер уходит, на его место поднимается блок «Не слышно уведомлений?»
    с такой же жёлтой рамкой и синей кнопкой. Владелец сначала принял его за
    баннер; по снимку экрана это другой блок с другим текстом.
  - **Шаг 3 прошёл** при открытом приложении (`66a5afbc`, `volrock2`) и при
    свёрнутом (`volrock3`: звонок пришёл пушем, на экране были и полноэкранный
    рингер, и плашка сверху). Скрипт `scratchpad/run-volume-rocker.sh`, время по
    часам телефона (`volrock3`):
    ```
    23:37:45.298 I/WindowManager: interceptKeyBeforeQueueing: VOLUME key-down while ringing: Silence ringer!
    23:37:45.300 D/CallConnection: onSilence: callId=1789418260568RAXRVSPxXbUExMll
    23:37:45.320 D/IncomingRinger: silence callId=1789418260568RAXRVSPxXbUExMll (deadline kept)
    ```
    - Владелец в обоих прогонах слышал, что рингтон замолчал сразу; при
      свёрнутом — «даже раньше, чем полностью исчезла плашка». Звук канала
      `incoming_call_v2` после нажатия не доигрывал.
    - `STREAM_RING` до и после нажатия 15. Через 5 с после нажатия экран
      входящего и отсчёт на месте.
    - При открытом приложении звонок закрылся по сроку рингера: `no answer in
      30s … auto-rejecting` через 30 с после `arm`. При свёрнутом — отбоем веба
      (`Dismissing incoming call screen (remote hangup)`).
  - Шаг 2 не перепроверялся: отчёт из приложения сразу публикует issue.
- Статус: ☐ шаги 1 и 3 прошли на Samsung 2026-09-14 после починок; шаг 2 и Pixel не проверены

### Слот Telecom по callId
- Коммит: dac97dcb
- Почему нужен человек: contract-тест доказывает вложенные extras и
  проверку id у трёх читателей, `CallSlotPolicyTest` — правило. Не
  доказано: что Telecom на реальных прошивках доносит вложенный bundle
  (лог `onCreateOutgoingConnection: callId=<непустой>`) и что второй
  входящий во время разговора больше не рвёт разговор (нужна третья
  сторона).
- На чём: Pixel ↔ Samsung + веб как третий звонящий, обе сборки. Шаги —
  F32 в `docs/call-fix-checklist.md`.
- Шаги:
  1. Исходящий с Pixel: в логе непустой callId.
     ✅ 2026-09-09 на Samsung SM-A528B (Android 14):
     `onCreateOutgoingConnection: callId=1788954840884hVNAUhH8ehEk5JZ7`.
  2. Второй входящий с веба в разговоре: «занято» второму, разговор
     живёт, строка `leaving it` в логе; после конца — `MODE_NORMAL`.
     Измерено 2026-09-10 на Samsung SM-A528B ↔ веб TEST1 + веб
     `test23438111`, сборка `f83b30e7` с FCM (логи
     `scratchpad/runs/3way-decline2-*`, `3way-accept1-*`).
     - «Занято» второму — да: JS отправил `m.call.reject` через 21 мс после
       приглашения, обе регистрации в Telecom получили `reporting busy`.
     - Разговор жив, после конца — `MODE_NORMAL`.
     - Строки
       `reportCallEnded(<id второго>): slot holds <id первого>, leaving it` нет
       ни в одном прогоне. В этом сценарии ей и неоткуда взяться: ветка
       `hasLiveCall` в `call-service.ts` отклоняет второй звонок без
       `finalizeCall` (так задумано, см. комментарий там), поэтому
       `reportCallEnded` для него не вызывается. Проверку id в
       `reportCallEnded` на аппарате ничто не задело — её доказывает только
       `CallSlotContractTest`.
- Статус: ☐ шаг 1 проверен, шаг 2 — частично (нет строки `leaving it`)

### Перезвон сразу после звонка доходит и звонит
- Коммит: `d8a52a0d`
- Почему нужен человек: то, что `finalizeCall` зовёт `retirePendingMarkers`
  первым шагом, что гашение матчит и callId, и roomId, и что комната
  передаётся только когда в ней нет другого живого звонка, закрыто
  юнит-тестами
  (`native-call-bridge.pending-marker.test.ts`, `finalize-call.test.ts`,
  `PendingCallMarkerTest`, `PendingMarkerStampContractTest`). Не закрыто ничем:
  что метод плагина реально доезжает до Kotlin через Capacitor, и что сквозные
  сценарии на аппарате действительно чинятся.
- На чём: реальный аппарат + второй собеседник (веб-клиент годится).
- Шаги:
  1. **Сторона отказа.** Позвонить и не отвечать, дать рингеру сработать на
     30-й секунде (`no answer in 30s … auto-rejecting` -> `onReject`).
     Перезвонить из той же комнаты через 10-15 секунд. Аппарат обязан
     зазвонить: `onCreateIncomingConnection` с новым callId и
     `IncomingRinger: arm`, без строки `[call-service] Pre-rejected incoming
     call` — до фикса она была именно здесь.
  2. **Сторона ответа.** Позвонить, снять трубку, поговорить пару секунд,
     положить. Перезвонить из той же комнаты через 10-15 секунд. Аппарат
     обязан **зазвонить**, а не снять трубку сам: в логе не должно быть
     `[call-service] Pre-accepted incoming call, skipping ringer`. Это тот
     самый симптом «я не снимал трубку — она сама снялась».
  3. В обоих случаях в логе рядом с завершением видно
     `NativeCall … retirePendingMarkers`.
  4. **Известный остаток, чинить не в этой правке.** Если Telecom пришлёт
     `onAnswer` повторно уже после того, как `finalizeCall` отработал для
     этого callId, метка перезапишется, а повторный `finalizeCall` для того же
     callId — no-op в течение 30 секунд, и гасить будет некому. Живёт до
     истечения TTL метки, то есть не дольше минуты.
  5. **Второй известный остаток, чинить не в этой правке.** Правило «в комнате
     нет другого живого звонка» опирается на то, что JS узнал о новом звонке
     раньше, чем завершился старый. Нативный экран может опередить: если
     пользователь примет второй звонок до того, как JS о нём услышал, и ровно
     тогда завершится первый, метка второго будет стёрта. Последствие мягче
     исходных симптомов — телефон зазвонит повторно, а не снимет трубку сам.
     Воспроизвести на стенде не удалось: нужен реальный стык Telecom и FCM.
  7. **Проверено 2026-09-10** на Samsung SM-A528B, шаги 1-3 (лог
     `scratchpad/runs/accept-race-144354.log`) — четыре звонка подряд **из
     одной комнаты**, попеременно ответ и отказ, каждый следующий звонил сам:

     ```
     14:44:30.898 arm  …VrMIzeK7aqGX8i9X
     14:45:00.705 onAnswer …VrMIzeK7aqGX8i9X          ← ответили
     14:45:45.502 arm  …MvY7EdAW4s0uR7AL              ← перезвон ЗВОНИТ (шаг 2)
     14:46:15.493 onAnswer …MvY7EdAW4s0uR7AL
     14:47:02.469 arm  …H8ujNKvZge7F72SD
     14:47:32.471 Decline pressed → onReject          ← авто-отказ на 30-й с
     14:48:17.481 arm  …tYEweeg1Vl6fvMiw              ← перезвон ЗВОНИТ (шаг 1)
     ```

     Ни одной строки `Pre-accepted` / `Pre-rejected` во всём логе. Шаг 3 тоже
     виден напрямую — метод действительно доезжает до Kotlin через Capacitor:
     `To native (Capacitor plugin): pluginId: NativeCall, methodName:
     retirePendingMarkers` с `methodData: {"callId":"…","roomId":"…"}` рядом с
     каждым завершением. Поправка к шагу 1: авто-отказ пишет не `no answer in
     30s`, а `Decline pressed` — это отсчёт `IncomingCallActivity`, который
     зовёт `decline()`.
  6. **Только на сборке из CI.** Повторить шаги 1-2 так, чтобы звонок пришёл
     через push: приложение убито, экран заблокирован. Это отдельный путь —
     соединение тогда ключуется event_id, и до правки гашение на нём было бы
     инертным. Локальная отладочная сборка сюда не годится: в ней нет
     `google-services.json`, а значит нет и FCM, и все локальные прогоны идут
     через `/sync`, где идентификаторы совпадают.
- Шаг 6 (перезвон по пушу) не проверен: прогон 2026-09-10
  (`scratchpad/runs/push-redial-reject-*`, сборка `f83b30e7` с FCM) упёрся в
  другой дефект. Первый звонок пришёл пушем в процесс, оставшийся от прошлого
  push-звонка, чей рингер смахнули из «недавних». Система убила процесс через
  14 мс после `Push received`: уведомление уже выставлено, а до
  `IncomingCallActivity` и `addNewIncomingCall` дело не дошло, звонок не
  зазвонил.
  ```
  21:32:57.974 D/FortaPush(21491): Push received: …
  21:32:57.977 I/FortaPush(21491): Call invite: callId=1789065175857OSZU7xbKuVQkSyKg, …
  21:32:57.981 I/NotificationManager(21491): com.forta.chat: notify(2075940556, forta_push, Notification(channel=incoming_call_v2 …
  21:32:57.988 I/ActivityManager: Killing 21491:com.forta.chat/u0a294 (adj 905): remove task
  21:32:58.013 W/ActivityManager: Scheduling restart of crashed service com.forta.chat/.FortaFirebaseMessagingService in 1000ms for start-requested
  ```
  Сервис поднялся в новом процессе, но сообщение повторно не пришло. Второй
  звонок прогона попал уже в новый процесс, зазвонил и отклонился по 30-секундному
  сроку — перезвон проверять было не на чем. Задач приложения в «недавних» в
  момент убийства не было и никто их не удалял; похоже на отложенное системой
  убийство за задачу рингера, смахнутую, пока процесс был занят звонком.
  Воспроизведено повторно (`push-hangup2-*` → `kill-repro1-*`, скрипты
  `run-push-hangup.sh` и `run-push-kill-repro.sh`): `Killing 25771 … remove
  task` через 3 мс после `notify`, ни `onCreateIncomingConnection`, ни `arm`.
  Починка — `33868ae2`, запись «Процесс со смахнутым звонком не съедает
  следующий push-звонок».
  Перепроверено 2026-09-14 на сборке `044f25f5` (`ph2` → `kr2`): процесс со
  смахнутым рингером завершился сам через 3 с после `Ring timeout`, следующий
  push-звонок поднял новый процесс и зазвонил (`onCreateIncoming=1 arm=1`).
- Шаг 6 с заблокированным экраном проверен 2026-09-10 (UTC) на Samsung
  SM-A528B (Android 14), сборка `f83b30e7` с FCM, звонящий — веб TEST1.
  Скрипт `scratchpad/run-push-redial-lock.sh`, логи
  `scratchpad/runs/push-redial-lock-{reject,answer}-*`. Приложение не
  смахивается из «недавних» — это задевает дефект выше: Home и `am kill`,
  процесс мёртв, телефон блокирует владелец. Оба звонка каждой пары пришли
  пушем: A — в мёртвый процесс, B — в процесс, поднятый пушем A. Рингер
  поднимался поверх блокировки.
  - **Сторона отказа.** На 30-й секунде рингер сам отклонил A; B через 21 с
    зазвонил:
    ```
    01:15:17.593 I/FortaPush: Call invite: callId=1789078516251WNPN7RSrv2KYhrzU, …
    01:15:18.111 D/IncomingRinger: arm callId=1789078516251WNPN7RSrv2KYhrzU
    01:15:48.112 W/IncomingRinger: no answer in 30s for 1789078516251WNPN7RSrv2KYhrzU — auto-rejecting
    01:15:48.134 D/CallConnection: onReject: callId=1789078516251WNPN7RSrv2KYhrzU, roomId=!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app
    01:15:54.360 I/Capacitor/Console: [call-service] Pre-rejected incoming call, calling reject(): 1789078516251WNPN7RSrv2KYhrzU
    01:16:08.908 I/FortaPush: Call invite: callId=17890785682588A5chUn7rfzoyZeL, …
    01:16:09.122 D/IncomingRinger: arm callId=17890785682588A5chUn7rfzoyZeL
    ```
    `Pre-rejected` назвал только A: JS после холодного старта отправил отказ
    за A по его собственной метке. Для B — `arm`, ни `Pre-rejected`, ни
    `Pre-accepted`, ни `onAnswer`; веб положил B примерно через 24 с.
  - **Сторона ответа.** «Принять» на рингере A (`adb input tap`) при
    заблокированном экране; веб положил A в 01:18:40; B через 13 с зазвонил,
    а не ответился сам:
    ```
    01:18:03.506 D/IncomingRinger: arm callId=17890786812305IsmC4Ol2lkj2x57
    01:18:13.642 D/CallConnection: onAnswer: callId=17890786812305IsmC4Ol2lkj2x57, roomId=!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app
    01:18:19.824 I/Capacitor/Console: [call-service] Pre-accepted incoming call, skipping ringer: 17890786812305IsmC4Ol2lkj2x57
    01:18:21.214 D/Capacitor/Console: sendEvent of type m.call.answer in !XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app …
    01:18:40.679 I/CallTeardown: endCall reason=DISCONNECT callId=17890786812305IsmC4Ol2lkj2x57 …
    01:18:53.975 I/FortaPush: Call invite: callId=1789078733276ij6MxAfkf1NqzpnM, …
    01:18:54.192 D/IncomingRinger: arm callId=1789078733276ij6MxAfkf1NqzpnM
    ```
    `Pre-accepted` назвал только A — это его собственный ответ, доведённый
    JS после холодного старта: от «Принять» до `m.call.answer` 7,6 с, у веба
    `connected/stable`. Для B — `arm`, ни `onAnswer`, ни `Pre-accepted`; веб
    положил B примерно через 24 с.
  - Гашение меток видно на каждом завершении (`To native … pluginId:
    NativeCall, methodName: retirePendingMarkers`).
- Статус: ☑ шаги 1-3 проверены на реальном Samsung SM-A528B (Android 14,
  WebView 151) 2026-09-09, звонящий — веб-клиент из той же комнаты, ответ на
  входящий подан через `adb input tap`.
  **Сторона ответа**, 21:17:46-21:18:20: `onCreateIncomingConnection` ->
  `IncomingRinger: arm` -> `onAnswer` -> при завершении
  `NativeCall.retirePendingMarkers {"callId":"1788977864809EhmZOJxf82ayh361",
  "roomId":"!XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app"}` -> перезвон через 13 с
  дал `onCreateIncomingConnection` с новым callId и `arm`, строки
  `Pre-accepted incoming call, skipping ringer` нет. Это тот самый симптом
  «я не снимал трубку — она сама снялась».
  **Сторона отказа**, 21:19:33-21:20:16: `arm` -> `no answer in 30s …
  auto-rejecting` -> `onReject` -> `CallTeardown: endCall reason=REJECT …
  audioMode=1 … actions=[]` -> `retirePendingMarkers` с той же парой ключей ->
  перезвон через 12 с зазвонил, строки `Pre-rejected incoming call` нет.
  В обоих прогонах `roomId` в запросе присутствует — правило «в комнате нет
  другого живого звонка» разрешило широкий матч, а это ровно та ветка, без
  которой гашение было бы инертным на push-пути.
  Шаг 4 (остаток) ☐ не проверен. Push-шаг (6 в списке) проверен 2026-09-10
  с заблокированным экраном на сборке `f83b30e7` с FCM — блок выше.

### Возраст метки pendingAnswer/pendingReject доезжает до JS
- Коммит: `e67f3863`
- Почему нужен человек: политика сопоставления покрыта
  `pending-call-marker.test.ts`, а прошивка стамп-сеттеров —
  `PendingMarkerStampContractTest`. Не покрыто ничем: что `ret.put("atMs", Long)`
  на JSObject доезжает в WebView числом, а не строкой, и что сквозной сценарий
  действительно чинится на аппарате.
- На чём: реальный аппарат + второй собеседник (веб-клиент годится).
- Шаги:
  1. Позвонить на аппарат и **не отвечать** — дать рингеру самому сработать на
     30-й секунде. В логе должно быть
     `IncomingRinger: no answer in 30s … — auto-rejecting` и `onReject`
     (именно этот путь пишет метку; если звонящий положил трубку первым, будет
     `reason=DISCONNECT` и метки не будет — тогда повторить).
  2. Подождать больше минуты и позвонить снова из той же комнаты.
  3. Аппарат обязан **зазвонить**: в логе есть `onCreateIncomingConnection`
     с новым callId и `IncomingRinger: arm`. Строки
     `[call-service] Pre-rejected incoming call` быть не должно.
  4. Отдельно: сразу после шага 1 позвонить снова в пределах минуты — тут
     старое поведение допустимо и звонок может быть отклонён по roomId; это
     осознанный компромисс ради холодного старта из пуша.
- Статус: ☑ шаги 1-3 проверены на реальном Samsung SM-A528B (Android 14,
  WebView 151) 2026-09-09, звонящий — веб-клиент из той же комнаты.
  Шаг 1, 19:11:26-19:11:57: `onCreateIncomingConnection callId=…VHB5Prgj7wvcs3uW`,
  `IncomingRinger: arm`, затем `no answer in 30s … auto-rejecting` -> `onReject`
  -> `CallTeardown: endCall reason=REJECT … audioMode=1 … actions=[]`. Живое
  приложение при этом само отправило `m.call.reject` через
  `[NativeCallBridge] Call declined`, так что метку никто не потребил — ровно
  то состояние, из которого раньше ломался следующий звонок.
  Шаги 2-3, 19:15:37 (метке 3 минуты, то есть больше TTL): `handleIncomingCall`
  прошёл насквозь без строки `Pre-rejected incoming call`, дальше
  `onCreateIncomingConnection callId=…tw7II5UU5wJ1O0sv` + `IncomingRinger: arm`
  + `MODE_RINGTONE` — аппарат зазвонил. `atMs` доезжает до JS числом: иначе
  сравнение возраста не сработало бы и звонок был бы отклонён по roomId.
  Шаг 4 проверен там же, 19:20:29-19:21:12, и подтвердил компромисс в полный
  рост: звонок А зазвонил и авто-сбросился в 19:20:59, перезвон через 13 секунд
  дал `[call-service] Pre-rejected incoming call, calling reject():
  …wZRHj3xutBX8VHfI` без `onCreateIncomingConnection` и без рингера. То есть
  «не успел взять трубку, звонящий сразу перезвонил» = второй звонок молча не
  доходит. Приложение при этом было живым и уже отправило `m.call.reject` само
  через слушатель `callDeclined`, так что метка была чистым остатком.
  Починено гашением обеих меток в `finalizeCall` — единственной точке, через
  которую проходит любой путь завершения. Гасится по callId **или** roomId:
  соединение, созданное из пуша, ключуется `data["call_id"] ?: data["event_id"]`,
  а этот homeserver кладёт в `call_id` именно event_id, который с Matrix
  callId не совпадает никогда — так что по одному callId гашение на push-пути
  было бы полностью инертным, а push и есть основная поверхность звонка.
  Проверка — в записи «Перезвон сразу после звонка доходит и звонит».

### Освобождение осиротевшего слота не рвёт звонок, который сняли параллельно
- Коммит: `0acd1dae`
- Почему нужен человек: гонку закрывает то, что проверка состояния и разрыв
  выполняются одним сообщением главного лупера — тем же, куда Telecom доставляет
  `onAnswer`. Юнит-тесты закрывают правило
  (`DisplacedConnectionPolicyTest.mayReleaseUnpresented`) и проводку
  (`IncomingCallSurfaceContractTest`), но сам ответ по Bluetooth или из системного
  экрана звонка воспроизводится только на живом аппарате.
- На чём: реальный аппарат с подключённой Bluetooth-гарнитурой.
- Шаги:
  1. `adb logcat -v time > /tmp/run.log &`.
  2. Позвонить с веба, не отвечать, смахнуть приложение во время звонка.
  3. Открыть приложение и сразу позвонить с веба ещё раз — так, чтобы
    `ensureIncomingCallVisible` попал на осиротевший слот.
  4. В момент второго звонка снять трубку **кнопкой на гарнитуре**.
     **Ожидается:** либо разговор идёт, либо в логе
     `Unpresented slot <callId> is no longer ringing (state=4) — leaving it alone`.
     **Недопустимо:** `Releasing an unpresented connection` сразу после
     `onAnswer` того же callId — это обрыв только что принятого разговора.
- Измерено 2026-09-13 на Samsung SM-A528B с AirPods Pro 2, сборка `0bdde967` с FCM
  (`scratchpad/run-stage3-orphan.sh orphan3a`, логи `scratchpad/runs/orphan3a-*`;
  время — часы телефона). Сценарий пошёл не по шагам записи: строки `Releasing an
  unpresented connection` не было, но нашлись два других дефекта.
  - Смахивание во время звонка A (02:38:03) глушит рингтон приложения, а соединение
    остаётся звонящим (`releaseOnTaskRemoved` оставляет его до таймаута) — в AirPods
    звенит дальше. Нажатие на ножку принимает его без экрана и без JS:
    ```
    02:38:14.298 BluetoothInCallService: BT - answering call(false)
    02:38:14.316 CallConnection: onAnswer: callId=17892562779348W0Y1OwUYehE4uAe
    02:38:14.318 CallConnection: onAnswer: JS listener not wired, queued for replay
    ```
    Отбой собеседника до телефона не дошёл: звонок висел в Telecom ACTIVE
    (`MODE_IN_COMMUNICATION`), холодный старт приложения его не тронул
    (`COLD_START … otherCallLive=true … actions=[]`), следующий входящий получил
    «занято». Снято `am force-stop` в 02:44.
  - Звонок B из той же комнаты соединился без ответа пользователя: JS применил
    отложенный ответ на A к B по совпадению комнаты, и микрофон телефона 41 с уходил
    собеседнику:
    ```
    02:38:19.947 [NativeCallBridge] Pending answer queued, waiting for matrixCall: 17892562779348W0Y1OwUYehE4uAe room: <room>
    02:38:20.138 [call-service] handleIncomingCall: callId=1789256297487pwCMsMV2ry9FSHda, type=voice
    02:38:21.942 [call-service] Pre-accepted incoming call, skipping ringer: 1789256297487pwCMsMV2ry9FSHda
    02:38:23.571 IncomingCallActivity: Call answered elsewhere — silencing ringer
    ```
  - Обе починки владелец отдал без его участия; запись перепроверить после них.
- Статус: ☐ не проверено

### Событие о завершившемся звонке не роняет текущий (push-путь)
- Коммит: `0acd1dae`
- Почему нужен человек: `callEnded`/`callDeclined` для соединения, созданного из
  пуша, приходят с `call_id`, в который этот homeserver кладёт event_id — он
  никогда не равен Matrix callId. Сверка идёт по комнате, а сам push-путь на
  локальной сборке недоступен: `google-services.json` в ней нет, все локальные
  звонки идут через `/sync`, где идентификаторы совпадают. Нужен release-APK из
  CI.
- На чём: реальный аппарат, release-сборка из CI (workflow «Android Test APK»).
- Шаги:
  1. Свернуть приложение, дождаться звонка через пуш (рингер из FCM).
  2. Отклонить звонок с нативного рингера.
     **Ожидается:** звонок завершается, в логе **нет** строки
     `callDeclined names … — ignoring` (отказ обязан дойти: id несравним, но
     комната совпадает).
  3. Известный незакрытый случай — записан здесь, чтобы не искать заново: два
     звонка **в одной комнате** внахлёст, где завершившийся создан из пуша. Их
     нечем различить, событие проходит, и JS кладёт трубку более новому звонку.
- Измерено 2026-09-10 на Samsung SM-A528B, локальная сборка с
  `google-services.json` (`scratchpad/runs/push-decline-*`,
  `push-decline-home-*`):
  - Предпосылка записи на этом homeserver сейчас не держится: `call_id` в пуше
    равен Matrix callId (`Call invite: callId=1789062887111e0D8kGYsMSy6q8ZN,
    eventId=$24trg…`), event_id приходит отдельным полем. Сверка по комнате,
    ради которой запись заведена, в обоих прогонах не понадобилась — id
    совпали точно.
  - Шаг 2, JS мёртв (приложение смахнуто): рингер из FCM, «Отклонить» →
    `onReject`; после холодного старта `[call-service] Pre-rejected incoming
    call, calling reject()` и `m.call.reject` через 4,1 с после `onReject`.
    Строк `— ignoring` нет. Событие в JS не приходило — отказ доехал меткой, а
    не через слушатель.
  - Шаг 2, JS жив (Home): `onReject` → `[NativeCallBridge] Call declined:
    <id>` через 25 мс → `m.call.reject` ещё через 13 мс. Строк `— ignoring`
    нет. Попутно найден дубль регистрации в Telecom — см. «Повторная
    регистрация звонящего вызова не завершает его в JS».
  - Не проверено: сам путь с несравнимыми id (event_id против callId) — на
    этом homeserver его сейчас не воспроизвести; шаг 3.
- Статус: ☐ не проверено

### iOS: метка получает возраст и перестаёт быть вечной
- Коммит: `f0387d90`
- Почему нужен человек: правка опирается на то, как ведёт себя `getActiveCalls()`
  в `@capgo/capacitor-incoming-call-kit` — как долго CallKit продолжает отдавать
  завершённый звонок и в каком состоянии. Логика проштампована юнит-тестами
  (`native-call-bridge.ios.test.ts`, блок «marker age»), само поведение CallKit
  — нет. Аппарата на iOS нет.
- На чём: реальный iPhone, сборка из Xcode.
- Шаги:
  1. Позвонить на iPhone и отклонить звонок с экрана CallKit.
  2. Подождать больше минуты, позвонить снова **из той же комнаты**.
     **Ожидается:** телефон звонит. **Раньше:** метка отказа не имела времени
     записи, сопоставление по комнате не старело никогда, и следующий звонок
     мог быть отклонён без звонка.
  3. Контроль холодного старта: убить приложение, позвонить, принять на экране
     CallKit — приложение должно поднять разговор (метке в этот момент
     несколько секунд, TTL 60 с её не трогает).
- Что считается провалом: звонок из шага 3 не поднимается — значит штамп
  ставится слишком рано (не в момент первого наблюдения, а раньше) или CallKit
  отдаёт звонок с большой задержкой, и 60 с не хватает.
- Статус: ☐ не проверено

### Решение на рингере не теряется из-за метки другого звонка
- Коммит: `f6c12385`
- Почему нужен человек: состояние собирается только через пуш и только когда
  Telecom **отказал** новому звонку (BUSY, потому что слот занят разговором) —
  на локальной отладочной сборке пуша нет. Правило (`PendingCallMarker.seeded`)
  закрыто юнит-тестами; на аппарате проверяется, что путь действительно такой.
- На чём: реальный аппарат, release-сборка из CI, три аккаунта.
- Шаги:
  1. Аккаунт A звонит, ответить, говорить (метка звонка A стоит и не снимется,
     пока разговор идёт).
  2. Аккаунт B звонит из **другой** комнаты — приходит пуш, рингер поверх
     разговора, Telecom отвечает BUSY.
  3. Нажать «Принять» на рингере B.
     **Ожидается:** метка `pendingAnswer` теперь принадлежит звонку B. Если
     сразу после этого WebView перезапустится (или процесс переживёт перезапуск
     JS), при старте в логе `Pending answer queued` должен стоять **id звонка
     B**.
     **Раньше:** `seedPendingAnswerIfEmpty` видел непустую метку A и не писал
     ничего — решение пользователя терялось, а при следующем старте JS
     переигрывалась метка звонка A.
  4. Проверить, что обычный путь не сломан: одиночный входящий по пушу,
     «Принять» → звонок отвечается, в логе одна метка со своим id.
  5. Зеркальная половина того же дефекта — стирание чужой метки. Пока метка
     звонка A не прочитана, показать рингер звонка B и **отклонить** его (а во
     втором прогоне — дать звонящему B положить трубку самому, чтобы сработал
     `dismissByRemote`).
     **Ожидается:** метка звонка A на месте; при следующем старте JS
     переигрывает именно её.
     **Раньше:** обе ветки делали `pendingAnswer = PendingCallMarker.NONE`
     без разбора и выбрасывали ответ, который пользователь уже дал.
- Измерено 2026-09-10 на Samsung SM-A528B ↔ веб TEST1 + веб `test23438111`,
  сборка `f83b30e7` с FCM (логи `scratchpad/runs/3way-accept1-*`,
  `3way-decline2-*`).
  - Шаги 1–2 проходят: A в разговоре, пуш B из другой комнаты, рингер поверх,
    BUSY.
  - Шаги 3 и 5 на этой сборке так, как описаны, не воспроизводятся: во время
    разговора метки звонка A уже нет. `reportCallConnected` снимает её, как
    только ответ ушёл в сеть (`retirePendingMarkersForCall(it.callId, null)` в
    `CallPlugin.kt`). В `3way-accept1` это случилось через 2,4 с после
    `onAnswer` A (22:38:06.189 → 22:38:08.599), а «Принять» на рингере B нажато
    через 41 с. Метка B ложится в пустой слот — старый
    `seedPendingAnswerIfEmpty` сделал бы то же самое, так что прогон фикс не
    отличает. С «Отклонить» (`3way-decline2`) то же: метка A снята задолго до
    нажатия. Окно в 2,4 с между ответом A и соединением через adb не поймать:
    второй пуш и тап занимают больше.
  - Шаг 4 проходит: одиночный входящий по пушу (A) отвечен с рингера,
    `matrixCall ready, answering (matchById=true, matchByRoom=false)`.
- Статус: ☐ не проверено

### Завершение соседнего соединения не роняет звонок в той же комнате
- Коммит: `06389f92`
- Почему нужен человек: нужны два соединения **в одной комнате**, наложенные во
  времени, где завершившееся создано пушем (его id — event_id, с Matrix callId
  не сравним). Юнит-тесты закрывают правило; воспроизведение требует пуша.
- На чём: реальный аппарат, release-сборка из CI, второй аккаунт.
- Что именно закрыто: если JS усыновил свой звонок по метке с **точным**
  совпадением id — то есть соединение под ним заведено по Matrix callId
  (маршрут `/sync`), — то push-событие о завершении из той же комнаты заведомо
  про другое соединение и отбрасывается.
  Обратный случай (свой звонок тоже создан пушем) **не закрыт сознательно**:
  сопоставить его по комнате — значит угадать, а неверно записанный ключ
  навсегда заглушил бы teardown собственного звонка, то есть привёл бы к
  «залипанию в режиме звонка» — худшей из двух ошибок. Подробности в шапке
  `native-call-event-scope.ts`.
- Шаги:
  1. Звонок из комнаты R по `/sync` (приложение открыто), принять.
  2. Пока разговор идёт, добиться push-соединения из той же комнаты R (второй
     входящий, Telecom отвечает BUSY) и дождаться его завершения.
  3. **Ожидается:** разговор жив; в логе `callEnded names $… , but JS holds …`.
     **Раньше:** событие о чужом push-соединении нельзя было отличить от
     события о своём звонке (одна комната), и JS клал трубку.
  4. Обратная проверка, что teardown не потерян: принять звонок и завершить его
     **с гарнитуры или из системного экрана звонка** (не из приложения) — JS
     обязан завершить звонок, аудиорежим вернуться в `MODE_NORMAL`.
- Измерено 2026-09-10 на Samsung SM-A528B, сборка `f83b30e7` с FCM (логи
  `scratchpad/runs/3way-decline2-*`, `3way-accept1-*`).
  - Шаги 1–3 на этом homeserver не воспроизводятся. Пуш теперь несёт Matrix
    callId, а не event_id (см. «Повторная регистрация звонящего вызова…»):
    соединение из пуша заведено под тем же id, что и звонок в JS. Id события
    и звонка сравнимы, и `nativeCallEventAppliesTo` решает по точному
    равенству раньше, чем доходит до ветки `nativeKey`, ради которой фикс.
    Эта ветка срабатывает только для id вида `$…`.
  - Второй входящий во время разговора соединения не создаёт вовсе: Telecom
    отвечает BUSY на обе регистрации, так что `callEnded` о нём не приходит.
  - Шаг 4 (завершить с гарнитуры) переносится в сессию с AirPods.
- Статус: ☐ не проверено

### Видео собеседника показывается на нативном экране звонка
- Коммит: `1c4ff317`
- Почему нужен человек: тесты доказывают две вещи. Прокси складывает аудио и
  видео одного msid в один `MediaStream`, а `call-service` сообщает экрану
  звонка, что видео пошло. Показывает ли `CallActivity` картинку собеседника
  вместо заглушки, видно только на аппарате.
- На чём: реальный Android с нативным движком WebRTC; годится Samsung
  SM-A528B ↔ веб TEST1 с фейковой камерой Chrome
  (`scratchpad/run-rear-camera.sh`).
- Найдено 2026-09-12 при проверке «Тыловая камера не зеркалится» (`rearcam2`,
  `rearcam3`). Кадры собеседника приходили, а на экране оставалась заглушка «TE».
  Через ~50 мс после `onAddTrack: video` JS присылал
  `updateRemoteVideoState {"muted":true}`, и `CallActivity` прятал
  `remote_video`: `surfaceDestroyed`, дальше
  `EglRenderer: remote_video Dropping frame - No surface` до конца звонка.
  - Причина. Прокси создавал новый `MediaStream` на каждый трек. SDK строил фид
    из потока с одним аудиотреком, а второй поток отбрасывал
    (`pushRemoteFeed() ignoring stream because we already have a feed for it`).
  - Итог: `isVideoMuted()` оставался true весь звонок, и новое значение экрану
    не отправлялось.
- Шаги:
  1. Видеозвонок с телефона вебу, веб отвечает с камерой, приложение уже
     запущено (тёплый старт). На холодном старте этот шаг проверяется только
     вместе с записью «Своё видео в первом видеозвонке после холодного старта»
     (`19a2a4cb`): до неё `CallActivity` при первом видеозвонке не привязывал
     рендерер собеседника вовсе.
     - **Ожидается:** на телефоне видео собеседника вместо заглушки. В логе
       после `onAddTrack: video` последний `updateRemoteVideoState` —
       `{"muted":false}`, серии `remote_video Dropping frame - No surface`
       после соединения нет.
     - **Раньше:** заглушка с инициалами весь звонок, последний
       `updateRemoteVideoState` — `{"muted":true}`.
  2. То же во входящем видеозвонке с веба на телефон.
  3. Веб выключает камеру посреди звонка — на телефоне заглушка; включает —
     видео снова. Выключенная камера собеседника по-прежнему прячет картинку.
  4. Голосовой звонок: у телефона экран голосового режима, в логе нет
     `updateRemoteVideoState` с `{"muted":false}`.
- Измерено 2026-09-14 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5` с
  FCM. Видеозвонок телефон → веб TEST1 с фейковой камерой Chrome, скрипт
  `scratchpad/run-rear-camera.sh`. Логи и кадры — `scratchpad/runs/rearcold2-*`
  (первый видеозвонок после холодного старта) и `rearwarm2-*` (второй в том же
  процессе); кадры остаются локально.
  - **Шаг 1 прошёл в обоих прогонах.** Владелец видел движущуюся зелёную картинку
    веба вместо заглушки, снимки экрана показывают то же. По часам телефона
    (`rearcold2`):
    ```
    23:54:06.710 D/NativeWebRTCManager: [pc_1_…] onAddTrack: video
    23:54:06.749 V/Capacitor: … pluginId: NativeWebRTC, methodName: updateRemoteVideoState, methodData: {"muted":true}
    23:54:06.750 V/Capacitor: … pluginId: NativeWebRTC, methodName: updateRemoteVideoState, methodData: {"muted":false}
    23:54:07.683 I/org.webrtc.Logging: SurfaceEglRenderer: remote_video: Reporting first rendered frame.
    23:54:07.684 I/org.webrtc.Logging: SurfaceEglRenderer: remote_video: Reporting frame resolution changed to 640x360 with rotation 0
    ```
    - После первых секунд `remote_video` рисовал 12–20 кадров/с до отбоя, строк
      `Dropping frame - No surface` нет.
    - В `rearwarm2` так же: последнее значение `{"muted":false}`, 13–20 кадров/с,
      ни одного `Dropping frame`.
  - Шаги 2–4 не проверялись.
- Статус: ☐ шаг 1 прошёл 2026-09-14 на холодном и тёплом старте; шаги 2–4 не проверены

### Своё видео в первом видеозвонке после холодного старта
- Коммит: `19a2a4cb`
- Почему нужен человек: контракт-тест доказывает только, что GL-контекст
  создаётся при первом обращении и фабрика WebRTC берёт тот же. Рисуются ли
  окошко своего видео и видео собеседника в первом звонке после холодного
  старта, видно только на аппарате.
- На чём: реальный Android; годится Samsung SM-A528B ↔ веб TEST1
  (`scratchpad/run-rear-camera.sh` на холодном старте).
- Найдено 2026-09-12 (`rearcam2`): в первом видеозвонке после холодного старта
  окошко своего видео чёрное весь звонок.
  - По часам телефона `initVideoRenderers: eglBase is null!` в 16:06:14.652, а
    `Initialized with HW acceleration` — только в 16:06:15.304.
  - Экран звонка открывался раньше, чем плагин создавал фабрику и вместе с ней
    контекст. Рендереры в этом звонке не настраивались вовсе, ни свой, ни
    собеседника; повторная попытка была только в `toggleVideo`.
- Шаги:
  1. Свернуть приложение, убить процесс (`am kill com.forta.chat`), открыть
     приложение, дождаться списка чатов и позвонить вебу видеозвонком.
     - **Ожидается:** в логе нет `initVideoRenderers: eglBase is null!`,
       `initVideoRenderers: renderers initialized` идёт раньше
       `Initialized with HW acceleration`. Окошко своего видео показывает
       камеру; после ответа веба на экране видео собеседника (вместе с
       записью «Видео собеседника показывается на нативном экране звонка»).
     - **Раньше:** `eglBase is null!` и чёрное окошко весь звонок.
  2. Второй видеозвонок в том же процессе — окошко и видео собеседника
     по-прежнему есть: контекст один на процесс.
  3. Входящий видеозвонок с веба на холодном старте — окошко своего видео и
     видео собеседника есть.
  4. Регресс WEE-47: до первого звонка после холодного старта `RECORD_AUDIO`
     без `(running)`; голосовой звонок соединяется, звук в обе стороны.
- Измерено 2026-09-14 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5` с
  FCM. `scratchpad/run-rear-camera-cold.sh`: Home, `am kill com.forta.chat`, затем
  `run-rear-camera.sh`. Логи `scratchpad/runs/rearcold2-*` и `rearwarm2-*`.
  - **Шаг 1 прошёл.** Процесс 4469 убит в 23:53:42 по часам телефона, новый процесс
    5689 набрал вебу видеозвонок. `eglBase is null` — ни одной строки, рендереры
    настроены раньше фабрики, окошко своего видео рисует с первых секунд; владелец
    видел изображение сразу:
    ```
    23:54:00.773 D/CallActivity: initVideoRenderers: initializing, remoteView visible=false
    23:54:00.786 D/CallActivity: initVideoRenderers: renderers initialized, remote renderer attached
    23:54:01.415 D/NativeWebRTCManager: Initialized with HW acceleration
    23:54:01.486 D/NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
    23:54:03.416 I/org.webrtc.Logging: SurfaceEglRenderer: local_video: Reporting first rendered frame.
    23:54:08.798 I/org.webrtc.Logging: EglRenderer: local_videoDuration: 4002 ms. Frames received: 99. Dropped: 0. Rendered: 98. …
    ```
    Видео собеседника в том же звонке есть — см. «Видео собеседника показывается
    на нативном экране звонка».
  - **Шаг 2 прошёл** (`rearwarm2`, тот же процесс 5689): `initVideoRenderers:
    renderers initialized`, первые кадры своего и чужого видео, своё — 15–25
    кадров/с, собеседник — 13–20; `eglBase is null` нет.
  - Шаги 3–4 не проверялись.
  - Попутно, `rearcold1`: скрипт нажал видеозвонок через 13 с после холодного
    старта, и звонок молча не ушёл — `[call-service] No Matrix client`, без
    сообщения пользователю. Эта починка тут ни при чём: `startCall` выходит, ничего
    не показав, пока клиент Matrix не создан. Вынесено отдельной задачей; скрипт
    теперь ждёт `matrixReady`.
- Статус: ☐ шаги 1–2 прошли 2026-09-14; шаги 3–4 не проверены

### Отложенный ответ на один звонок не принимает следующий из той же комнаты
- Коммит: `d25ea8a8`
- Почему нужен человек: юнит-тесты доказывают правило. Отложенный ответ или
  отказ с настоящим Matrix callId подходит только своему звонку, а по комнате
  сопоставляется лишь метка с event_id или без id. Что следующий звонок после
  ответа гарнитурой на смахнутый звонок звонит, а не соединяется сам, видно
  только на аппарате: нужны смахивание, ответ по Bluetooth без JS и холодный
  старт.
- На чём: реальный Android с Bluetooth-гарнитурой; годится Samsung SM-A528B с
  AirPods Pro 2 ↔ веб TEST1, сборка с FCM (`scratchpad/run-stage3-orphan.sh`).
- Найдено 2026-09-13 при проверке «Освобождение осиротевшего слота не рвёт
  звонок, который сняли параллельно» (`orphan3a`).
  - Звонок A смахнули во время звонка, нажатие на ножку AirPods приняло его без
    JS. На следующем старте `wire()` взял отложенный ответ с настоящим callId A.
  - Через 6 с пришёл звонок B из той же комнаты. `consumePendingAnswerCallId`
    сопоставил метку A с B по комнате: `matchesPendingCallMarker` сравнивал
    комнату для любой метки моложе жизни invite.
  - B соединился без участия пользователя, микрофон телефона 41 с уходил
    собеседнику.
- Шаги:
  1. `adb logcat -v time > /tmp/run.log &`.
  2. Позвонить с веба, не отвечать, смахнуть приложение во время звонка. Пока
     звонок звенит в гарнитуре, принять его нажатием на ножку.
  3. Открыть приложение и сразу позвонить с веба ещё раз из того же чата.
     - **Ожидается:** второй звонок сам не соединяется. В логе нет
       `Pre-accepted incoming call` с callId второго звонка, у веба нет
       соединения без нажатия на телефоне.
     - **Раньше:** `Pre-accepted incoming call, skipping ringer: <callId B>`
       через ~2 с после `handleIncomingCall`, звонок соединялся, звук с
       микрофона телефона шёл вебу.
     - Первый звонок, принятый без JS, держит слот Telecom до 90 с (запись
       «Звонок, принятый без JS, не висит ACTIVE бесконечно»), и второй звонок в
       это время получает «занято».
  4. Регресс холодного старта из пуша: свернуть приложение, убить процесс
     (`am kill com.forta.chat`), позвонить с веба и принять на экране
     входящего до загрузки приложения. **Ожидается:** в логе
     `Pre-accepted incoming call` или `matrixCall ready, answering
     (matchById=true` с callId этого же звонка, звук в обе стороны. Метку с event_id (пуш без `call_id`) этот сервер не шлёт — тот
     путь закрыт только тестами.
- Измерено 2026-09-15 (UTC) с владельцем на Samsung SM-A528B (Android 14), сборка
  `044f25f5` с FCM, AirPods Pro 2 (`scratchpad/run-headset-orphan.sh`, логи
  `scratchpad/runs/ho-reopen1-*` и `ho-talkcold1-*`, время по часам телефона).
  - **Шаги 2–3 прошли** (`ho-reopen1`). Скрипт смахнул приложение во время звонка A,
    владелец принял его нажатием на ножку, скрипт сразу открыл приложение, веб
    положил трубку и тут же позвонил снова (B):
    ```
    04:04:40.659 I/BluetoothInCallService: BT - answering call(false)
    04:04:40.680 D/CallConnection: onAnswer: callId=17894342616195kOdJDHdVurwedgx, roomId=!…
    04:04:41.924 I/CallTeardown: endCall reason=COLD_START callId=null State(audioMode=3, otherCallLive=true, …)
    04:04:44.553 I/Capacitor/Console: [NativeCallBridge] Pending answer queued, waiting for matrixCall: 17894342616195kOdJDHdVurwedgx room: !…
    04:05:03.084 W/CallConnectionService: Incoming call while a call is established — reporting busy
    04:05:14.937 W/Capacitor/Console: [NativeCallBridge] Timed out waiting for matrixCall: 17894342616195kOdJDHdVurwedgx
    ```
    За 35 с после звонка B без нажатий: `Pre-accepted incoming call` — 0, ответов JS и
    `onAnswer` — 0, `reporting busy` — 2 (регистрации пуша и JS). Отложенный ответ с
    callId A ждал только A и истёк. Владелец: приложение открылось само, второй звонок
    звонил, на него не отвечали.
  - Попутно: для B, уже получившего «занято», пуш через 160 мс всё равно поднял рингер
    (`IncomingRinger: arm callId=1789434302209xTlBSEljOZ394BXp`), и звонок звонил 30 с до
    `no answer in 30s … auto-rejecting`. Вынесено отдельной задачей.
  - **Шаг 4 прошёл** (`ho-talkcold1`). Приложение смахнуто до звонка (`Killing 26802 …
    remove task`), звонок поднял процесс пушем, скрипт нажал «Принять» до загрузки JS:
    ```
    04:10:19.949 I/ActivityManager: Start proc 31219:com.forta.chat/u0a294 for broadcast {com.forta.chat/com.google.firebase.iid.FirebaseInstanceIdReceiver}
    04:10:31.017 D/IncomingCallActivity: Accept pressed
    04:10:31.037 D/CallConnection: onAnswer: callId=1789434618863TSC8C4IF4PqxoKFK, roomId=!…
    04:10:35.749 D/Capacitor/Console: FetchHttpApi: <-- POST …/_matrix/client/v3/login [538ms 200]
    04:10:37.509 I/Capacitor/Console: [call-service] Pre-accepted incoming call, skipping ringer: 1789434618863TSC8C4IF4PqxoKFK
    04:10:39.547 V/Capacitor: … methodName: reportCallConnected, methodData: {"callId":"1789434618863TSC8C4IF4PqxoKFK"}
    ```
    Разговор шёл 2,5 минуты, у веба `connected/stable`. От телефона к вебу шла почти
    одна тишина: 14,8 КБ за 150 с, энергия 0,0027 к 60-й секунде и дальше не росла.
    Слышен ли звук веба в AirPods, владелец пока не ответил.
- Статус: ☐ шаги 2–3 прошли 2026-09-15; шаг 4 — ответ до загрузки подхвачен с тем же
  callId, звук в обе стороны не подтверждён

### «Динамик» переключает звук через Telecom
- Коммит: `4adc0eec`
- Почему нужен человек: тесты доказывают три вещи. Устройства переводятся в
  маршруты Telecom (`TelecomAudioRouteTest`). Правило пина действует и для смены
  маршрута самим Telecom (`AudioRoutePolicyTest`). Проводка на месте
  (`TelecomAudioRouteContractTest`): выбор уходит в соединение Telecom —
  `requestCallEndpointChange` на Android 14+, `setAudioRoute` раньше, — а маршрут,
  о котором сообщил Telecom, показывается в интерфейсе. Слышно ли громкий динамик и
  как ведёт себя настоящая гарнитура, видно только на аппарате.
- На чём: реальный Android 14+; отдельно — Android 13 или ниже (путь
  `setAudioRoute`). Годится Samsung SM-A528B с AirPods Pro 2 ↔ веб TEST1
  (`scratchpad/run-stage3-route.sh`).
- Найдено 2026-09-13 при проверке «Громкая связь: отказ и пин ручного выбора».
  Аудиорежимом звонка владеет Telecom, и `setCommunicationDevice` из `AudioRouter`
  игнорировался: за 16 выборов «Динамик» звуковая политика ни разу не выбрала
  громкий динамик.
- Шаги:
  1. `adb logcat -v time > /tmp/run.log &`. Голосовой звонок с веба, ответить на
     телефоне.
  2. «Аудио» → «Динамик».
     - **Ожидается:** звук из решётки на нижнем торце. В логе
       `requestCallEndpointChange(SPEAKER): done`, у Telecom
       `Entering state ActiveSpeakerRoute`, у звуковой политики
       `selected devices {AUDIO_DEVICE_OUT_SPEAKER`. Значок «Аудио» — динамик.
     - **Раньше:** звук оставался в верхней части телефона, Telecom не выходил из
       `ActiveEarpieceRoute`.
  3. «Аудио» → «Динамик телефона» — звук снова в верхней части телефона; ещё раз
     «Динамик» — снова внизу.
  4. При включённом динамике подключить AirPods.
     - **Ожидается:** звук остаётся на динамике или возвращается на него через
       мгновение, в логе `restoring pinned SPEAKER`.
     - Выбрать AirPods — звук в AirPods; убрать AirPods в кейс — звук в верхней
       части телефона.
  5. Видеозвонок: звук сразу из решётки на нижнем торце, в логе
     `requestCallEndpointChange(SPEAKER): done` после `start(video)`.
  6. Переключатель динамика в окне звонка приложения — то же, что шаг 2.
- Измерено 2026-09-15 (UTC) с владельцем на Samsung SM-A528B (Android 14), сборка
  `044f25f5` с FCM, AirPods Pro 2. Голосовой звонок с веба TEST1 (`route4`,
  `scratchpad/run-stage3-route.sh`), видеозвонки с телефона (`rearmirror1`) и с веба
  (`route7v`, тот же скрипт с `VIDEO=1`), разговор с ответом ножкой (`ho-talkopen2`);
  логи в `scratchpad/runs/`, время по часам телефона.
  - **Шаги 2–3 прошли** (`route4`). Каждый выбор на шторке доходит до Telecom и
    звуковой политики; владелец слышал звук снизу после «Динамик» и сверху после
    «Динамик телефона»:
    ```
    03:01:08.728 D/AudioRouter: setDevice: SPEAKER (pinned)
    03:01:09.450 I/Telecom: … Entering state ActiveSpeakerRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_SPEAKER(cfc)@Gx0
    03:01:09.458 V/APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_SPEAKER, @:}
    03:01:09.481 D/CallConnection: requestCallEndpointChange(SPEAKER): done
    03:01:18.520 D/AudioRouter: setDevice: EARPIECE (pinned)
    03:01:18.522 I/Telecom: … Entering state ActiveEarpieceRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_EARPIECE(cfc)@GyA
    03:01:18.570 V/APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_EARPIECE, @:}
    ```
  - **Шаг 4, подключение AirPods — прошло.** Telecom сам увёл звонок в гарнитуру,
    приложение вернуло закреплённый динамик за 0,9 с (на слух владелец этот момент
    отдельно не подтверждал):
    ```
    03:02:30.167 I/Telecom: … Entering state ActiveBluetoothRoute: BSR.oR->CARSM.pM_BT_ACTIVE_DEVICE_PRESENT->…
    03:02:31.014 D/AudioRouter: Telecom moved the call to BLUETOOTH — restoring pinned SPEAKER
    03:02:31.889 I/Telecom: … Entering state ActiveSpeakerRoute: …->CSW.rCEC->CARSM.pM_USER_SWITCH_SPEAKER(cast)@E-E-…
    03:02:31.905 D/CallConnection: requestCallEndpointChange(SPEAKER): done
    ```
  - **Шаг 4, «Выбрать AirPods» — провалено.** С динамика выбор AirPods ничего не
    меняет, 5 раз из 5 (03:02:44, 03:03:22, 03:03:49, 03:04:13, 03:04:26). Telecom
    отвечает успехом через 15–30 мс, но в `ActiveBluetoothRoute` не переходит,
    гарнитуру не подключает (`connectAudio()` нет), звуковая политика остаётся на
    динамике:
    ```
    03:03:49.576 D/AudioRouter: setDevice: BLUETOOTH (pinned)
    03:03:49.589 V/APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_SPEAKER, @:}
    03:03:49.605 D/CallConnection: requestCallEndpointChange(BLUETOOTH): done
    03:04:10.060 D/AudioRouter: setDevice: SPEAKER (pinned)
    ```
    С «Динамика телефона» тот же выбор сработал 4 раза из 5:
    ```
    03:03:44.295 D/AudioRouter: setDevice: BLUETOOTH (pinned)
    03:03:44.311 I/BluetoothHeadset: connectAudio()
    03:03:44.314 I/Telecom: … Entering state ActiveBluetoothRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_BLUETOOTH->BRM.pM_100->CARSM.pM_BT_AUDIO_CONNECTED…
    03:03:44.346 D/CallConnection: onCallEndpointChanged: 1789430434328qlnkDJlanqHMlEBQ -> BLUETOOTH
    ```
    Владелец: с «Динамика» на AirPods звук иногда не переключался, а через «Динамик
    телефона» иногда переключался. До нашего `onError` отказ не доходит, поэтому в логе
    приложения провала не видно.
  - Шаг 4, «убрать AirPods в кейс» — прошло по логу в голосовом звонке
    (`ho-talkopen2`): после выбора AirPods и кейса звонок перешёл в трубку и у Telecom,
    и у звуковой политики. На вопрос, откуда звук после кейса, владелец ответил только,
    что звук был в наушниках, пока они были подключены:
    ```
    03:51:16.700 D/AudioRouter: setDevice: BLUETOOTH (pinned)
    03:51:27.951 I/Telecom: … Entering state ActiveEarpieceRoute: BSR.oR->BRM.pM_201->CARSM.pM_BT_AUDIO_DISCONNECTED@HTc
    03:51:27.976 V/APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_EARPIECE, @:}
    03:51:28.951 D/CallConnection: onCallEndpointChanged: 1789433316759339IPRcYQc8yeL5A -> EARPIECE
    ```
  - **Шаг 5 прошёл.** В трёх видеозвонках (два с телефона в `rearmirror1`, один с веба в
    `route7v`) динамик запрошен сразу после `start(video)`, Telecom и звуковая политика
    согласны. После `rearmirror1` владелец ответил «скорее сверху»; в `route7v` владелец
    закрыл пальцем решётку на нижнем торце, и звук заметно стих — он шёл снизу:
    ```
    03:41:46.630 D/AudioLifecycle: start(video): set mode=MODE_IN_COMMUNICATION, initial active=SPEAKER
    03:41:47.175 I/Telecom: … Entering state ActiveSpeakerRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_SPEAKER(cfc)@HLI
    03:41:47.416 D/CallConnection: requestCallEndpointChange(SPEAKER): done
    03:41:47.505 V/APM_AudioPolicyManager: getOutput() stream 0 selected devices {AUDIO_DEVICE_OUT_SPEAKER, @:}, output 13
    ```
    Попытки `route5v` и `route6v` не в счёт: веб не нашёл кнопку видеозвонка (в личном
    чате она в «Инфо» → «Ещё»), потом звонок пропустили и рингер закрылся по своему
    30-секундному сроку.
  - **Видеозвонок после AirPods — провалено** (остаточный риск этой записи). В `route7v`
    AirPods подключились посреди видеозвонка, звук ушёл в них. Когда AirPods убрали в
    кейс, Telecom сам перевёл звонок в трубку, и приложение до конца звонка динамик
    обратно не запросило; владелец: звук «скорее сверху»:
    ```
    03:42:32.731 I/Telecom: … Entering state ActiveBluetoothRoute: BSR.oR->CARSM.pM_BT_ACTIVE_DEVICE_PRESENT->…
    03:42:33.665 D/CallConnection: onCallEndpointChanged: 1789432898580IiStHgeULYUe0Fy4 -> BLUETOOTH
    03:42:59.011 I/Telecom: … Entering state ActiveEarpieceRoute: BSR.oR->BRM.pM_201->CARSM.pM_BT_AUDIO_DISCONNECTED@HNA
    03:42:59.036 V/APM_AudioPolicyManager: getNewOutputDevices selected devices {AUDIO_DEVICE_OUT_EARPIECE, @:}
    03:42:59.707 D/CallConnection: onCallEndpointChanged: 1789432898580IiStHgeULYUe0Fy4 -> EARPIECE
    ```
  - **Шаг 6 прошёл** (`route4`): кнопка громкой связи в окне звонка приложения (нажата
    через CDP, `scratchpad/run-route-toggle.sh`) увела звонок в трубку и вернула на
    динамик; владелец слышал, как звук ушёл наверх и вернулся вниз:
    ```
    03:01:36.670 D/AudioRouter: setDevice: EARPIECE (pinned)
    03:01:36.675 I/Telecom: … Entering state ActiveEarpieceRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_EARPIECE(cfc)@GzA
    03:01:37.510 D/CallConnection: requestCallEndpointChange(EARPIECE): done
    03:01:44.126 D/AudioRouter: setDevice: SPEAKER (pinned)
    03:01:44.922 I/Telecom: … Entering state ActiveSpeakerRoute: CSW.rCEC->CARSM.pM_USER_SWITCH_SPEAKER(cfc)@Gzk
    03:01:44.928 D/CallConnection: requestCallEndpointChange(SPEAKER): done
    ```
  - Android 13 и ниже (путь `setAudioRoute`) не проверялся — нет аппарата.
- Статус: ☐ шаги 2, 3, 5, 6 прошли 2026-09-15; шаг 4 провален: выбор AirPods с динамика
  не срабатывает, а видеозвонок после ухода AirPods остаётся в трубке

---

## Проверено

### Превью задней камеры на нативном экране звонка не зеркалится
- Коммит: `2612eaee`
- Почему нужен человек: контракт-тест доказывает только одно. Зеркало превью
  ставит менеджер по камере, которая сейчас открыта: при старте камеры, при
  привязке окошка и после переключения. Как превью выглядит, видно только на
  аппарате с настоящими объективами.
- На чём: реальный Android; годится Samsung SM-A528B ↔ веб TEST1
  (`scratchpad/run-rear-camera.sh`).
- Найдено 2026-09-12 на шаге 2 записи «Тыловая камера не зеркалится»
  (`rearcam3`): на нативном экране звонка превью задней камеры было зеркальным.
  - `CallActivity.initVideoRenderers` один раз ставил
    `localVideoView.setMirror(true)`, а `switchCamera()` зеркало не трогал.
  - Собеседник получал незеркальную картинку с обеих камер.
- Шаги:
  1. Видеозвонок с телефона вебу; в окошке своего видео фронтальная камера.
     **Ожидается:** превью зеркальное — поднятая правая рука в окошке справа,
     как в зеркале.
  2. Переключить на заднюю камеру и навести на текст.
     - **Ожидается:** в окошке текст читается нормально, в логе
       `Camera switched, front: false`.
     - **Раньше:** текст задом наперёд.
  3. Переключить обратно на фронтальную — превью снова зеркальное.
  4. У веба картинка с обеих камер незеркальная, текст читается: зеркалится
     только превью.
  5. Завершить звонок на задней камере и позвонить снова. Звонок начинается с
     фронтальной, превью зеркальное.
- Измерено 2026-09-14 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5` с
  FCM, те же прогоны `rearcold2` и `rearwarm2` (`scratchpad/run-rear-camera.sh`).
  Камера переключалась через `NativeWebRTC.switchCamera` — тот же `switchCamera()`
  менеджера, что у кнопки. Владелец держал перед камерой обложку книги с крупным
  текстом; кадры остаются локально.
  - Шаг 1 прошёл: на фронтальной камере текст в окошке зеркальный — владелец.
  - **Шаг 2 прошёл** в обоих прогонах: `Camera switched, front: false`, текст в
    окошке читается нормально — владелец и снимки экрана `back-phone.png`.
  - Шаг 3 прошёл: после `Camera switched, front: true` превью снова зеркальное
    (снимок `rearwarm2-shots/front2-phone.png`).
  - Шаг 4 прошёл: у веба кадры с фронтальной и задней камеры незеркальные, текст
    читается (`rearcold2-shots/front-frame.png`, `back-frame.png`).
  - Шаг 5 не проверялся: оба звонка завершились на фронтальной камере.
- Измерено 2026-09-15 (UTC), тот же аппарат и сборка, без владельца
  (`scratchpad/run-rear-redial.sh`, логи `scratchpad/runs/rearredial1-*`).
  - **Шаг 5, камера — прошло.** Видеозвонок вебу, переключение на заднюю, отбой на
    задней кнопкой в JS. Через 26 с второй видеозвонок снова начался с
    фронтальной:
    ```
    01:40:29.429 D/NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
    01:40:39.198 D/NativeWebRTCManager: Camera switched, front: false
    01:40:45.285 D/CallConnection: onDisconnect: 1789425628227lDiYdBwm0HNdae93
    01:41:37.515 D/NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
    ```
    Камера 1 на этом аппарате — `CAMERA_FACING_FRONT` (`CameraManagerGlobal`).
  - Шаг 5, зеркало превью — не проверено: менеджер не пишет зеркало в лог, снимок
    экрана не делали. Нужен взгляд на окошко во втором звонке.
  - Попутно: второй звонок открыл экран через 23 с после `CallActivity created`, в
    первом — через 0,5 с. Держала система, не приложение: в эти секунды
    `system_server` писал `Slow dispatch took 30084ms android.bg` и `Long monitor
    contention … ProxyManager.updateTimeoutsIfNeeded`. После паузы звонок ушёл
    через 0,8 с.
- Измерено 2026-09-15 (UTC) с владельцем, тот же аппарат и сборка, AirPods в кейсе
  (`scratchpad/run-rear-redial.sh`, логи `scratchpad/runs/rearmirror1-*`).
  - **Шаг 5 прошёл целиком.** Первый видеозвонок скрипт переключил на заднюю камеру и
    завершил на ней; второй через 24,5 с снова начался с фронтальной:
    ```
    03:21:08.549 D/NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
    03:21:18.298 D/NativeWebRTCManager: Camera switched, front: false
    03:21:23.349 D/CallConnection: onDisconnect: 1789431667571ruM74sa7i1ZQDsVp
    03:21:47.874 D/NativeWebRTCManager: Local video started with camera: 1 (peerId=, pcs=0)
    ```
    Во втором звонке владелец смотрел на окошко своего видео и выбрал ответ «лицо,
    поднятая правая рука справа, как в зеркале» — превью фронтальной камеры зеркальное.
- Статус: ☑ шаги 1–4 проверены 2026-09-14, шаг 5 — 2026-09-15

### Звонок, принятый без JS, не висит ACTIVE бесконечно
- Коммит: `b10bc69f`
- Почему нужен человек: тесты доказывают правило (`StaleCallPolicyTest`) и
  проводку (`AnswerAdoptionContractTest`). Ответ, который JS не подхватил за
  90 с, отпускается через `onDisconnect`; сообщение JS о соединении снимает
  таймер; при возврате в приложение то же правило проверяется ещё раз. Что
  Telecom действительно завершает такой звонок, отпускает аудиорежим и
  следующий звонок звонит, видно только на аппарате с гарнитурой.
- На чём: реальный Android с Bluetooth-гарнитурой; годится Samsung SM-A528B с
  AirPods Pro 2 ↔ веб TEST1, сборка с FCM (`scratchpad/run-stage3-orphan.sh`).
- Найдено 2026-09-13 при проверке «Освобождение осиротевшего слота не рвёт
  звонок, который сняли параллельно» (`orphan3a`).
  - Звонок A смахнули во время звонка. `releaseOnTaskRemoved` оставляет
    звонящее соединение таймауту ринга, и оно звенело в AirPods.
  - Нажатие на ножку приняло его без JS (`BT - answering call`). `onAnswer` снял
    таймаут ринга, других таймеров у соединения не было.
  - Отбой веба до телефона не дошёл, холодный старт соединение не тронул
    (`COLD_START … otherCallLive=true … actions=[]`). Звонок висел ACTIVE до
    `am force-stop` через 6 минут, следующий входящий получил «занято».
- Шаги:
  1. `adb logcat -v time > /tmp/run.log &`.
  2. Позвонить с веба, не отвечать, смахнуть приложение во время звонка. Пока
     звенит в гарнитуре, принять нажатием на ножку. Приложение не открывать,
     веб кладёт трубку.
     - **Ожидается:** через ~90 с после `onAnswer` в логе
       `Answered call was never picked up by JS — releasing <callId>`, затем
       `onDisconnect`. В `adb shell dumpsys telecom | grep 'Call id=TC@'` нет
       звонка в `state=ACTIVE`, режим звука — `MODE_NORMAL`.
     - **Раньше:** звонок висел `state=ACTIVE` до `am force-stop`, режим
       `MODE_IN_COMMUNICATION`.
  3. После освобождения позвонить с веба ещё раз — телефон звонит, в логе нет
     `reporting busy`.
  4. То же, что шаг 2, но открыть приложение сразу после нажатия на ножку.
     Соединение отпускается тем же сроком: JS не находит звонок, веб уже
     положил трубку. Звонок, пришедший раньше этого срока, получает «занято».
  5. Регресс холодного старта из пуша: убить процесс (`am kill com.forta.chat`),
     позвонить с веба, принять на экране входящего до загрузки приложения и
     разговаривать больше 2 минут. Звонок не рвётся, в логе нет
     `never picked up by JS`.
  6. Регресс при открытом приложении: принять входящий нажатием на ножку и
     разговаривать больше 2 минут — звонок не рвётся.
- Измерено 2026-09-15 (UTC) с владельцем на Samsung SM-A528B (Android 14), сборка
  `044f25f5` с FCM, AirPods Pro 2 (`scratchpad/run-headset-orphan.sh`, логи
  `scratchpad/runs/ho-{release2,reopen1,talkcold1,talkopen2}-*`, время по часам
  телефона).
  - **Шаги 2–3 прошли** (`ho-release2`). Скрипт смахнул приложение во время звонка,
    владелец принял звонок нажатием на ножку, приложение не открывали, веб положил
    трубку через 10 с:
    ```
    03:57:47.972 I/BluetoothInCallService: BT - answering call(false)
    03:57:47.988 D/CallConnection: onAnswer: callId=1789433849914nzJ1eGcjfMn5Z7y9, roomId=!…
    03:59:17.992 W/CallConnection: Answered call was never picked up by JS — releasing 1789433849914nzJ1eGcjfMn5Z7y9
    03:59:17.993 D/CallConnection: onDisconnect: 1789433849914nzJ1eGcjfMn5Z7y9
    03:59:21.033 I/IdleProcessExit: Call over and nothing left to present — ending the process (disconnect 1789433849914nzJ1eGcjfMn5Z7y9)
    03:59:42.911 D/IncomingRinger: arm callId=1789433981221smT8nO91pGA1FYfL
    ```
    Звонок отпущен через 90,0 с после `onAnswer`, затем в Telecom нет живых звонков,
    режим `MODE_NORMAL`. Перезвон поднял новый процесс пушем и зазвонил, `reporting
    busy` нет. Владелец: после нажатия в наушниках, похоже, тишина; второй звонок
    звонил.
  - Первая попытка (`ho-release1`) не в счёт: скрипт считал строки в кольцевом буфере
    `adb logcat -d`, старая строка `arm` вытеснилась, и он сбросил звонящий звонок как
    незазвонивший. Теперь скрипт считает по своему файлу лога.
  - **Шаг 4 прошёл** (`ho-reopen1`, подробности — в записи «Отложенный ответ на один
    звонок не принимает следующий из той же комнаты»). Приложение открыто сразу после
    нажатия, JS ждал звонок до `Timed out waiting for matrixCall`. Соединение отпущено
    тем же сроком — `04:06:10.681 W/CallConnection: Answered call was never picked up by
    JS — releasing 17894342616195kOdJDHdVurwedgx`, через 90,0 с после `onAnswer`
    в 04:04:40.680. Звонок B в 04:05:03 получил «занято». Строка освобождения — из
    параллельного потока `scratchpad/runs/rearwarm1-phone.log`: лог `ho-reopen1`
    закончился в 04:05:45.
  - **Шаг 5 прошёл** (`ho-talkcold1`, строки — в той же записи): процесс поднят пушем,
    «Принять» до загрузки JS, `reportCallConnected` через 8,5 с после `onAnswer`,
    2,5 минуты разговора без `never picked up by JS`, отбой веба — `onDisconnect`
    в 04:13:04.406.
  - **Шаг 6 прошёл** (`ho-talkopen2`): приложение открыто, ответ ножкой, JS подхватил
    звонок через 0,3 с, 2,5 минуты разговора без `never picked up by JS`; владелец
    слышал звук до конца:
    ```
    03:48:56.385 I/BluetoothInCallService: BT - answering call(false)
    03:48:56.409 D/CallConnection: onAnswer: callId=1789433316759339IPRcYQc8yeL5A, roomId=!…
    03:48:56.725 I/Capacitor/Console: [NativeCallBridge] matrixCall ready, answering (matchById=true, matchByRoom=false): 1789433316759339IPRcYQc8yeL5A
    03:48:59.417 V/Capacitor: … methodName: reportCallConnected, methodData: {"callId":"1789433316759339IPRcYQc8yeL5A"}
    03:51:39.305 D/CallConnection: onDisconnect: 1789433316759339IPRcYQc8yeL5A
    ```
  - Попутно в `ho-talkopen2`: один звонок зарегистрирован в Telecom дважды — пушем и
    через 0,1 с из JS (`reportIncomingCall` в 03:48:38.162). Вторую регистрацию (`TC@69`)
    отклонил сервис звонков приложения (`Reason: (duplicate-incoming)`), звонок `TC@68`
    звонил дальше.
- Статус: ☑ шаги 2–6 проверены 2026-09-15

### Процесс со смахнутым звонком не съедает следующий push-звонок
- Коммит: `33868ae2`
- Почему нужен человек: тесты доказывают правило (`IdleProcessExitPolicyTest`)
  и проводку (`IdleProcessExitContractTest`). Когда звонок закончился, процесс
  проверяет себя через 3 с. Если у приложения нет задачи с активити, соединения
  Telecom, звонящего рингера, запущенных сервисов и push-звонка за последние
  10 с, он сбрасывает
  SharedPreferences на диск и завершается. Что отложенное системой убийство
  уходит вместе с процессом и следующий push-звонок звонит, видно только на
  аппарате с FCM.
- На чём: Samsung SM-A528B ↔ веб TEST1, сборка с FCM, экран разблокирован;
  скрипты `scratchpad/run-push-hangup.sh` и `scratchpad/run-push-kill-repro.sh`.
- Найдено 2026-09-10 в шаге 6 записи «Перезвон сразу после звонка доходит и
  звонит» (`push-hangup2-*` → `kill-repro1-*`).
  - Рингер push-звонка смахнули из «недавних», пока Telecom держал звонок.
    Убить процесс сразу система не может и откладывает убийство (`remove
    task`). В Android 14 эту отметку ничего не снимает: она срабатывает при
    следующей смене группы планирования процесса.
  - В `kill-repro1` это был `startActivity` рингера следующего push-звонка:
    `Killing 25771 … remove task` в ту же миллисекунду, через 11 мс после
    `Call invite:`. До Telecom и рингера дело не дошло, звонок не зазвонил.
  - Эксперимент с `excludeFromRecents` пропущен: `RecentTasks.isInVisibleRange`
    в Android 14 оставляет в «недавних» самую свежую задачу, даже исключённую,
    так что рингер на экране по-прежнему можно смахнуть.
- Шаги:
  1. `run-push-hangup.sh ph1`: звонок приходит пушем в мёртвый процесс, рингер
     смахивают во время звонка, веб кладёт трубку.
     - **Ожидается:** через ~3 с после `Ring timeout` в логе `IdleProcessExit:
       Call over and nothing left to present — ending the process (reject
       <callId>)`, затем система пишет о смерти процесса; `adb shell pidof
       com.forta.chat` пуст.
     - **Раньше:** процесс оставался жить без интерфейса.
  2. Сразу после — `run-push-kill-repro.sh kr1`.
     - **Ожидается:** `leftover process: ''`, для callId нового звонка
       `onCreateIncoming=1 arm=1`, телефон звонит.
     - **Раньше:** `Killing … remove task` через 3–14 мс после `Call invite:`,
       звонок не звонил.
  3. Перезвон в окне проверки: повторить шаг 1, но веб звонит снова через 1–2 с
     после `Ring timeout`. **Ожидается:** в логе `staying after reject …` с
     `hasConnection=true` или `recentCallPush=true`, `ending the process` нет,
     телефон звонит.
  4. Регресс, приложение открыто: позвонить с веба, ответить, положить трубку.
     **Ожидается:** в логе `staying after disconnect … uiTaskRunning=true`,
     процесс тот же, приложение не перезагружается.
  5. Регресс, приложение свёрнуто кнопкой Home (не смахнуто): позвонить,
     отклонить на рингере. **Ожидается:** процесс тот же (`uiTaskRunning=true`),
     при открытии приложение не грузится заново.
  6. Регресс меток: убить процесс (`am kill com.forta.chat`), позвонить, нажать
     «Отклонить» на рингере. **Ожидается:** в логе телефона `Pre-rejected
     incoming call` с callId этого звонка, веб получает отказ, `ending the
     process` не раньше этой строки.
  7. Ответ гарнитурой без JS (шаг 2 записи «Звонок, принятый без JS, не висит
     ACTIVE бесконечно»). **Ожидается:** через ~3 с после `Answered call was
     never picked up by JS — releasing` в логе `ending the process`, следующий
     звонок звонит.
- Остаточный риск:
  - Процесс, поднятый пушем, теперь завершается после каждого звонка, у
    которого не осталось интерфейса. Следующий пуш запускает процесс заново —
    это обычный холодный путь push-звонка.
  - Обработанный push-звонок держит процесс 10 с, пока Telecom создаёт
    соединение. Пуш, чей приёмник уже сработал, а сервис FCM ещё не запущен,
    в проверку не попадает: если завершение придётся ровно на эти
    миллисекунды, звонок потеряется.
  - Если Telecom не принял соединение и рингер не поднялся, проверку ничто не
    запускает: помеченный системой процесс может остаться.
- Измерено 2026-09-14 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5` с
  FCM, звонящий — веб `test23438111`. Логи
  `scratchpad/runs/{ph2,pr1,kr2,idle4,idle5,idle6}-*`, время по часам телефона.
  - **Шаг 1 прошёл** дважды (`ph2`, `pr1`). Приложение смахнуто до звонка, звонок
    поднял процесс пушем, рингер смахнут во время звонка, веб положил трубку
    (`ph2`):
    ```
    20:16:37.551 I/ActivityManager: Start proc 11223:com.forta.chat/u0a294 for broadcast {com.forta.chat/com.google.firebase.iid.FirebaseInstanceIdReceiver}
    20:16:37.966 I/FortaPush: Call invite: callId=1789406196233i3LD2lHHvmFEt5l1, …
    20:17:23.118 W/CallConnection: Ring timeout — no answer or decline reached us, rejecting 1789406196233i3LD2lHHvmFEt5l1
    20:17:23.146 I/CallTeardown: endCall reason=REJECT callId=1789406196233i3LD2lHHvmFEt5l1 State(…)
    20:17:26.220 I/IdleProcessExit: Call over and nothing left to present — ending the process (reject 1789406196233i3LD2lHHvmFEt5l1)
    20:17:26.272 I/ActivityManager: Process com.forta.chat (pid 11223) has died: cch+5 CEM (178,1603)
    ```
    В конце `pidof` пуст, `MODE_NORMAL`. В `pr1` так же: `ending the process`
    через 3,1 с после `endCall`.
  - **Шаг 2 прошёл** (`kr2`, сразу после `ph2`): `leftover process: ''`, звонок
    поднял новый процесс 11645, для его callId `onCreateIncoming=1 arm=1
    started-activity=1`, убийств `remove task` нет — телефон зазвонил.
  - Шаг 3 не проверялся.
  - Шаг 4 проверен в варианте со свёрнутым кнопкой Home приложением, а не
    открытым (`idle4`): ответ на рингере, отбой, затем `staying after call
    service destroyed (attempt 0): Snapshot(uiTaskRunning=true,
    hasConnection=false, …)`, процесс 11645 до и после звонка. В строке стоит
    причина последнего из событий, запустивших проверку.
  - **Шаг 5 прошёл** (`idle5`): Home, «Отклонить» на рингере — `endCall
    reason=REJECT`, `staying after call service destroyed … uiTaskRunning=true`,
    процесс тот же.
  - **Шаг 6 прошёл** (`idle6`); процесс убит смахиванием, а не `am kill`:
    ```
    20:23:49.165 I/ActivityManager: Killing 11645:com.forta.chat/u0a294 (adj 900): remove task
    20:24:08.708 I/ActivityManager: Start proc 13840:com.forta.chat/u0a294 for broadcast {com.forta.chat/com.google.firebase.iid.FirebaseInstanceIdReceiver}
    20:24:19.636 D/CallConnection: onReject: callId=1789406647752ePOjTWa13FWAhANT, roomId=!…
    20:24:22.663 D/IdleProcessExit: staying after reject 1789406647752ePOjTWa13FWAhANT (attempt 0): Snapshot(uiTaskRunning=true, …)
    20:24:24.334 I/Capacitor/Console: [call-service] Pre-rejected incoming call, calling reject(): 1789406647752ePOjTWa13FWAhANT
    ```
    Дальше `sendEvent of type m.call.reject`, сервер ответил, у веба соединение
    закрыто. Процесс не завершался: «Отклонить» поднимает `MainActivity`, задача
    жива.
  - Шаг 7 не проверялся — нужны AirPods.
  - Попутно: после автоотклонения по 30-секундному сроку рингер тоже поднимает
    `MainActivity`, и процесс законно остаётся — приложение само появляется на
    экране после пропущенного push-звонка. Так было и до починки.
- Измерено 2026-09-15 (UTC), тот же аппарат и сборка, без владельца. Логи
  `scratchpad/runs/idle-redial{1,2}-*` (`scratchpad/run-idle-redial.sh`): первым
  звонит веб TEST1 и кладёт трубку через 25 с, перезванивает веб `test23438111`.
  - **Шаг 3 прошёл** (`idle-redial2`). Приложение смахнуто, звонок A поднял
    процесс пушем, рингер смахнут во время звонка. Перезвон B пришёл через 0,8 с
    после `Ring timeout`, внутри трёхсекундного окна проверки:
    ```
    01:43:52.836 W/CallConnection: Ring timeout — no answer or decline reached us, rejecting 1789425786001ICmpTJAxvkiHrXuV
    01:43:53.636 I/FortaPush: Call invite: callId=1789425832559rlC6hjWlpoHMvgni, …
    01:43:53.677 D/CallConnectionService: onCreateIncomingConnection: callId=1789425832559rlC6hjWlpoHMvgni, caller=test23438111, roomId=!…
    01:43:53.902 D/IncomingRinger: arm callId=1789425832559rlC6hjWlpoHMvgni
    01:43:55.878 D/IdleProcessExit: staying after reject 1789425786001ICmpTJAxvkiHrXuV (attempt 0): Snapshot(uiTaskRunning=true, hasConnection=true, ringerArmed=true, busyServices=0, recentCallPush=true)
    ```
    `ending the process` нет, процесс 19703 тот же до конца прогона, B звонил до
    30-секундного срока рингера.
  - Первая попытка (`idle-redial1`) окно не задела: перезвон запускался через 1 с
    после `Ring timeout`, а веб тратит ~3,5 с от команды до пуша. Процесс успел
    завершиться (`ending the process` через 3,1 с после `Ring timeout`), пуш B
    поднял новый процесс, и B всё равно зазвонил (`onCreateIncoming=1 arm=1`).
    Во второй попытке перезвон запускался через 43 с после `onCreateIncoming` A:
    `Ring timeout` наступает через 45 с.
  - Попутно в `idle-redial1`: во время звонка B на телефоне нажали кнопки питания
    и громкости. Дважды `silence callId=… (deadline kept)`: звонок затих и
    закрылся по своему 30-секундному сроку.
- Измерено 2026-09-15 (UTC) с владельцем, тот же аппарат и сборка, AirPods Pro 2
  (`scratchpad/run-headset-orphan.sh`, логи `scratchpad/runs/ho-release2-*` и
  `ho-talkopen2-*`).
  - **Шаг 7 прошёл** (`ho-release2`, подробности — в записи «Звонок, принятый без JS,
    не висит ACTIVE бесконечно»). Через 3,0 с после освобождения звонка процесс
    завершился сам, следующий звонок поднял новый процесс и зазвонил:
    ```
    03:59:17.992 W/CallConnection: Answered call was never picked up by JS — releasing 1789433849914nzJ1eGcjfMn5Z7y9
    03:59:21.033 I/IdleProcessExit: Call over and nothing left to present — ending the process (disconnect 1789433849914nzJ1eGcjfMn5Z7y9)
    03:59:21.141 I/ActivityManager: Process com.forta.chat (pid 19703) has died: cch+15 CEM (278,1870)
    03:59:42.092 I/ActivityManager: Start proc 26802:com.forta.chat/u0a294 for broadcast {com.forta.chat/com.google.firebase.iid.FirebaseInstanceIdReceiver}
    03:59:42.911 D/IncomingRinger: arm callId=1789433981221smT8nO91pGA1FYfL
    ```
  - **Шаг 4 прошёл** с открытым приложением (`ho-talkopen2`): звонок принят ножкой, веб
    положил трубку — `03:51:43.731 D/IdleProcessExit: staying after call service
    destroyed (attempt 0): Snapshot(uiTaskRunning=true, hasConnection=false, …)`,
    процесс 19703 до и после звонка. В строке причина последнего триггера: сервис
    звонка остановился примерно через 1,4 с после `onDisconnect` и перезапустил
    3-секундную проверку. Строка из шага дословно есть в `ho-reopen1` (приложение
    открыто, соединение отпущено страховкой): `04:06:13.697 D/IdleProcessExit: staying
    after disconnect 17894342616195kOdJDHdVurwedgx (attempt 0):
    Snapshot(uiTaskRunning=true, …)`.
- Статус: ☑ шаги 1–7 проверены (1, 2, 5, 6 — 2026-09-14; 3, 4, 7 — 2026-09-15)

### Второй звонок из другой комнаты не забирает экран у звонящего
- Коммит: `27541373`
- Почему нужен человек: тесты доказывают правило (`SecondRingPolicyTest`) и
  проводку (`SecondRingContractTest`). Пока рингер звонит для звонка из другой
  комнаты, пуш второго звонка только передаётся в JS: без уведомления, без
  экрана входящего и без регистрации в Telecom. Что экран остаётся у первого
  звонка и «Принять» соединяет с ним, видно только на аппарате с тремя
  аккаунтами.
- На чём: Samsung SM-A528B, веб TEST1 (A) и веб `test23438111` (B), сборка с
  FCM, `scratchpad/run-rebind.sh`.
- Найдено 2026-09-10 в шаге 4 записи «Второй звонок перерисовывает экран
  входящего» (`rebind2`). JS отклонил B через 4 мс (`hasLiveCall` считает живым
  и звонящий A), а пуш через 250 мс перерисовал рингер на B. Telecom не принял
  B, пока A в RINGING, «Принять» никого не соединило бы, и 30-секундный таймер
  A закрыл экран с B через 11 с.
- Решение: звонящий звонок оставляет экран себе, второму звонку JS отказывает,
  как и раньше. Повторный звонок из той же комнаты по-прежнему перерисовывает
  экран: hangup-пуш сюда не приходит, и с убитым JS старый рингер иначе прятал
  бы новый звонок до своего срока.
- Шаги:
  1. `run-rebind.sh rb3`: звонит A, через 8 с после рингера A звонит B.
     - **Ожидается:** в логе `Second call <B> from <комната B> while <A> rings —
       leaving the screen to that call`, строки `onNewIntent: second call` нет,
       на экране по-прежнему `test3823818`, отсчёт не сброшен.
     - **Раньше:** экран перерисовывался на B.
  2. В том же прогоне нажать «Принять» до 30-й секунды A. **Ожидается:**
     соединение с A, звук в обе стороны; веб B получил отказ.
  3. Повторный звонок: A звонит, веб A кладёт трубку и сразу звонит снова (до
     10 с). **Ожидается:** телефон звонит для нового звонка, «Принять»
     соединяет.
  4. С убитым JS (нужен выданный full-screen intent): `am kill com.forta.chat`,
     A звонит пушем, затем B. **Ожидается:** рингер остаётся у A, в логе строка
     из шага 1.
- Остаточный риск:
  - Второй живой звонок из той же комнаты (групповая комната) перерисовывает
    экран, как раньше.
  - Если Telecom не принял звонящий звонок, его комната неизвестна — экран
    перерисовывается, как раньше.
  - С убитым JS отказать второму звонку некому: у звонящего B звонит до своего
    срока.
- Перепроверено 2026-09-15 (01:15–01:22 по часам телефона) на Samsung SM-A528B
  (Android 14), сборка `044f25f5` с FCM. A — веб TEST1 (`test3823818`), B — веб
  третьего аккаунта (`test23438111`). Скрипты `scratchpad/run-second-ring.sh`
  (режимы open и dead) и `scratchpad/run-same-room-redial.sh`, логи
  `scratchpad/runs/sr-open1-*`, `srr1-*`, `sr-dead1-*`.
  - Шаг 1 прошёл (`sr-open1`, приложение открыто):
    ```
    01:16:28.317 I/FortaPush: Second call 1789424187105Y0LS530b1JSbcB9o from <комната B> while 1789424165800hTf3W6AjlGPoURgm rings — leaving the screen to that call
    01:16:28.360 E/CallConnectionService: onCreateIncomingConnectionFailed
    01:16:28.390 I/Capacitor/Console: [call-service] handleIncomingCall: already in call, rejecting
    ```
    Строки `onNewIntent: second call` нет. Экран до конца показывал
    `test3823818` с отсчётом A. Веб B получил отказ: его соединение закрылось
    через 2 с.
  - Шаг 2 в `sr-open1` не проверен из-за стенда. Скрипт заметил рингер A, когда
    до срока оставалась 21 с, и дошёл до «Принять» уже после 30-секундного
    срока A (`no answer in 30s … auto-rejecting`). Тайминг скрипта исправлен.
  - Шаг 3 прошёл (`srr1`). TEST1 отменил звонок через 10 с и перезвонил через
    3 с. Новый звонок получил свой рингер (`arm callId=1789424332005…`, на
    экране отсчёт 27 с). «Принять» ответило на него: `onAnswer` для нового
    звонка — 1, для первого — 0. У веба второе соединение `connected/stable`.
  - Шаг 4 прошёл (`sr-dead1`): приложение смахнуто, процесса нет, оба звонка
    пришли пушем.
    ```
    01:20:51.129 D/IncomingRinger: arm callId=17894244491805a2cR4zQRNNSaFAg
    01:21:03.669 I/FortaPush: Second call 17894244626355bLrpJq0xD0QsV4Z from <комната B> while 17894244491805a2cR4zQRNNSaFAg rings — leaving the screen to that call
    01:21:15.235 D/CallConnection: onAnswer: callId=17894244491805a2cR4zQRNNSaFAg, …
    01:21:15.238 W/CallConnection: onAnswer: JS listener not wired, queued for replay
    ```
    Рингер остался у A: при нажатии «Принять» на экране был `test3823818` с
    отсчётом 7 с. «Принять» соединило A, у веба A `connected/stable`.
  - В `sr-dead1` веб B так и не получил отказ, хотя JS поднялся через 5 с после
    ответа и обработал оба приглашения (`handleIncomingCall` для B и для A).
    Звонок B висел без ответа до конца окна скрипта (60 с) — это остаточный
    риск, описанный выше.
  - **Шаг 2 прошёл** (`sr-audio1`, 01:46–01:48, `scratchpad/run-second-ring-audio.sh`).
    Приложение открыто. A звонит с веба и снимает статистику звука; B звонит, когда
    у A на экране остаётся ~24 с.
    ```
    01:47:05.324 I/FortaPush: Second call 1789426024284Kcqh6YV2zvSJLx8t from <комната B> while 1789426014383vot1YMawURVWKPzf rings — leaving the screen to that call
    01:47:05.355 E/CallConnectionService: onCreateIncomingConnectionFailed
    01:47:05.360 I/Capacitor/Console: [call-service] handleIncomingCall: already in call, rejecting
    01:47:13.612 D/CallConnection: onAnswer: callId=1789426014383vot1YMawURVWKPzf, …
    01:47:13.918 I/Capacitor/Console: [NativeCallBridge] matrixCall ready, answering (matchById=true, matchByRoom=false): 1789426014383vot1YMawURVWKPzf
    01:47:15.940 D/Capacitor/Console: sendEvent of type m.call.answer in <комната A> …
    ```
    - При нажатии «Принять» на экране `test3823818` с отсчётом 13 с, строк
      `onNewIntent: second call` нет.
    - Звук у веба A через 8 и 16 с после ответа: `inEnergy` 0,034 → 0,119,
      получено 10,7 → 25,0 кБ, отправлено 15,0 → 40,3 кБ.
    - Веб B получил отказ: его соединение `closed/closed`. Через 20 с после обоих
      звонков `MODE_NORMAL`.
    - Энергию на стороне телефона из logcat не снять (см. «Микрофон отпускается
      после исходящего звонка без ответа»). Поэтому «звук в обе стороны»
      подтверждён со стороны веба: байты идут в обе стороны, энергия — от телефона
      к вебу.
- Статус: ☑ шаги 1–4 проверены 2026-09-15 на Samsung SM-A528B, сборка `044f25f5`

### Микрофон отпускается после исходящего звонка без ответа
- Коммит: `77402000`
- Почему нужен человек: запись держит нативный модуль звука libwebrtc
  (`WebRtcAudioRecordExternal`), а не WebView. Контракт-тест доказывает только
  порядок вызовов: запись выключается перед `close()` последнего соединения и
  включается снова для каждого нового. Что `setAudioRecording(false)` в самом
  деле останавливает `AudioRecord` у соединения в HAVE_LOCAL_OFFER и что
  следующий звонок не уходит без звука, видно только на аппарате.
- На чём: реальный Android; годится Samsung SM-A528B ↔ веб TEST1
  (`scratchpad/run-mic-leak.sh`, `scratchpad/run-mic-scope.sh`).
- Найдено 2026-09-11 при проверке «Микрофон освобождается после смахивания».
  Исходящий звонок, на который не ответили, оставлял запись включённой в каждом
  прогоне (`micleak1`, `micscope1`, `micscope2`): `RECORD_AUDIO` — `(running)`,
  у pid приложения активная входная дорожка `VOICE_COMMUNICATION` в
  `dumpsys media.audio_flinger`, индикатор микрофона горел без звонка, пока
  приложение не смахнули. В логе `startRecording` сразу после HAVE_LOCAL_OFFER
  и ни одного `stopRecording`. Соединение, дошедшее до STABLE, останавливало
  запись внутри `close()` (`micscope3`).
- Шаги:
  1. Исходящий голосовой звонок с телефона собеседнику, у которого веб закрыт;
     дождаться завершения по таймауту (~60 с). **Ожидается:** в логе
     `Audio recording stopped before close`, следом
     `WebRtcAudioRecordExternal: stopRecording`; через 5 с после закрытия экрана
     звонка `cmd appops get com.forta.chat RECORD_AUDIO` без `(running)`, у pid
     приложения нет активной входной дорожки в `dumpsys media.audio_flinger`.
     **Раньше:** `(running)` до смахивания приложения.
  2. То же, но трубку кладёт телефон через ~10 с. **Ожидается** то же, что в
     шаге 1.
  3. Сразу после шага 1 или 2, не смахивая приложение: входящий звонок с веба.
     **Ожидается:** пока звонит, `RECORD_AUDIO` без `(running)` — соединение
     входящего создаётся уже на ринге, и включение записи на нём не должно
     поднимать микрофон до ответа. Принять — звук в обе стороны (energy > 0 у
     веба и у телефона).
  4. Затем исходящий звонок вебу, веб отвечает. **Ожидается:** звук в обе
     стороны, в логе нет `Could not enable audio recording`. Выключатель записи
     общий для всех соединений: если новое соединение его не подняло, звонок
     уходит без звука.
  5. Регресс отвеченных звонков: `run-mic-scope.sh` с `LEGS="in out"` — звук в
     обе стороны, после завершения запись закрыта.
- Измерено 2026-09-15 (UTC) на Samsung SM-A528B (Android 14), сборка `044f25f5` с
  FCM, собеседник — веб TEST1, приложение открыто. Логи
  `scratchpad/runs/{micleak3,micnext1}-*`, время по часам телефона.
  - **Шаг 1 прошёл** (`micleak3`): веб закрыт, звонок закончился сам через 60 с.
    ```
    01:26:20.967 I/org.webrtc.Logging: WebRtcAudioRecordExternal: startRecording
    01:27:21.214 I/org.webrtc.Logging: WebRtcAudioRecordExternal: stopRecording
    01:27:21.434 I/org.webrtc.Logging: WebRtcAudioRecordExternal: doAudioRecordStateCallback: STOP
    01:27:21.443 D/NativeWebRTCManager: [pc_2_1789424780697] Audio recording stopped before close
    01:27:21.469 D/NativeWebRTCManager: [pc_2_1789424780697] PeerConnection closed (remaining: 0)
    ```
    Строки идут в обратном порядке против шага, и это не расхождение:
    `setAudioRecording(false)` ждёт остановки рекордера, а строка пишется после
    возврата. Сразу после закрытия экрана звонка и ещё 40 с, замер каждые 5 с:
    `RECORD_AUDIO` без `(running)`, `duration=+1m0s459ms` не растёт.
    `dumpsys media.audio_flinger` в этом прогоне не снимался; в `micnext1` тот же
    сценарий дал 0 входных дорожек у pid приложения.
  - **Шаг 2 прошёл** (`micleak3`): телефон положил трубку через 13 с.
    `stopRecording` в 01:28:23.407, `Audio recording stopped before close` в
    01:28:23.616, `PeerConnection closed (remaining: 0)` в 01:28:23.657. 40 с
    `RECORD_AUDIO` без `(running)`, `duration=+12s738ms` не растёт.
  - **Шаг 3 прошёл** (`micnext1`): сразу после звонка без ответа, приложение не
    смахивали.
    - Пока звонило, 5 замеров из 5: `RECORD_AUDIO` без `(running)`, у pid 0
      входных дорожек. Запись включилась только после ответа: `onAnswer` в
      01:31:30.248, `startRecording` в 01:31:31.363.
    - Звук с телефона у веба первые ~24 с шёл тишиной Opus DTX (~85 Б/с, энергия
      около 1e-8), потом настоящий: энергия 2,6e-4 → 4,6e-4, ~1,6 кБ/с. Веб
      отправлял ~3 кБ/с.
    - Энергию на стороне телефона снять не удалось: отчёты `getStats` в logcat
      обрезаны на ~4000 символов, и записи входящего звука в них не попадают.
    - После отбоя 0 входных дорожек сразу, `duration=+50s29ms` 30 с не растёт.
    - Попутно: регистрация входящего пришла дважды за 27 мс, сработала защита
      `Duplicate incoming registration … keeping the connection that already
      rings` (`f83b30e7`).
  - **Шаг 4 прошёл** (`micnext1`): исходящий вебу, веб ответил. Энергия у веба
    0,15 → 0,285 за 20 с, ~2,5 кБ/с. За весь прогон `Could not enable audio
    recording` — 0; `Audio recording stopped before close` и `stopRecording` —
    по 3 на 3 звонка.
  - **Шаг 5 прошёл** (`micscope4`, `LEGS="in out"`).
    - Входящий: ответ на рингере, разговор 1 мин 55 с, трубку положил веб.
      Энергия у веба 0,04 → 1,0. После отбоя 0 входных дорожек сразу,
      `duration=+1m55s116ms` 30 с не растёт.
    - Исходящий: веб ответил, телефон положил трубку через 25 с. Энергия у веба
      0,001 → 0,018. После отбоя 0 входных дорожек сразу, `duration=+24s303ms`
      не растёт.
  - **Попутно — полсекундный повторный старт записи после отбоя**, 1 звонок из 7
    (шаг 4 `micnext1`):
    ```
    01:34:06.959 I/org.webrtc.Logging: WebRtcAudioRecordExternal: stopRecording
    01:34:08.969 E/org.webrtc.Logging: WebRtcAudioRecordExternal: Join of AudioRecordJavaThread timed out
    01:34:09.267 I/org.webrtc.Logging: WebRtcAudioRecordExternal: createAudioRecordOnMOrHigher
    01:34:09.268 D/NativeWebRTCManager: [pc_6_1789425218853] Audio recording stopped before close
    01:34:09.317 V/APM_AudioPolicyManager: startInput input:318, session:9561)
    01:34:09.814 V/APM_AudioPolicyManager: stopInput portId 533
    ```
    - Остановка ждала поток записи 2 с. В ту же миллисекунду, когда освобождение
      закончилось, был создан новый `AudioRecord`, и вход запустился на 0,5 с.
      `appops` это видит: через 5 с после отбоя `duration=+497ms`.
    - Кто вызвал создание, по логу не видно: строки `initRecording(` перед ним нет.
      В остальных 6 звонках остановка не ждала потока и запись не пересоздавалась.
    - Следующие 86 с, до звонка из шага 5: 0 входных дорожек, `RECORD_AUDIO` без
      `(running)`. Созданный объект пролежал до этого звонка, и тот переиспользовал
      сессию 9561 без нового `createAudioRecordOnMOrHigher`; звук у веба был.
    - Мигал ли индикатор микрофона эти полсекунды, не проверялось.
- Статус: ☑ шаги 1–5 проверены 2026-09-15 на Samsung SM-A528B, сборка `044f25f5`;
  попутно — полсекундный повторный старт записи после одного отбоя

### Тыловая камера не зеркалится
- Коммит: `3ac40372`
- Почему нужен человек: эмулятор отдаёт синтетическую камеру — проверяется логика
  выбора, но не картинка. Нужен настоящий объектив.
- На чём: любой реальный Android.
- Шаги:
  1. Видеозвонок, переключиться на заднюю камеру.
  2. Навести на любой текст — он должен читаться нормально, не зеркально.
  3. Переключиться на фронтальную — она **должна** зеркалиться, это селфи-режим.
- Измерено 2026-09-12 на Samsung SM-A528B (Android 14), сборка `f83b30e7` с FCM:
  видеозвонок телефон → веб TEST1 (у веба фейковая камера Chrome), камера
  переключалась через `NativeWebRTC.switchCamera` — тот же `switchCamera()`
  менеджера, что у кнопки. В кадре упаковка с надписью «babyfox»; владелец
  смотрел на экран телефона. Логи и кадры — `scratchpad/runs/rearcam2-*` и
  `rearcam3-*`, остаются локально (в кадре владелец).
  - Шаг 1 прошёл: камера переключается в обе стороны
    (`Camera switched, front: false`, затем `true`).
  - Шаг 2 **провален** на нативном экране звонка. На задней камере надпись в
    окошке своего видео задом наперёд — это видели владелец и снимок экрана.
    `CallActivity.initVideoRenderers` один раз ставит
    `localVideoView.setMirror(true)`, а `flipCamera()` только вызывает
    `switchCamera()`, зеркало не меняется. Починка `3ac40372` меняла выбор
    зеркала только в `CallWindow.vue`, а на Android поверх него открыт нативный
    `CallActivity`. Собеседник получает незеркальную картинку с обеих камер:
    кадры у веба читаются нормально.
  - Шаг 3 прошёл: фронтальная камера зеркалится и до задней, и после неё.
  - Попутно, `rearcam2`: в первом видеозвонке после холодного старта окошко
    своего видео чёрное весь звонок (снимок экрана и глаза владельца). По часам
    телефона `initVideoRenderers: eglBase is null!` в 16:06:14.652, а
    `Initialized with HW acceleration` — только в 16:06:15.304. Экран звонка
    открывается раньше, чем фабрика WebRTC создаёт `EglBase`, а второй вызов
    `initVideoRenderers` есть только в `toggleVideo`. В уже запущенном процессе
    (`rearcam3`) окошко работает.
  - Попутно, `rearcam3`: видео собеседника на телефоне не показывается, на экране
    остаётся заглушка «TE». Кадры 640×360 приходят (`onAddTrack: video`, первый
    кадр отрисован), затем `EglRenderer: remote_video Dropping frame - No surface`.
    Причина не разобрана.
  - Все три дефекта владелец отложил в стадию 7. Починены без владельца
    2026-09-12, у каждого своя запись с шагами проверки:
    - «Превью задней камеры на нативном экране звонка не зеркалится»
      (`2612eaee`);
    - «Своё видео в первом видеозвонке после холодного старта» (`19a2a4cb`);
    - «Видео собеседника показывается на нативном экране звонка» (`1c4ff317`).
- Перепроверено 2026-09-14 (UTC) на той же модели, сборка `044f25f5` с FCM, после
  починки `2612eaee`. Прогоны `rearcold2` и `rearwarm2`, подробности — в записи
  «Превью задней камеры на нативном экране звонка не зеркалится».
  - Шаг 1 прошёл: `Camera switched, front: false`, затем `true`.
  - Шаг 2 прошёл: на задней камере текст в окошке читается нормально — владелец и
    снимки экрана.
  - Шаг 3 прошёл: на фронтальной камере превью зеркальное — владелец и снимок
    экрана после обратного переключения.
- Статус: ✅ проверено 2026-09-14 на Samsung SM-A528B (Android 14), сборка `044f25f5`

### Микрофон освобождается после смахивания приложения из «недавних»
- Коммит: `00ddc636`
- Почему нужен человек: индикатор доступа к микрофону — системный, рисуется вне
  приложения. Юнит-тест доказывает, что `closeAllPeerConnections()` вызывается на
  обоих путях завершения сервиса, но не то, что аудио-HAL действительно отпустил
  захват.
- На чём: реальный аппарат с Android 12+ (у более старых нет индикатора).
- Шаги:
  1. Позвонить, дождаться соединения.
  2. Смахнуть приложение из «недавних» прямо во время разговора.
  3. Индикатор микрофона в статус-баре обязан погаснуть сразу; в
     Настройки → Конфиденциальность → Панель управления Forta не должен
     значиться как использующий микрофон.
  4. Сразу же позвонить снова — звук должен быть в обе стороны (протёкшая
     дорожка отравляла следующий звонок).
- Проверено 2026-09-11 на Samsung SM-A528B (Android 14), сборка `f83b30e7` с
  FCM. Звонит веб TEST1 из той же комнаты, Wi-Fi, телефон разблокирован.
  Скрипт `scratchpad/run-mic-swipe.sh` (жест — `scratchpad/swipe-away.sh`),
  прогоны `micswipe2`, `micswipe3` и `micswipe4`, логи
  `scratchpad/runs/micswipe*-*`. Время в выдержках — часы телефона (UTC+3),
  отметки скрипта — UTC.
  - Шаги 1-2 (`micswipe3`). Телефон принял звонок, у веба `connected/stable`,
    `cmd appops get com.forta.chat RECORD_AUDIO` — `(running)`. Через 16 с
    после ответа приложение смахнуто из «недавних»:
    ```
    18:44:48.501 IncomingCallActivity: Accept pressed
    18:44:48.505 CallConnection: onAnswer: callId=1789141484491OHhZhJ6TG0EJHQxT
    18:45:04.606 HoneySpace.TaskListViewModel: onTaskRemoved : 20851
    18:45:04.710 CallConnection: onDisconnect: 1789141484491OHhZhJ6TG0EJHQxT
    18:45:04.712 CallTeardown: endCall reason=DISCONNECT callId=1789141484491OHhZhJ6TG0EJHQxT
    ```
  - Шаг 3. Первая же проверка, через 1-2 с после смахивания, видит закрытую
    запись: `RECORD_AUDIO … duration=+14s898ms` без `(running)`. Так же при
    каждой следующей проверке до перезвона. В `micswipe2` то же:
    `duration=+50s837ms` через 2 с после смахивания. В `micswipe4` —
    `duration=+16s184ms` через 2 с: это отрезок от `startRecording`
    (19:09:13.124) до `endCall reason=DISCONNECT` (19:09:29.457), то есть
    запись закрылась в момент teardown. Владелец в `micswipe2` и `micswipe3`
    видел, как зелёная точка индикатора гаснет сразу после смахивания. На
    обрезках статус-бара (`micswipe3-bar-talking.png`,
    `micswipe3-bar-after-swipe.png`) точки нет даже в разговоре, поэтому на
    этом Samsung индикатор подтверждает только глаз.
    «Панель конфиденциальности» владелец в настройках Samsung не нашёл. Тот же
    журнал доступа к микрофону открыт интентом
    `android.intent.action.REVIEW_PERMISSION_HISTORY` в 18:50, после прогона.
    Последняя запись Forta Chat — «18:45, 2 минуты»: разговор и перезвон
    одним интервалом, отметки «используется сейчас» нет.
  - Шаг 4 (`micswipe3`, `micswipe4`). Веб кладёт трубку брошенного звонка:
    после смахивания `m.call.hangup` отправить некому, см. «Известное
    ограничение» в записи «Смахивание приложения завершает звонок в Telecom».
    Сразу после этого веб перезванивает. Оба раза звонок пришёл в тот же
    процесс (pid 4430) через 27-30 с после смахивания, JS поднялся заново,
    ответил владелец. `micswipe3`:
    ```
    18:45:31.973 FortaPush: Started IncomingCallActivity for test3823818
    18:45:34.683 IncomingCallActivity: Accept pressed
    18:45:34.909 CallTeardown: endCall reason=COLD_START callId=null State(audioMode=3, otherCallLive=true, …)
    18:45:37.220 NativeWebRTCManager: onTrack: audio
    18:45:38.653 WebRtcAudioTrackExternal: startPlayout
    18:45:38.749 WebRtcAudioRecordExternal: Number of active recording sessions: 0
    18:45:38.749 WebRtcAudioRecordExternal: startRecording
    18:46:04.744 CallTeardown: endCall reason=DISCONNECT callId=1789141530915h3gzOYLCYnIdnAK2
    ```
    Запись нового звонка оба раза стартовала без чужих активных сессий
    (`Number of active recording sessions: 0`). Звук идёт в обе стороны:
    - телефон → веб: у веба за 22 с после соединения входящий поток 22,8 КБ,
      `totalAudioEnergy` выросла с 0 до 0,47 (`micswipe3`, владелец говорил).
      В `micswipe4` за те же 22 с — 13,2 КБ и 0,04;
    - веб → телефон (`micswipe4`, `getStats` в WebView телефона): через ~9 с
      после соединения входящий поток телефона 29,2 КБ при энергии 2,66,
      через ~21 с — 66,4 КБ и 5,93. В первом звонке того же прогона, до
      смахивания, было 30,6 КБ и 2,88.
    После отбоя — `MODE_NORMAL`, запись `RECORD_AUDIO` закрыта:
    `duration=+25s961ms` в `micswipe3` и `+24s521ms` в `micswipe4`. Это
    отрезки от `startRecording` до `endCall` — 26,0 и 24,6 с. Через 40 с
    запись по-прежнему закрыта. В `micswipe3` владелец видел, что точка снова
    погасла. В `micswipe2` перезвон пришёл уже в новый процесс и остался без
    ответа, там шаг 4 не засчитан.
    Попутно: в `micswipe4` звук пошёл через 30 с после «Принять», а в
    `micswipe3` — через 4 с. Холодный старт JS заново логинится в Matrix, и
    сервер ответил на `POST /_matrix/client/v3/login` за 26 103 мс (в
    `micswipe3` — за 503 мс). Приложение в это время ничего не ждало, задержка
    на стороне сервера.
- Статус: ☑ проверено 2026-09-11 на Samsung SM-A528B (шаги 1-4)

### Нативная шторка выбора звука сообщает об отказе
- Коммит: `556c8450`
- Почему нужен человек: тост рисует система, и нужен именно тот момент, когда
  `AudioRouter` ещё (или уже) не запущен — между ответом и первым аудиокадром,
  и после teardown. Контрактный тест доказывает только проводку: что ветка
  отказа есть и что строка переведена.
- На чём: реальный аппарат (проверялось на Samsung SM-A528B).
- Шаги:
  1. Принять звонок и сразу же, до появления «Соединение установлено», открыть
     шторку «Аудио» и выбрать «Динамик».
     **Ожидается:** либо маршрут переключается (звук идёт из динамика), либо
     появляется тост «Не удалось переключить звук. Попробуйте после
     соединения.» — молча шторка закрываться не должна.
     **Раньше:** шторка закрывалась в любом случае, галочка оставалась на
     прежнем устройстве, и пользователь не понимал, нажалось ли вообще (кластер
     O08).
  2. То же самое во время установившегося разговора — там маршрут обязан
     переключиться без тоста.
  3. На русской локали проверить, что тост по-русски.
- Измерено 2026-09-10 на Samsung SM-A528B (лог `scratchpad/runs/audio-143137.log`):
  шаг 2 проходит — три выбора подряд применились (`setDevice: … (pinned)` →
  `setCommunicationDevice(…): true`), шторка закрылась, тоста не было.
  Шаг 1 достать не удалось: роутер поднимается через ~1,5 с после `onAnswer`, а
  самый быстрый тап через adb (`uiautomator dump` + tap) — через ~8 с, окно
  отказа к этому моменту закрыто. Ветка отказа остаётся доказанной только
  тестами.
- Проверено 2026-09-11 на Samsung SM-A528B (Android 14), сборка `f83b30e7` с
  FCM. Звонит веб TEST1 из той же комнаты, Wi-Fi, телефон разблокирован.
  Скрипты `scratchpad/run-early-pick2.sh` (шаг 1) и
  `scratchpad/run-early-speaker3.sh` (шаг 3), логи
  `scratchpad/runs/early-pick2-*` и `scratchpad/runs/early-spk4-*`. Время в
  выдержках — часы телефона (UTC+3).
  - Шаг 1 — ветка «маршрут переключается»: «Динамик» выбран до соединения.
    ```
    01:55:24.548 IncomingCallActivity: Accept pressed
    01:55:25.736 To native: NativeCall, methodName: startAudioRouting
    01:55:25.778 AudioLifecycle: setCommunicationDevice(EARPIECE): true
    01:55:25.929 WindowManager: Changing focus from null to Window{… CallActivity}
    01:55:26.736 WindowManager: Changing focus … to Window{… PopupWindow}
    01:55:26.894 AudioRouter: setDevice: SPEAKER (pinned)
    01:55:26.896 AS.AudioDeviceBroker: setCommunicationRouteForClient … type:speaker
    01:55:26.897 AudioLifecycle: setCommunicationDevice(SPEAKER): true
    01:55:27.716 To native: NativeCall, methodName: reportCallConnected
    ```
    Ветку отказа в этот момент через интерфейс не достать. После
    `call.answer()` JS вызывает `launchCallUI` и `startAudioRouting` с
    разницей в 1 мс, и роутер активен за ~150 мс до того, как экран звонка
    получает фокус. Тап до конца анимации открытия система не доставляет
    (`InputDispatcher: … waiting because NOT_VISIBLE`, прогон `early-pick1`).
  - Шаг 3 — окно после teardown. Шторка открыта в разговоре, веб кладёт
    трубку, «Динамик» нажимает наблюдатель на телефоне по строке
    `stopAudioRouting`:
    ```
    01:49:34.953 To native: NativeCall, methodName: stopAudioRouting
    01:49:34.974 To native: NativeCall, methodName: reportCallEnded
    01:49:34.997 W/AudioRouter: setDevice(SPEAKER) refused: router inactive
    01:49:35.007 I/Toast: show: caller = com.forta.chat.plugins.calls.CallActivity.showAudioRouteSheet$lambda$26:630
    01:49:35.037 WindowManager: Changing focus from Window{… PopupWindow … EXITING} to Window{… CallActivity}
    01:49:35.104 To native: NativeWebRTC, methodName: dismissCallUI
    01:49:35.260 WindowManager: Changing focus from Window{… CallActivity} to null
    ```
    На скриншотах `scratchpad/runs/early-spk4-td1-shot1.png` и `-shot2.png`
    тост по-русски: «Не удалось переключить звук. Попробуйте после
    соединения.» Шторка не закрылась молча: отказ, тост, затем закрытие.
    Окно узкое: от `stopAudioRouting` до ухода `CallActivity` проходит
    40–540 мс (логи `amode-*`), нажатие пришло через 44 мс.
  - Шаг 2 — блок «Измерено» выше, тоста там не было.
- Статус: ☑ проверено 2026-09-11 на Samsung SM-A528B (шаги 1-3)

### Баннер непрочитанного переживает схлопывание записей о звонке
- Коммит: `2d639194`
- Проверено 2026-09-10. Устройство A — Samsung SM-A528B, сборка `f83b30e7`.
  Устройство B — веб-профиль `profile-phone` того же аккаунта
  `Testtest11223344`. Вторая сторона комнаты — веб TEST1. Wi-Fi.
- Скрипты `scratchpad/run-banner-dbl.sh` и `run-banner-dbl2.sh`, логи
  `scratchpad/runs/bannerdbl1-*` и `bannerdbl2-*`. После каждого шага через CDP
  читались:
  - отметка прочтения из Dexie (`lastReadInboundTs`);
  - сырой список событий (`chat.activeMessages`);
  - что из него отрисовано (`[data-message-id]`).
- Отступление от шага 1: звонил TEST1, а телефон отвечал. Чтобы записи
  схлопнулись, трубку клали обе стороны в одно мгновение — по таймерам внутри
  страниц.
- `bannerdbl1` проверяет путь самого фикса.
  - Звонок `1789070888336OyiDyC4olCHDIy5v` оставил три записи: локальное эхо
    своего сброса (отрисовано), сброс TEST1 `$1lPPVYznx5dZ6` (ts
    1789070914641) и серверную копию своего сброса `$bpPffxZ92SV8W`. Две
    последние схлопнуты и не отрисованы.
  - Перед открытием комнаты `lastReadInboundTs = 1789070914641`, то есть
    отметка указывала ровно на схлопнутую запись; `unreadCount: 3`.
  - После открытия разделитель один — «3 unread messages». Под ним ровно три
    новых сообщения, сам он стоит сразу за уцелевшей записью о звонке (скриншот
    `bannerdbl1-phone.png`).
  - Здесь телефон вышел из комнаты после звонка. Его собственная фиксация
    отметки при уходе дала бы то же значение, что и чтение на B, поэтому
    источник отметки этот прогон не различает.
- `bannerdbl2` проверяет, что отметка приходит именно с другого устройства.
  - Телефон вышел из комнаты посреди разговора: `lastReadInboundTs =
    1789070975348`, это последнее сообщение прошлого прогона.
  - После сброса звонка значение не изменилось, а `unreadCount` стал 1.
  - После чтения комнаты на B: `lastReadInboundTs = 1789071303085` — сброс
    TEST1 `$ozQ9ZMQsWinlT`, `unreadCount: 0`. Больше это значение взяться было
    неоткуда: телефон был вне комнаты и в фоне.
  - После трёх сообщений и открытия — «3 unread messages», под разделителем три
    сообщения.
  - Схлопывания здесь не случилось. Таймер на телефоне сработал на ~0,8 с
    позже, звонок к тому времени уже закончился по сбросу с веба, и запись о
    звонке одна.
- Баннер берёт только отметку из Dexie, так что два прогона вместе закрывают
  цепочку. Чтение на B ставит отметку на запись о звонке (`bannerdbl2`), а
  отметка на схлопнутой записи даёт разделитель на уцелевшей (`bannerdbl1`).
- Не засчитаны:
  - `banner1`: на прод forta.chat нет `data-testid` композера (они появились в
    `0d2a8ccd`), сообщения не ушли.
  - `banner2`: комната осталась открытой в свёрнутом приложении, и новые
    сообщения оно отметило прочитанными само, так что разделителя не было. Это
    отдельный дефект. Проба 2026-09-10: приложение в комнате → Home (сверху
    лаунчер) → TEST1 прислал сообщение → через 15 с `lastReadInboundTs` сдвинулся
    на его ts при `unreadCount: 0`.
  - `banner3`: разделитель был, но звонок оставил одну запись, так что путь
    фикса не проверялся.

### Карточка «Как прошёл звонок?» не перекрывает поле ввода в вебе
- Коммит: `278333ca`
- Проверено 2026-09-10 в вебе на `npm run dev`: рабочее дерево на HEAD, в нём
  изменён только `docs/`. Headless Chrome, профиль TEST1, открыт чат с
  `Testtest11223344`. Скрипт `scratchpad/web/card-check.mjs` поднимает
  карточку сниппетом из шага 2 и меряет её рамку относительно композера.
  Замеры — `scratchpad/runs/card1/card-check.json`, там же скриншоты
  `card-1280.png`, `card-1280-closed.png` и `card-700.png`.
- Шаги 1–3, окно 1280×900:
  - карточка внутри дока (`.shrink-0.px-2.pb-2`) и над полем ввода: низ
    карточки на 831 px, строка композера начинается с 840;
  - по горизонтали карточка занимает 328–1272 px при колонке чата 320–1280 и
    иконках композера примерно 340–1260, то есть края совпадают с точностью до
    внутреннего отступа;
  - центр поля ввода кликабелен: `elementFromPoint` попадает в `textarea`;
  - на скриншоте список сообщений не зажат, последний звонок целиком виден над
    карточкой, ✕ читается.
- Шаг 4: ✕ убрал карточку (`cardGone: true`); `[role="dialog"]` на странице
  было 1, стало 0. Формы баг-репорта на скриншоте после закрытия нет.
- Шаг 5, окно 700×900: карточка снова в нижнем оверлее (`inDock: false`),
  шириной 384 px по центру и поверх поля ввода — как мобильная вёрстка до
  фикса.

### «Принять» на чужом рингере не снимает трубку текущего разговора
- Коммит: `675baf71`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14), три стороны: веб TEST1
  (A) и веб `test23438111` (B, другая комната), Wi-Fi. Сборка `f83b30e7` с
  `android/app/google-services.json` (FCM) — отладочная, не release из CI. Логи
  `scratchpad/runs/3way-accept1-*`, скрипт
  `scratchpad/run-3way.sh <run> accept`. Время телефона местное (+03:00).
- Шаг 1: на звонок A ответили кнопкой «Принять» (`onAnswer` в 22:38:06.189), у
  веба A `connected/stable`.
- Шаг 2: пуш B пришёл посреди разговора, рингер B поднялся поверх
  (`Started IncomingCallActivity for test23438111` в 22:38:35.149). Telecom
  отказал обеим регистрациям B, от FCM и от JS. Строки
  `createFailedConnection(BUSY)` в логе нет — отказ виден по строке
  `Incoming call while a call is established — reporting busy`, она есть
  дважды (22:38:35.329 и 22:38:35.394). JS отклонил B по `/sync` ещё раньше, в
  22:38:34.791.
- Шаг 3, «Принять» на рингере B:
  ```
  22:38:49.326 D/IncomingCallActivity: Accept pressed
  22:38:49.358 W/IncomingCallActivity: accept: slot holds 1789069074532fSNz10ZIaXjoEK2D in state 4, not this ringer's 1789069113812kma4xqVWU7ZIDkE7 — answering via JS
  22:38:49.372 I/Capacitor/Console: [NativeCallBridge] Call answered: 1789069113812kma4xqVWU7ZIDkE7 room: !NDxNWedLDBbmFoqirc:matrix.pocketnet.app
  ```
  Разговор с A не прервался и не перезапустился: повторного `onAnswer` для A
  нет, у веба A одно соединение на весь звонок, трафик — 168527 → 203571 байт
  за 10 с после нажатия. JS 30 с ждал звонок B и сдался
  (`Timed out waiting for matrixCall` в 22:39:19.547): B к тому времени уже был
  отклонён. Слот держал настоящий Matrix callId, а не `$…` — на этом
  homeserver пуш теперь несёт callId (см. «Повторная регистрация звонящего
  вызова…»), так что здесь хватило бы и проверки по id.
- Шаг 4: разговор с A завершили кнопкой в приложении
  (`endCall reason=DISCONNECT` в 22:39:05.822), через 30 с `MODE_NORMAL`.
  Перезвон из комнаты A в 22:39:58 — телефон зазвонил
  (`IncomingRinger: arm callId=1789069197903t842sd8JOmd8b7Hy`).
  `matrixCall ready, answering` и `Pre-accepted` для этого звонка нет; он
  закончился, когда веб положил трубку через 20 с.

### Второй входящий не кладёт трубку идущему разговору
- Коммит: `e3079e09`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14), три стороны: веб TEST1
  (A) и веб третьего аккаунта `test23438111` (B), Wi-Fi, сборка `f83b30e7` с
  `android/app/google-services.json` (FCM). Логи
  `scratchpad/runs/3way-decline2-*`, скрипт
  `scratchpad/run-3way.sh <run> decline`. Время телефона местное (+03:00), веба —
  UTC.
- Первый прогон (`3way-decline-*`) не засчитан из-за стенда: телефон ещё не
  вступил в комнату B, приглашение висело, а разговор A закрыл таймер
  веб-скрипта.
- Шаги 1–2: на звонок A ответили кнопкой «Принять» на рингере (`onAnswer` в
  22:34:57.403), у веба A `connected/stable`. B позвонил посреди разговора.
- Шаг 3: разговор с A жив до конца. Входящий трафик у веба A — 176673 → 212536
  байт за 10 с после нажатия на рингере B; до «Завершить» строк завершения A в
  логе нет.
- Шаг 4: обе регистрации B в Telecom, от FCM и от JS, получили отказ:
  ```
  22:35:26.407 D/CallConnectionService: onCreateIncomingConnection: callId=1789068925069fb9QolQPvrtzMl3B, caller=test23438111, …
  22:35:26.408 W/CallConnectionService: Incoming call while a call is established — reporting busy
  22:35:26.446 D/CallConnectionService: onCreateIncomingConnection: callId=1789068925069fb9QolQPvrtzMl3B, caller=test23438111, …
  22:35:26.446 W/CallConnectionService: Incoming call while a call is established — reporting busy
  ```
  Отказ звонящему отправил JS, а не Telecom. Приглашение пришло по `/sync` в
  22:35:25.837, через 21 мс — `handleIncomingCall: already in call, rejecting`
  и `m.call.reject`, пуш пришёл позже, в 22:35:26.198. У веба B
  `onRejectReceived()` через 1,1 с после отправки приглашения, соединение
  `closed/closed` — в пустоту B не звонил.
- Шаг 5: рингер B всё равно поднял FCM. «Отклонить» → в 22:35:40.456
  `decline ignored: slot holds an established call, not this ringer`; разговор
  с A продолжился (шаг 3).
- Шаг 6: «Завершить» на экране звонка в приложении (клик по кнопке через CDP в
  WebView, не пальцем) → `m.call.hangup` в 22:35:56.795 →
  `endCall reason=DISCONNECT callId=1789068885580OYCATbPI7EOtmLYx
  State(audioMode=3, …)` в 22:35:56.841. Через 30 с `dumpsys audio` показал
  `Actual mode = MODE_NORMAL` — это прямое чтение режима вместо ползунка
  громкости.

### Повторная регистрация звонящего вызова не завершает его в JS
- Коммит: `f83b30e7`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, Wi-Fi,
  сборка с этим коммитом и `android/app/google-services.json` (FCM). Логи
  `scratchpad/runs/push-decline-home2-*`, `push-accept-home2-*` и
  `push-decline2-*`, скрипт `scratchpad/run-push-decline.sh` — он же считает
  `onCreateIncoming`, `displaced` (`Displacing a stale connection`) и
  `duplicate-kept` (`Duplicate incoming registration`).
- **До фикса** на том же стенде (`push-decline-home-*`, `push-accept-home-*`):
  при свёрнутом приложении каждый входящий по пушу регистрировался в Telecom
  дважды, вторая регистрация вытесняла первую, и JS получал завершение звонка,
  который ещё звонил:
  ```
  20:54:48.586 D/CallConnectionService: onCreateIncomingConnection: callId=1789062887111e0D8kGYsMSy6q8ZN, …
  20:54:48.600 D/CallConnectionService: onCreateIncomingConnection: callId=1789062887111e0D8kGYsMSy6q8ZN, …
  20:54:48.605 I/CallTeardown: endCall reason=DISCONNECT callId=1789062887111e0D8kGYsMSy6q8ZN …
  20:54:48.632 I/Capacitor/Console: [NativeCallBridge] Call ended natively: 1789062887111e0D8kGYsMSy6q8ZN
  20:54:48.839 D/IncomingRinger: arm callId=1789062887111e0D8kGYsMSy6q8ZN
  ```
  Трубку JS не положил только потому, что `setMatrixCall` в этот момент ещё
  ждал секундную проверку других вкладок.
- Шаги 1–2 (Home, звонок с веба), `push-decline-home2`:
  ```
  21:18:15.421 I/FortaPush: Call invite: callId=1789064294161qSa7ruda43EXqiBj, eventId=$Uze1vCIj…
  21:18:15.575 D/CallConnectionService: onCreateIncomingConnection: callId=1789064294161qSa7ruda43EXqiBj, …
  21:18:15.605 D/CallConnectionService: onCreateIncomingConnection: callId=1789064294161qSa7ruda43EXqiBj, …
  21:18:15.605 W/CallConnectionService: Duplicate incoming registration for 1789064294161qSa7ruda43EXqiBj — keeping the connection that already rings
  21:18:16.255 D/IncomingRinger: arm callId=1789064294161qSa7ruda43EXqiBj
  ```
  Счётчики `onCreateIncoming=2 displaced=0 duplicate-kept=1`; `Call ended
  natively` до нажатия — ни одного. В `push-accept-home2` то же самое:
  регистрации через 22 мс одна за другой, `duplicate-kept=1`, `displaced=0`.
- Шаг 3 («Отклонить»): `onReject` в 21:18:26.434 → `[NativeCallBridge] Call
  declined` через 23 мс → `m.call.reject` ещё через 32 мс; у веба соединение
  закрылось (`closed/closed`). Строк `— ignoring` нет.
- Шаг 4 («Принять», `push-accept-home2`): `onAnswer` в 21:20:15.175 → `Call
  answered` через 21 мс → `reportCallConnected` через 2,5 с; у веба
  `connected/stable`. Единственный `Call ended natively` — в 21:20:48.088,
  после того как веб положил трубку.
- Шаг 5 (приложение смахнуто, JS мёртв), `push-decline2`: одна
  `onCreateIncomingConnection`, счётчики `onCreateIncoming=1 displaced=0
  duplicate-kept=0`. «Отклонить» → `onReject` в 21:22:42.118 → холодный старт
  → `[call-service] Pre-rejected incoming call, calling reject()` и
  `m.call.reject` через 4,0 с; у веба `closed/closed`.

### Запросы приложения идут через Tor на Android
- Коммит: `a9bb63ca`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, Wi-Fi,
  сборка с этим коммитом, Tor «Всегда» через Snowflake. Логи
  `scratchpad/runs/tor-fixed2-*`, `tor-file-*`, `tor-file-download-web.jsonl` и
  `direct-after-tor-*`, скрипты `scratchpad/run-tor2.sh`, `run-tor3.sh`,
  `run-direct-msg.sh` и `web/fetch-file.mjs`. Перед каждым прогоном скрипт
  убеждался, что страница на новом service worker: в ассете есть правка,
  `waiting` пуст. После установки APK новый воркер сначала застрял в
  `waiting`, и прогон проверил бы старый код.
- **До фикса** на том же стенде (логи `tor-*`): Tor поднялся через Snowflake за
  100 с, проверка выхода вернула `isTor: true`, но service worker ронял каждый
  запрос с телом, который пускал через Tor:
  ```
  19:54:21.976 chromium: "Uncaught (in promise) TypeError: Failed to execute 'fetch' on 'WorkerGlobalScope': The `duplex` member must be specified for a request with a streaming body", source: https://localhost/service-worker.js?platform=capacitor&appVersion=dev (0)
  ```
  32 такие ошибки на 36 запросов через Tor: RPC нод pocketnet, Matrix, ping.
  Сообщение с телефона веб за 180 с не получил, звонок не соединился, за
  2,5 минуты через Tor прошло 360 байт. Демон при этом работал — ломался
  только путь запросов приложения.
- Счётчики «Статистика сети» считают только байты **ответов**
  (`responseBuffer.byteLength` в воркере), тело запроса в них не попадает.
- Шаг 1 (Tor «Всегда»): в обоих прогонах статус «Подключён» и проверка выхода
  `isTor: true`; в `tor-file` от записи torrc до `Tor ready` — 17,5 с.
- Шаг 2 (сообщение): веб получил его через 6 с, «Всего, Tor» выросло с 0 до
  43 947 байт при неизменном прямом. Ошибок `duplex` — 0 на 38 запросов через
  Tor.
- Шаг 3 (звонок): соединился через 8 с после набора в `tor-fixed2` и через 7 с
  в `tor-file`. Тост «Звонки идут мимо Tor» к этому фиксу не относится, он
  разобран в записи «Факты ICE и Tor в отчёте».
- Шаг 4 (файл): 256 КБ, меньше 5-мегабайтного порога нативного TorFile, так что
  загрузка идёт через воркер. Воркер спросил маршрут у Tor для самой загрузки,
  и та завершилась:
  ```
  20:25:38.309 Capacitor/Console: FetchHttpApi: --> POST https://matrix.pocketnet.app/_matrix/media/v3/upload?…
  20:25:38.314 Capacitor: callback: 55065632, pluginId: Tor, methodName: isUseWithTor, methodData: {"url":"https:\/\/matrix.pocketnet.app\/_matrix\/media\/v3\/upload?…
  20:25:45.456 Capacitor/Console: FetchHttpApi: <-- POST https://matrix.pocketnet.app/_matrix/media/v3/upload?…
  ```
  За окно загрузки «Всего, Tor» выросло на 4 492 байта, прямой счётчик стоял
  на 43 295 — ответ загрузки пришёл через Tor. Ошибок `duplex` нет. Веб увидел
  файл через 10 с, а скачивание кнопкой в его пузыре (штатный путь: загрузка,
  расшифровка, `<a download>`) дало 262 144 байта без единого расхождения с
  отправленным образцом.
- Шаг 5 (вернуть «Никогда»): сообщение дошло до веба через 5 с, прямой счётчик
  вырос с 55 600 до 58 862 байт, «Всего, Tor» осталось 74 839.

### Исходящий видеозвонок с Android соединяется
- Коммит: `87e8dd1b`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, Wi-Fi,
  сборка с этим коммитом. Логи `scratchpad/runs/video-out3-*`, `video-say-*` и
  `reneg-fixed-*`, скрипты `scratchpad/run-video-out.sh` и `run-reneg-out.sh`.
- **До фикса** на том же стенде (логи `video-out-*`, `video-out2-*`): все 4
  исходящих видеозвонка мёртвые. Прокси стрелял `negotiationneeded` на каждый
  из двух треков, и SDK вслед за invite через 1,2–5,4 с отправлял
  `m.call.negotiate` со вторым offer. Веб, ещё звоня, применял его и отвечал
  сам, а нажатие «Принять» падало:
  `gotCallFeedsForAnswer() failed to create answer: InvalidStateError` — звонок
  у веба завершался. Телефон через 15–16 с после ответа получал ICE `FAILED` и
  клал трубку с `ice_failed`; в одном звонке ICE на 0,2 с дошёл до `CONNECTED`,
  но кадров у веба не было ни в одном. Голосовых это не касалось: один трек —
  одно событие.
- Шаги 1–2, 4 видеозвонка с телефона. В каждом одна
  `addTrack: firing negotiationneeded`, один invite и ни одного
  `m.call.negotiate` до ответа; `ice_failed` — 0:
  ```
  19:14:33.380 Capacitor/Console: [NativeRTCProxy] addTrack: firing negotiationneeded (iceState=new)
  19:14:33.907 Capacitor/Console: sendEvent of type m.call.invite in !XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app …
  19:14:36.846 Capacitor/Console: Call 1789056872277ovCfqMJH4kv4dBaj onAnswerReceived() running …
  ```
  Строки `m.call.negotiate` у веба есть только при загрузке, за 6–7 с до
  первого звонка: `CallEventHandler … discarding possible call event as we
  don't have a call` — это старые события, не эти звонки.
- Шаг 3: у веба ни одной `failed to create answer` и `InvalidStateError`,
  ответы на 4 звонка из 4. За 20 с у веба 262–266 декодированных кадров,
  разрешение до 960×540. У телефона через 12 с после соединения 243–248 кадров
  и 34,7–37,5 КБ звука от веба. Звук с телефона проверен речью: в тихой
  комнате у веба ~100 Б/с (DTX на громкой связи), с `say` рядом с телефоном —
  3 795 Б/с, прирост энергии +0,41. Подробности — в записи «Одна камера на
  исходящий видеозвонок».
- Шаг 4, голосовой звонок с телефона, камера через 13 с после соединения:
  ```
  19:50:50.682 NativeWebRTCManager: [pc_4_1789059029939] startLocalVideo: attached video track
  19:50:51.122 Capacitor/Console: [NativeRTCProxy] addTrack: firing negotiationneeded (iceState=connected)
  19:50:51.161 NativeWebRTCManager: [pc_4_1789059029939] onSignalingChange: HAVE_LOCAL_OFFER
  19:50:51.177 Capacitor/Console: sendEvent of type m.call.negotiate in !XfcsFwyJkEXLRTnPzc:matrix.pocketnet.app …
  19:50:52.852 NativeWebRTCManager: [pc_4_1789059029939] onSignalingChange: STABLE
  ```
  Первый кадр у веба через 0,6 с после `STABLE`, за 30 с 374 кадра, разрешение
  от 320×180 до 960×540. На весь звонок два `negotiationneeded`: при наборе и
  при включении камеры.

### Микрофон не глохнет на исходящем звонке
- Коммит: `01fedeed`
- Проверено 2026-09-10 на Samsung SM-A528B (Android 14) ↔ веб TEST1, сборка
  с этим коммитом. Логи `scratchpad/runs/fx3-out-*` и `fx3-in-*`, скрипты
  `scratchpad/run-fx-out.sh`, `run-fx-in.sh`, окна посчитаны
  `scratchpad/fx-analyze.py`.
- **До фикса** на том же стенде: все 4 исходящих из логов (09-09 14:54 и
  14:57, 09-10 17:31 и 17:37) — `Focus: TRANSIENT_LOSS — muting mic` за
  40–80 мс до `onCreateOutgoingConnection`, у веба ~87 Б/с при энергии ~1e-8
  на весь звонок. Из ~20 отвеченных входящих не замьючен ни один.
- Шаги 1–3, исходящий с телефона:
  ```
  18:24:20.270 WebRTCAudio: Audio focus requested (GAIN), result=1
  18:24:20.797 WebRTCAudio: Audio focus requested (GAIN), result=2
  18:24:21.145 WebRTCAudio: Focus: TRANSIENT_LOSS — muting mic
  18:24:21.224 CallConnectionService: onCreateOutgoingConnection: callId=17890538601591oTGEyh4x8bahK7x
  18:24:21.225 WebRTCAudio: Unmuting the mic a focus loss muted: Telecom took the outgoing call
  ```
  Потеря фокуса по-прежнему приходит раньше соединения, так что на этой
  прошивке работает ветка снятия мьюта, а не `mic stays on`.
- Шаг 4, входящее аудио у веба по секундам (UTC):

  | окно | Б/с | прирост энергии |
  |------|-----|-----------------|
  | до речи, микрофон открыт | 3753 | +0,05 |
  | `say`, микрофон открыт | 3795 | +0,80 |
  | `say` после тапа «Микрофон» (15:24:47) | 0 | +0,00 |
  | `say` после второго тапа (15:25:01) | 4007 | +0,42 |

  Счётчик байт стоял на 62 151 ровно от первого тапа до второго. Телефон к
  45-й секунде отправил 106 204 Б; до фикса было 3 105 Б к 27-й. Первый
  прогон на промежуточной сборке (18:05) дал ту же картину: 0 Б/с под
  мьютом, счётчик стоял на 80 616.
- Шаг 5, входящий от веба: у веба `matrix_ready` до набора, телефон зазвонил через 7 с, ответ
  тапом по `btn_accept`. `Audio focus requested (GAIN), result=2` дважды,
  мьюта нет; у веба 3754–3779 Б/с, энергия растёт на `say`; телефон принял
  52 952 Б при энергии 4,80.
- Не проверено на аппарате: GAIN на звонке без Telecom, где фокус держит само
  приложение, — на стенде не воспроизвести, остаётся за `FocusLossMuteTest`.

### Кнопка приёма не роняет приложение на границе таймаута
- Коммит: `00ddc636`
- Проверено 2026-09-10 на Samsung SM-A528B (лог
  `scratchpad/runs/accept-race-144354.log`, скрипт
  `scratchpad/run-accept-race.sh`).
- **Сначала прогон был мимо.** Запись велела жать на 44–46-й секунде — это
  граница `armRingTimeout`. Но пока экран входящего на виду, первым срабатывает
  собственный 30-секундный дедлайн `IncomingRinger` (`autoDecline` →
  `decline()`), и все три тапа пришлись в активность, которой уже пятнадцать
  секунд как не было: «процесс выжил» без единого нажатия по кнопке. Настоящая
  граница — 30 с; 45 с остаются страховкой для случая, когда рингер не запущен.
- Четыре попытки вокруг неё, тап резолвится по `id/btn_accept` из живой
  иерархии, координаты не запоминаются:

  | # | `arm` | что произошло | Δ |
  |---|-------|---------------|---|
  | 1 | 14:44:30.898 | `onAnswer` 14:45:00.705 | **29,807 с** |
  | 2 | 14:45:45.502 | `onAnswer` 14:46:15.493 | **29,991 с** |
  | 3 | 14:47:02.469 | `Decline pressed` 14:47:32.471 → `onReject` | **30,002 с** |
  | 4 | 14:48:17.481 | `onAnswer` 14:48:47.505 | **30,024 с** |

  Дедлайн взят с обеих сторон: в №2 тап выиграл за 9 мс до него, в №4 ответ
  прошёл через 24 мс **после** номинальных 30 с, в №3 таймер выиграл и тап не
  нашёл кнопки. Во всех четырёх pid не менялся (20787), `FATAL EXCEPTION` нет.
  `[callee-crash-guard] step=connection.onAnswer failed` не появилось ни разу —
  запись допускает эту строку, но не требует: Telecom в этих попытках ни разу
  не бросил, а защита на то и стоит, чтобы бросок не убивал процесс.

### Приложение открывается с первого раза после смахивания во время звонка
- Коммит: `ec716f7c`
- Почему нужен человек: смерть рендерера WebView наступает только после
  системного жеста смахивания на живом аппарате — `onRenderProcessGone` не
  вызывается ни из юнит-теста, ни из сборки. Тесты закрывают таблицу решений
  (`WebViewRecoveryPolicyTest`) и проводку слушателя
  (`WebViewRecoveryContractTest`), но не поведение системы.
- На чём: реальный аппарат. Воспроизводилось на Samsung SM-A528B, Android 14,
  WebView 151.0.7922.199.
- Шаги:
  1. `adb logcat -v time > /tmp/run.log &`.
  2. Позвонить с веба на телефон и **не отвечать** — оставить звонить.
  3. Смахнуть приложение вверх из «недавних», пока оно звонит.
  4. Дождаться `CallConnection onReject` (бэкстоп `armRingTimeout`, ~45 с).
  5. Открыть приложение с иконки **один раз**.
     **Ожидается:** приложение открывается и JS поднимается —
     строки `Capacitor/Console` идут в течение нескольких секунд. В логе
     допустима строка `WebView render process gone … -> DISCARD_STALE`.
     **Раньше:** `chromium: Renderer process (N) crash detected (code -1)`,
     процесс исчезал, ни одной строки `Capacitor/Console`, и приложение
     открывалось только со второго нажатия.
  6. Позвонить с веба ещё раз — телефон должен зазвонить.
- Что считается провалом: `-> LET_SYSTEM_KILL` в этом сценарии (означает, что
  сработала защита от цикла, а не обычное восстановление) или повторяющиеся
  `-> RECREATE_ACTIVITY` подряд.
- Статус: ☑ проверено 2026-09-10 на Samsung SM-A528B. Смахивание во время
  звонка, приложение остаётся закрытым, затем **одно** нажатие на иконку:

  ```
  04:23:22.616 ActivityManagerService.removeTask, taskId = 20801
  04:23:37.310 W/MainActivity: WebView render process gone (didCrash=false)
               -> RECREATE_ACTIVITY
  04:23:38     JS поднялся с ПЕРВОГО нажатия (Capacitor/Console пошли снова)
  ```

  `LET_SYSTEM_KILL` не встретился, повторных `RECREATE_ACTIVITY` нет.

### Осиротевшее соединение не глушит входящий звонок по /sync
- Коммит: `5b26b5db`
- Почему нужен человек: нужно состояние, в котором Telecom держит RINGING-слот,
  а рингера нет ни на экране, ни в звуке. Юнит-тесты закрывают решение
  (`IncomingSurfacePolicyTest`) и проводку (`IncomingCallSurfaceContractTest`),
  но само состояние создаётся только системным смахиванием.
- На чём: реальный аппарат, Samsung SM-A528B.
- Шаги:
  1. Позвонить с веба, не отвечать, смахнуть приложение во время звонка.
  2. Сразу открыть приложение (до истечения 45-секундного бэкстопа).
  3. Позвонить с веба ещё раз, **не дожидаясь** `onReject`.
     **Ожидается:** `ensureIncomingCallVisible: releasing an unpresented slot
     <старый callId>`, затем `onCreateIncomingConnection` с новым callId и
     `IncomingRinger: arm` — телефон звонит. Звонок должен **продолжать**
     звонить: раньше он умирал через 116 мс от `callEnded` предыдущего звонка.
     **Раньше:** `ensureIncomingCallVisible: ringer already up, skip` и тишина
     до конца бэкстопа.
  4. Контроль, что «занято» не сломалось: во время **разговора** позвонить с
     третьего аккаунта — второй звонящий должен получить занято, разговор не
     должен прерваться.
- Статус: ☑ проверено 2026-09-10 на Samsung SM-A528B (шаги 1-3; шаг 4 —
  контроль «занято» — остаётся за третьим аккаунтом, его на стенде нет):

  ```
  04:23:42.169 W/CallPlugin: ensureIncomingCallVisible: releasing an
               unpresented slot 1789003395419Kx1lXi4RapEDWOQL
  04:23:42.169 D/CallConnection: onDisconnect: 1789003395419Kx1lXi4RapEDWOQL
  04:23:42.178 [NativeCallBridge] callEnded names 1789003395419… — ignoring
  04:23:42.229 onCreateIncomingConnection: callId=1789003417314UErm7BsdtQr5e8Va
  04:23:42.475 D/IncomingRinger: arm callId=1789003417314UErm7BsdtQr5e8Va
  04:24:04     звонок всё ещё жив спустя 20 с
  ```

  До правки этот же прогон давал `arm` и через 116 мс `onDisconnect` того же
  callId. Повторено 2026-09-10 на сборке **после** ужесточения гонки (главный
  лупер + `mayReleaseUnpresented`) — освобождение по-прежнему срабатывает, слот
  был RINGING:

  ```
  12:48:19.223 W/CallPlugin: releasing an unpresented slot 17890336720117RNZJYviWfKvNUBe
  12:48:19.236 [NativeCallBridge] callEnded names 17890336720117RNZJYviWfKvNUBe
               in !XfcsFwyJkEXLRTnPzc…, but JS holds … — ignoring
  12:48:19.294 onCreateIncomingConnection: callId=1789033693915hQX5VJmcjoBM5pZP
  12:48:19.543 D/IncomingRinger: arm callId=1789033693915hQX5VJmcjoBM5pZP
  ```

  Ни одного `onDisconnect`/`onReject` по новому callId до конца лога (22 с).

### Смахивание приложения завершает звонок в Telecom, и следующий звонок звонит
- Коммит: `a38e3082` (в паре с `1c994c32` и `f4b76c53`)
- Почему нужен человек: смахивание из «недавних» — системный жест, его нельзя
  выполнить из юнит-теста; состояние соединения живёт в Telecom
  (`dumpsys telecom`), а не в приложении. Контрактные тесты доказывают только,
  что `onTaskRemoved` вызывает `releaseOnTaskRemoved` в нужном месте.
- На чём: реальный аппарат. Воспроизводилось на Samsung SM-A528B, Android 14.
- Шаги:
  1. Запустить `adb logcat -v time > /tmp/run.log &` (именно потоком: `logcat -d`
     выгружает весь буфер и отстаёт на минуту).
  2. Позвонить с веба на телефон, принять на телефоне, поговорить ~7 секунд.
  3. Смахнуть приложение вверх из «недавних».
     **Ожидается** в течение секунды после `removeTask`:
     `CallConnection onDisconnect: <callId>` и
     `CallTeardown: endCall reason=DISCONNECT`. В `adb shell dumpsys telecom`
     соединение должно быть `DISCONNECTED`.
     **Раньше** там не было ни одной из этих строк, а соединение оставалось
     `ACTIVE` до перезапуска процесса.
  4. Подождать ~25 секунд, открыть приложение.
     **Ожидается:** `endCall reason=COLD_START … otherCallLive=false`;
     строки `Pending answer queued, waiting for matrixCall` быть не должно.
  5. Позвонить с веба ещё раз.
     **Ожидается:** `onCreateIncomingConnection` с **новым** callId,
     `IncomingRinger: arm`, `MODE_RINGTONE` — телефон звонит.
     **Не должно быть:** `ensureIncomingCallVisible: ringer already up, skip`,
     `matrixCall ready, answering (matchByRoom=true)`,
     `answerCall: begin` без нажатия на «Принять».
  6. Отдельный прогон: смахнуть приложение **во время звонка**, не отвечая, и
     позвонить снова. Телефон должен зазвонить. Здесь `onTaskRemoved` не
     срабатывает (сервис переднего плана стартует только после ответа), сироту
     подбирает `armRingTimeout` — ожидание до 45 секунд допустимо.
- Известное ограничение, не баг: WebView при смахивании уничтожается, поэтому
  `m.call.hangup` отправить некому — звонящий на вебе будет видеть «соединение»
  до истечения своего invite lifetime (~60 с). Отдельная задача, требует
  нативного сигналинга.
- Статус: ✅ проверено 2026-09-10 на Samsung SM-A528B (Android 14,
  WebView 151.0.7922.199)
- Прогон 1 — основной сценарий (смахивание во время разговора):
  ```
  01:54:59.319 removeTask, taskId = 20781
  01:54:59.648 CallConnection onDisconnect: 1788994481551mCLgX9uZdaj9oo66
  01:54:59.650 CallTeardown endCall reason=DISCONNECT callId=1788994481551…
  01:55:30.726 CallTeardown endCall reason=COLD_START … otherCallLive=false
  01:55:34.806 onCreateIncomingConnection: callId=1788994532677G1ONyGGHydJBlzyO
  01:55:35.018 IncomingRinger: arm callId=1788994532677…
  01:56:05.018 IncomingRinger: no answer in 30s
  ```
  Соединение рвётся через 329 мс после смахивания. Второй звонок позвонил и
  честно истёк по таймауту: `answerCall: begin` в логе ровно один — на первом
  звонке, который приняли руками. Ни `ringer already up`, ни `matchByRoom=true`,
  ни `Pending answer queued` не появились.
- Прогон 2 — шаг 6 (смахивание во время звонка, без ответа):
  ```
  02:15:43.326 IncomingRinger: arm callId=1788995740662TwmRmy7Uz7oJ7TFp
  02:15:47.269 removeTask, taskId = 20789
  02:16:27.907 CallConnection onReject: callId=1788995740662…   ← armRingTimeout
  02:22:46.662 CallTeardown endCall reason=COLD_START … otherCallLive=false
  02:24:02.794 onCreateIncomingConnection: callId=1788996240822beLBbNIIUaq7jGWa
  02:24:03.065 IncomingRinger: arm callId=1788996240822…
  ```
  Бэкстоп — 44,6 с после `arm`; воспроизведён трижды (44,6 / ~41 / ~40 с).
  Слот пуст на COLD_START в том самом процессе, который держал сироту, — то
  есть застрявшее соединение следующий звонок уже не глушит.
- Найдено попутно, **отдельный баг, этим фиксом не закрыт**: если смахнуть
  приложение **во время звонка**, процесс остаётся жить (Telecom держит
  RINGING-соединение), и рендерер WebView успевает умереть. Первое открытие
  приложения после этого гибнет сразу: `Loading app at https://localhost` →
  `App started` → `chromium: Renderer process (N) crash detected (code -1)` →
  процесс исчезает, ни одной строки `Capacitor/Console`. Второе нажатие на
  иконку открывает приложение нормально (JS поднимается за ~3 с).
  Воспроизведено 3 раза из 3; в сценарии с отвеченным звонком, где
  `onTaskRemoved` срабатывает и процесс умирает штатно, этого нет. Пока
  приложение не поднято, входящие по `/sync` не доходят.

- Перепроверено 2026-09-10 на сборке, которая несёт освобождение осиротевшего
  слота и сверку событий по callId: обе правки трогают тот же слот и тот же
  retire меток, поэтому регрессия прогонялась целиком.

  ```
  12:56:14     onAnswer (принято тапом по btn_accept)
  12:56:26.430 ActivityManagerService.removeTask
  12:56:26.821 D/CallConnection: onDisconnect            ← 391 мс
  12:56:26.824 CallTeardown: endCall reason=DISCONNECT callId=…pxgxtIqnl2JkYLG4
  12:56:34.778 CallTeardown: endCall reason=COLD_START … otherCallLive=false
  12:56:44     перезвон: onCreateIncomingConnection + IncomingRinger arm
  ```

  Ни одного `answerCall: begin` без тапа, ни `Pre-accepted`, ни `Pre-rejected`
  — то есть и `d8a52a0d` (перезвон звонит, а не снимается сам) держится.
  Замечание для стенда: тап по «Принять» надо брать из иерархии
  (`uiautomator dump`, `id/btn_accept`), а ждать ответа — по `onAnswer: callId=`;
  паттерн вида `"CallConnection: onAnswer"` не совпадает никогда, потому что
  logcat печатает `D/CallConnection(<pid>):`.

- Перепроверено ещё раз 2026-09-10 на сборке с правкой меток и сверкой событий
  по ключу соединения (лог `scratchpad/runs/regress-145000.log`):

  ```
  14:50:33.973 onAnswer callId=17890410215680q5q4iNOi9oskM8G
  14:50:34.284 [NativeCallBridge] matrixCall ready, answering (matchById=true, matchByRoom=false)
  14:50:46.520 ActivityManagerService.removeTask
  14:50:46.871 onDisconnect                              ← 351 мс
  14:50:46.873 CallTeardown endCall reason=DISCONNECT
  14:50:54.895 CallTeardown endCall reason=COLD_START … otherCallLive=false
  14:51:04.394 IncomingRinger arm callId=1789041062568JQqv2Sl3GBrPbG7Q   ← перезвон звонит
  ```

  Единственный `answerCall: begin` во всём логе — у звонка, который приняли
  тапом, и он пришёл через `matchById=true`: это ровно та ветка, которая теперь
  запоминает ключ соединения, то есть путь пройден на живом аппарате, а не
  только в тестах. У перезвона ни `matrixCall ready`, ни `answerCall: begin`.
